"""E11 Regime engine package (RegimeState v4.0.0; K=9 rule-tree regimes,
softmax classifier with fail-closed validation, EWMA λ=0.94 reference
state, Mahalanobis turbulence, Dirichlet T, Hamilton filter, 3-candle
hysteresis, live-regime gate default OFF)."""
from apex.engines.e11_regime.engine import (  # noqa: F401
    ALPHA_DIRICHLET,
    ANALYST_VERSION,
    BIAS_WEIGHTS,
    CONTRACT_LABEL,
    CONTRACT_VERSION,
    COV_REGULARIZATION,
    D,
    DELAY_BARS,
    E11_DEFAULTS,
    E11RegimeEngine,
    ENGINE,
    EPS,
    EVENT_CATALOG,
    EngineParams,
    HYSTERESIS_BARS,
    K,
    LAMBDA_EWMA,
    LIVE_GATE_OFF_REASON,
    MOMENTUM_CATEGORY_MAP,
    Q_TAGS,
    REGIMES,
    REGIME_STATE_ALLOWED,
    REGIME_STATE_REQUIRED,
    REQUIRED_IC_INPUTS,
    RegimeEngine,
    STATE_ENUM,
    THETA_H,
    TH_TURB_95,
    TH_TURB_99,
    TH_TURB_999,
    TRANSITION_STATUSES,
    VECTOR_KEYS,
    W_180D_H1,
    apply_delayed_labels,
    apply_quality_caps,
    base_rate_quality_cap,
    base_rates_report,
    calibration_report,
    catalog_events,
    cholesky_or_fail,
    compute_bias,
    compute_logits_softmax,
    compute_state_vector,
    deterministic_replay_check,
    e11_snapshot_id,
    entropy_normalized,
    estimate_T_dirichlet,
    ewma_update,
    forecast_next_regime,
    gaussian_log_emission,
    get_params,
    hamilton_filter_step,
    hysteresis_manager,
    load_v3_adapter,
    mahalanobis_turbulence,
    map_momentum_state,
    no_future_leak_check,
    observation_to_candle,
    param_hash,
    quality_score,
    redundancy_report,
    rolling_minmax_norm,
    rolling_sigmoid_norm,
    rule_tree_priority,
    run_engine,
    serialize_state,
    validate_state_schema,
    vector_to_array,
    wilson_ci,
)
