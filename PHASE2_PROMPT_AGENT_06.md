# KICKOFF PROMPT — AGENT-06 — CP-4 CONTEXT & SETUP CHAIN
(Owner: paste as the first message to the 6th coding agent. AGENT-06 only. Entry: CP-1..CP-3 all three CP-3 slots checked+pushed.)

You are AGENT-06 (slot 6 of 10; CP-4: Evidence Fabric → Pattern Intelligence → Setup Engine → Playbook → Strategy Arbitration).

## 0. First actions
1. Clone `https://github.com/sinamoosazadeh/Upstage`, main, pull.
2. Read: PROTOCOL (full), CHECKPOINTS §CP-4 (its SCOPE 1–7 is your work order, item by item), DECISION_LOG §A, HANDOFF_CP4 target file template, HANDOFF_CP2 (§INTERFACES base contract + consumer matrix), HANDOFF_CP3 (all three slot sections — your input surface).
3. Blueprint read set: Ch.8 L14672–14907; Ch.9 L14908–15208; Ch.10 L15209–15476; Ch.11 L15477–15859; Ch.12 L15860–15971. Plus: AI.4 reread (lifecycle you already have hooks for), §2.3 targeted (MTF windows consumption), §9.5 (frozen setup weights), AI.11 consumers rows. You consume E01–E12 as REAL pushed code (import; targeted signature reads allowed; no body archaeology).

## 1. Mission (verbatim scope from CHECKPOINTS §CP-4 — implement all 7 items)
Key exactness reminders (non-exhaustive; the blueprint text rules): four conflict outputs ONLY (CONSENSUS/MATERIAL_CONFLICT/HARD_CONFLICT/INSUFFICIENT_EVIDENCE); permission-monotonicity; seven invariants as assertions; context_confidence = sigmoid(Σ w x) with the six governed default weights and normalization; solvency rule zeroes confidence via FULL evidence removal never zero-substitution; propagation thresholds 0.70/0.40 with EXACT role semantics (blocking / confirmatory-only / excluded); Gate 11 = lineage/snapshot integrity ONLY; Gate 7 H/ln(9)>0.85 ≠ E11 θ_H 0.65; conflict multiplier ×0.6 required-evidence only, redundancy Pearson |ρ|>0.85 over last 48 OK points, <20 skip, tie→higher engine id; relative-MTF index algorithm incl. vacuous-pass and no-coarser-bar→no-emit; all 13 gates + formula universal across 140 cells (no 15m-only variants); family SF_FVG_SWEEP_REV is the ONLY Wave-In family (registry entries beyond it: schema-validated, research-only, admission-gated per AC.3 — no fabricated performance rows, AI.12 "no populate until real backtest" rule applies to your registries); playbook PB_FVG_SWEEP_REV_A single instance universal; 16-field AE.1 template exact; BE/trail/time per X.1–X.4 with exit precedence; X.7 owner-static parameters non-injectable (mark them so in the governance metadata you expose to A09); arbitration = gatekeeper+advisory ranking only, NO-TRADE first-class, never orders/capital/overrides (AF).

## 2. Fixtures & tests duty
Construct `tests/fixtures/gf_sc_01.json` and `gf_sc_02.json` from the Ch.9 spec (12-bar spring / 10-bar upthrust shapes; kind=SYNTHETIC, symbol=SYNTH, tf=6h): you author the bar arrays (source has none), assert the documented pass/fail semantics, compute their SHA-256 with CP-1 hashes module (hash-after-exists rule). GF_SC_03..12: schema-only assertions in the same test file. Mandatory: T-DR-002 (50 synthetic historical setups re-fired identically), gates 1–13 boundary matrix (each gate independently flips QUARANTINED BLOCK), anti-monotonicity probe, thresholds table test, MTF vacuous-pass test, playbook exit-precedence test suite (BE→trail→time under X.4 with regime-dependent distances from X.2), registry governance tests (admission gates block non-approved families from ACTIVE), serialization compat for pattern_evidence/setup_candidate rows.

## 3. Interface publication (CP-5 depends on this — make it exhaustive)
HANDOFF_CP4 §INTERFACES: setup candidate field-for-field (matching Ch.4 DDL), the 13-gate result object, conflict_state + penalties, context_confidence accessor, regime-window gate input for arbitration, playbook management hooks (what MANAGED state may call), arbitration StrategyProposal shape. Traceability: MATRIX CP-4 rows (one per gate, per AC/AD/AE/AF clause, per §8.1 invariant).

## 4. Exit (P16): handoff ≤400 → STATUS CP-4 boxes → issues → `pytest tests/unit tests/integration -q` green → `[CP-4][AGENT-06]` commits → pull --rebase + push → ≤10-line report. RESUME at stage granularity (fabric/pattern/setup/playbook/arbitration).
Begin.
