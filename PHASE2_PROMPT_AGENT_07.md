# KICKOFF PROMPT — AGENT-07 — CP-5 DECISION/RISK/EXECUTION/LEDGER/SCHEDULER
(Owner: paste as the first message to the 7th coding agent. AGENT-07 only. Entry: CP-1..CP-4 checked+pushed. This is the safety core: highest precision demanded.)

You are AGENT-07 (slot 7 of 10; CP-5). Your scope = CHECKPOINTS §CP-5 items 1–8, exactly, complete.

## 0. First actions
1. Clone `https://github.com/sinamoosazadeh/Upstage`, main, pull.
2. Read: PROTOCOL (full), CHECKPOINTS §CP-5, DECISION_LOG §A (esp. ADR-P2-004), HANDOFF_CP4 (all), HANDOFF_CP5 target, HANDOFF_CP1 §INTERFACES (store/bus/config/identity), HANDOFF_CP3 rows for engine fields you consume (targeted signature reads allowed for evidence schemas).
3. Blueprint read set: Ch.13 L15972–16517 (incl. AG.0–AG.5), Ch.14 L16518–16590, Ch.15 L16591–16749, Ch.16 L16750–16936, Ch.19 L17357–17445; Ch.1 L253–297+337–355+400–417; AI.7, AI.8, AI.9, AI.10 rows you own; AI.11 rows E11/E12 fallbacks; §9.5-9/11/12/14; Ch.7 E-EXEC/E-PIT rows; §2.6 targeted (retention hook usage).

## 1. Hard safety rails (all are contract, not preference)
EU math units = multiples of R with the chapter's exact G/L/C/R_penalty definitions and ranking key `(-EU, -P, -C, U_sum, RR)`; eligibility conjunction EXACT (incl. P_min(tf), C_min, data_trust≥0.30, Q_raw≥Q_min(tf), freshness_ok, conflict≠HARD_CONFLICT); P<0.5-calibrated bootstrap ⇒ LIVE blocked via Setup Gates 10/12 (never a workaround — the gate check lives in your code); max_candidates=3; Risk: fourteen vetoes from the Canonical Registry as the SOLE source (summary list is non-authoritative), each independently sufficient, evaluated before any ALLOW; sizing machine line-by-line incl. zero/negative guards, min_quantity floor, Q==0→REJECT PORTFOLIO_CAPACITY; REDUCE correlation path 0.70 cap; ratchet upgrades-only; breakers 10–12 computed from ledger realized+unrealized with time/OWNER-reset only; margin thresholds 60/40/20 with auto-cancel-all at 20% routed through the adapter's five authorized operations; CVaR advisory consumes A09's package (interface per CHECKPOINTS) — advisory NEVER replaces a veto; authority Owner>Risk>Decision>Forecast>Evidence enforced structurally (Decision cannot veto; Forecast cannot permit).
Execution: FSM states/transitions EXACTLY SL-6 (+RECOVERY_REQUIRED), ARMED/TRIGGERED as nested READY substates, no skipping (transition-matrix test); reconcile-first invariant (RECONCILED only on ledger↔exchange agreement; divergence→RECOVERY_REQUIRED); ledger append-only hash-chained single-writer queue (Ch.23) — mutations funnel through it; idempotency key SHA-256(intent_id||order_id||fill_id||cancel_id||timestamp_UTC||nonce) + TTL + duplicate→original-response; order-type table (LIMIT IOC entry, STOP GTC, LIMIT GTC target, no MARKET ever for entry, aggressive flatten LIMIT+priceType=MARKET only per wire rules); retry ×3 backoff with unique key per attempt; adapter maps five operations to five endpoints (Ch.16 wire block verbatim: base api.toobit.com, X-BB-APIKEY, HMAC-SHA256 over query string, recvWindow 5000, symbols map, 1mo→1M, −1120 cell-disable, timeouts/−1006/−1007/−1146/−1147→UNKNOWN+reconcile NO resubmit, −1022 abort, −1003 backoff); forbidden ops validated-and-raised at the client boundary (withdraw/transfer/flashClose/reversePosition/auto_add_margin/hedge/CROSS/unlisted paths/type=MARKET); leverage min(Y.2 TF caps 2/3/4/5, owner, exchange-filter) — 125x never; rollover policy incl. veto 13 + time-exit closes at threshold−1d; stop-gap STOP_GAP_SLIPPAGE attribution + realized-worst-loss feeding next sizing; PROTECTION_FAILED→close+escalate; trade_plan DDL verbatim; scheduler per §9.5-14 with semaphore 4, HTF last-closed, single event loop + bounded pool (Ch.23 execution model), NTP-drift block per E12 rule.
Economic gate: Y.1 five checklist items as state (ECONOMIC_GATE_SIGNED env gates CAPITAL only, never environment existence; Y.4 terminology register — no FX vocabulary).

## 2. Test duty (your exit gate is the biggest in the chain)
T_VETO across all 14 on boundary samples (acceptable vs unacceptable pairs), T-VETO-SIZE guards, T-LR-001..003, T_MATCH, T_RECONCILE, T_LEDGER (covert UPDATE attempt breaks chain), T_ADAPTER_SUBMIT/DUPLICATE/LOST_ACK against a local fake exchange honoring the wire contract (test double only — production code path is the real aiohttp client; P8 no-simulation rule intact), E-EXEC-001 timeout path, RSK-ERR-506 ratchet, FSM illegal-transition matrix, monotonicity suite (T-MON-002 + T_MONOTONE), scheduler order/semaphore/drift tests, full-chain PAPER boot on fixture clock (no Telegram; bus events observable; `tests/integration/test_cp5_runtime_chain.py`), fail-closed drill `test_cp5_fail_closed.py`. Traceability: MATRIX CP-5 rows per veto/formula/wire-row/FSM group.

## 3. Exit (P16, strictest form)
HANDOFF_CP5 (INTERFACES: every symbol A08/A09/A10 will import — Trade Plan shape, veto result, ledger event stream topics+priorities, FSM entry, adapter operations + error map, scheduler entrypoint/stop semantics, ladder-state table, ECONOMIC_GATE read surface, CVaR consumption point) → STATUS CP-5 → issues → suite green → `[CP-5][AGENT-07]` commits → pull --rebase + push → ≤10-line report. RESUME splits: forecast+decision / risk / execution+ledger / scheduler+gate (whole, in that order).
Begin.
