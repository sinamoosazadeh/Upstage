# APEX_GEN5 — PHASE 2 CHECKPOINT CONTRACTS — Rev-3 (CP-1 … CP-8; one isolated stage = one account = one window)

Binding for the owning stage only. ENTRY = you may not write code until every entry item is green (log `LAW-ACK: G1..G20 + P1..P21 read <UTC>` as first handoff STATUS line). EXIT = every box checked with named evidence, handoff complete, board updated, pushed. WRITE SET = paths you may create/modify (plus: your own handoff, your STATUS block, your MATRIX Part-III section, your §B issues in DECISION_LOG). Anything else is read-only. Cross-cutting invariants (all stages): blueprint sole truth · zero deviation/no "simplified" anything · fail-closed over invented behavior · every §9.5 normative-tree file that belongs to you exists, non-empty, import-clean · tests ship with code in the same commit · no secrets · `data/` never committed.

---

## CP-1 — Foundation · Feature Fabric · Engine Base
WRITE: repo root packaging (pyproject.toml, requirements.lock, .gitignore, README skeleton, scripts/run_all_tests.sh) · apex/{__init__,errors,bus,config}.py · apex/identity/×4 · apex/data_catalog/ (contracts, catalog, store/sqlite_store, ingest/toobit_public, 5 tier packages + registry) · apex/quality/×3 · apex/engines/__init__.py + base.py · params/×6 YAML · tests/unit/ + tests/integration/ foundations.
ENTRY: repo contains 16 control files + frozen inputs (sha check §7 master plan); nothing else.
SEQUENCE: skeleton+packaging → config (9 env names exact; no dotenv) → errors (every Ch.7 row) → bus (P0–P3, sync consumer law, backpressure) → identity (canonical_json; single uuid_v7; snapshot envelope; hashes) → store (Ch.4/5 DDL verbatim: every table/index/constraint; migrations mechanism; retention) → catalog+registration (§3.13: unregistered id rejected; 74 rows; tier cache rules) → quality (§2.1 tables copy-exact, §2.2 eps tiers/quantize/guards, §2.3 as-of/MTF) → ingest (public Toobit client, retries, T-OM) → params (six YAMLs, values literal-from-§9.5/Ch.10/§2.1/Ch.16; loader precedence) → base.py (lifecycle, streaming+idempotency contract, emission validation vs 24-field DDL, versioned interfaces, WaveOutError plumbing) → tests (every id in MATRIX Part I CP-1 rows).
EXIT: [ ] all CP-1 tests green via scripts/run_all_tests.sh [ ] import-chain test passes for every tree file created so far [ ] params tests compare YAML literals against blueprint strings [ ] §3.13 74/74 + hooks green [ ] base.py marked FROZEN in docstring; handoff §INTERFACES lists every public API (config keys, error registry access, bus, identity fns, store/cursor semantics, catalog API, quality entry points, base hooks) [ ] board CP-1 boxes checked [ ] MATRIX CP-1 rows filled [ ] HANDOFF_CP1 nine headings ≤400 lines.

## CP-2 — Engines E01 Structure · E02 Liquidity · E03 Volume
WRITE: apex/engines/{e01_structure,e02_liquidity,e03_volume}/** · their params entries (additive keys only if §6 of the chapter demands; else reference CP-1 YAMLs) · their tests+fixtures.
ENTRY: CP-1 EXIT boxes all checked; read HANDOFF_CP1 §INTERFACES; run CP-1 suite green before first change.
RULES: engine file mirrors chapter order (§3 formulas → §4 flows → §5 schema → §6 params → §7 state → §8 validation); full fidelity incl. E02 Merton jump-diffusion + DBSCAN + salience-proximity HTF, E03 all six phases + the four numbered correction blocks, E01 every listed micro-structure metric; every §8 battery clause becomes a test; fixtures `FIX_*` constructed from blueprint examples (re-compute, never copy an "expected" you can't derive); base.py/store/quality are CONSUMED, never patched (need a change → G13 issue, not a commit).
EXIT: [ ] three engines §8 batteries + T-DR-001 + T-E01-001 green [ ] emission rows validate against store DDL [ ] HANDOFF_CP2 §INTERFACES per engine (topics, schema pointer, contract version, params keys, degradation branches) [ ] board + MATRIX CP-2 rows.

## CP-3 — Engines E04 Volatility · E05 FVG · E06 OrderBlock
WRITE/ENTRY/RULES: exactly as CP-2 for `e04_volatility,e05_fvg,e06_orderblock`; E04 ε=1e-12 tier + no adaptive ATR (Wave-Out raise); E05 min-width 0.2 + lifecycle; E06 consumes E01–E05 evidence via base-catalog interfaces (lint test forbids internal re-computation of SMA/ATR paths).
EXIT: three batteries green (T-DR-001 re-run ×3) + HANDOFF_CP3 + board + MATRIX.

## CP-4 — Engines E07 RTM · E08 Wyckoff · E09 Trend
WRITE/RULES: as CP-3 for `e07_rtm,e08_wyckoff,e09_trend`; E07 codes "E12 unavailable → degraded" per blueprint now (integration re-test deferred to CP-5 — record in ledger-note of handoff); E08 encyclopedia ch.2–4 stay Wave-Out with explicit raise; E09 AD-line + multi-TF per spec.
EXIT: three batteries + handoff + board + MATRIX.

## CP-5 — Engines E10 Momentum · E11 Regime · E12 Temporal + integration map
WRITE/RULES: as CP-3 for `e10_momentum,e11_regime,e12_temporal`; E10 four divergence kinds + CONVERGENCE/NONE; E11 K=9 dimensionality proof test (T-E11-K9) + live-gate flag default-off + e11_params_v4.yaml re-assert vs chapter §6 (ADR-P2-008); E12 session windows + §8.8 UTC-boundary rules; ADD E07↔E12 both-mode integration test (degraded branch must now assert it resolves when E12 present); APPEND §CP-5-INTEGRATION-NOTES to PHASE2_HANDOFF_CP5: merged 12-engine topic/version/param map (≤40 lines, dedup of HANDOFF_CP2/3/4 interface blocks).
EXIT: three batteries + E07 re-test green + map appended + handoff + board + MATRIX.

## CP-6 — Context chain + Forecast · Decision · Risk
WRITE: apex/fabric/×3 · apex/pattern/×2 · apex/setup/×2 · apex/playbook/ · apex/forecast/ · apex/decision/ · apex/risk/kernel.py · tests incl. tests/fixtures/gf_sc_01.json + gf_sc_02.json (hand-built bars; re-derived intermediate values per ADR-P2-007; expected-values block committed alongside).
RULES: Ch.8→Ch.15 in six commit groups (fabric → context/conflict → pattern/fibonacci → setup/family/gates → playbook → forecast/decision/risk); 13 gates individually testable with ±1 boundary cases; only Wave-In family; weights/decay/bands verbatim; conflict ×0.6 + ρ>0.85 rules; vacuous-pass assert on combiner; 14 vetoes registry before any sizing; FROZEN_BOOTSTRAP literals asserted from params; arbitration never imports execution (seam test); playbook exit precedence X.4; GF_SC_03..12 schema-only.
EXIT: [ ] MATRIX CP-6 rows incl. per-veto lines [ ] T_VETO ×14, T-DR-002/003, RSK-ERR-506, GF_SC_01/02 fire, gate matrix, seam test, all green [ ] synthetic end-to-end chain (engine evidence → StrategyProposal → sized plan) runs in PAPER with stub execution [ ] HANDOFF_CP6 §INTERFACES = StrategyProposal/trade-plan object + veto-result shape (CP-7 consumes ONLY this) [ ] board + MATRIX.

## CP-7 — Execution · Ledger · Scheduler · Telegram · Alerts
WRITE: apex/execution/×3 · apex/ledger/store.py · apex/scheduler/clock.py · apex/telegram/×2 · alerting per Ch.23 (within telegram or ops-lite per tree) · README run section FINAL · migrations: trade_plan/outcome/ladder-state if not already in CP-1 DDL (log which).
RULES: wire map exact (Ch.16 + SL-6 table; symbol map incl. `1mo→1M`, codes −1120/−1021/−1022/−1003/−2026); FSM legal-transition matrix incl. reconcile-first on boot; adapter = 5 ops, no silent retry, duplicate/lost-ack semantics per AI.3; single writer to ledger (grep-enforced); scheduler order + semaphore 4 + drift block + T_MONOTONE leverage min(); telegram: screens/callbacks/busy-guard/rate buckets/dedup/idempotent updates, Agg backend only, zero display calls, P0 alerts on Ch.23 policy, bot token from env only; E-TELE battery; T_MATCH/RECONCILE/LEDGER/ADAPTER trio; full PAPER loop boot on fixture clock with fake Toobit responder (test-only double, real client for prod).
EXIT: [ ] all CP-7 ids green [ ] README block copy-paste verified in clean sandbox [ ] HANDOFF_CP7 (data-changes: table diffs; how-to-run: full boot) [ ] board + MATRIX.

## CP-8 — Research · Governance · Ops · CLOSEOUT (last stage)
WRITE: apex/research/** (backtest, optimizer —writes to `apex/research/params_suggestions/` only, promotion, adapter-conformance harness, bootstrap runner) · apex/ops/{watchdog,backup}.py · scripts/ harnesses · finalization of `PHASE2_FINAL_REPORT.md` · corrective patches anywhere (bugfixes only, each with a test that failed before/passes after; no redesign).
RULES: Ch.17/18 + Part W/Z/AA capabilities complete (no stubs); red-line suite: any code path that lets research write live params must fail; governance registry 38 + rejected-6 absent; Z.8 numbers re-derived; determinism double-run; T-AD/T-PKG; watchdog heartbeat-loss + Gmail CRITICAL isolation; backup+restore drill (T-RESTORE-001, tempdir); then CLOSEOUT SWEEP (MATRIX Part III CP-8 table): tree conformance vs §9.5 · completeness grep (TODO/FIXME/pass-stub/`NotImplementedError`/`simplified`) · float-in-formula allowlist · single uuid/canonical-json/snapshot impl grep · env names exact · secrets history grep · every AI.10 id + AI.12 directive → row with real test [x] · every open DECISION_LOG issue → CLOSED-with-evidence or ESCALATED-TO-OWNER (verbatim) · full suite executed twice (nondeterminism = finding) · FINAL_REPORT assembled (PROMPT §18 structure + compliance declaration + external-measurement/owner procedure list + honest HARNESS-READY labels).
EXIT: [ ] sweep table complete with commands+results [ ] suite green twice [ ] FINAL_REPORT + HANDOFF_CP8 written [ ] board CP-8 checked; closure line left for owner (the board's only non-agent check).

---
Cross-stage invariants (recap): engine chapters never edit base/registry/store/quality · interface truth = handoffs, bodies are not read wholesale · params values exist ONLY in the six YAMLs (code loads, never hardcodes) · one green commit per module, never a broken HEAD on main · CONTINUE-NEEDED beats an overclaimed COMPLETE, always.
