# APEX_GEN5 — PHASE 2 CHECKPOINT DEFINITIONS (Rev-2; binding per-agent contracts)

Six checkpoints: CP-1..CP-6 (CP-7 = owner board-closure marker in the STATUS file). An agent reads ITS OWN section, plus predecessor handoffs per its prompt. READ SET entries are maximum reading assignments (PROTOCOL P15 / PART G19 govern). WRITE SET entries are the only paths creatable/modifiable (extension points marked ⤷). GATES = exit boxes in `PHASE2_CHECKPOINT_STATUS.md`; all checked ⇒ COMPLETE is lawful. Line ranges in `APEX_GEN5.md`; heading text is canonical if lines drift. Fixture/test naming: `tests/unit/test_<area>_*.py`, `tests/integration/test_cpN_*.py`, `tests/fixtures/e0N_FIX_*.json`, `tests/fixtures/gf_sc_01.json|gf_sc_02.json` (CP-4's). ⤷ = allowed edit of a predecessor file at a named extension point.

---

## CP-1 · AGENT-01 — Foundation + Feature Fabric (+ salvage conformance audit)

ENTRY: CP-0 owner-checked (all PHASE2_* present; board CP-0 rows checked). No predecessor handoff exists.

READ SET: PROTOCOL; this section; DECISION_LOG §A; `PHASE2_SALVAGE.md`; blueprint: 1–1197 (preamble, Ch.1 incl. SBOM/latency/FSMs/quality-gates/PIT-flow/param-deps/risk-ladder, Ch.2 complete), 4104–4158 (GLOBAL IDENTITY/PIT contract), 13529–14382 (§3.12 all 74 entries + §3.13), 14383–14671 (Ch.4, Ch.5, Ch.6 pointer, Ch.7), 18413–18812 (AI.3, AI.4, AI.5, AI.6, AI.8), 18221–18314 (Ch.23 config context only), 20128–20247 (§9.5 directives incl. tree+params+env).

WRITE SET: `pyproject.toml`⤷(extras), `requirements.lock`, `README.md` (run skeleton), `.gitignore`; `apex/{__init__,errors,bus,config}.py`; `apex/identity/{__init__,canonical_json,uuid_v7,snapshot,hashes}.py`; `apex/data_catalog/{__init__,contracts,catalog}.py`, `apex/data_catalog/store/{__init__,sqlite_store}.py`, `apex/data_catalog/ingest/{__init__,toobit_public}.py`, and the §3.12 tier packages `apex/data_catalog/{atomic,molecular,organismic,math,performance}/{__init__ + modules}`; `apex/quality/{__init__,vector,numerical,pit}.py`; `apex/engines/{__init__,base}.py`; `params/{universe_v1,risk_defaults_v1,setup_weights_v1,quality_weights_v1,toobit_wire_v1,e11_params_v4}.yaml`; `tests/unit/*`, `tests/integration/test_cp1_foundations.py`, `tests/integration/test_cp1_salvage_gate.py`; control sections (HANDOFF_CP1, MATRIX CP-1, STATUS CP-1, DECISION_LOG CP-1 + SALVAGE disposition fills).

SCOPE (all binding):
1. SALVAGE AUDIT first (worksheet §A01 rows): clone legacy read-only; per row verify→conform/rewrite→adopt at normative paths; every disposition written into `PHASE2_SALVAGE.md` rows (owner column = you) + a `[CP-1][AGENT-01] salvage(...)` trailer per adopted file; the legacy `apex/candle_intelligence/` must NOT survive as a path (features live under `apex/data_catalog/` tiers per ADR-P2-006); legacy DDL table names replaced by the canonical Ch.4/Ch.5 set; placeholder `SMA_V_20=Decimal("0")` path re-derived from formulas; second UUID implementation eliminated (single `apex/identity/uuid_v7.py`); `.env`-file stdlib parser added; frozen constants relocated to params YAML loaders (⤷`apex/config.py` exposes typed accessors; no magic literals in code).
2. Identity stack exactly per GLOBAL IDENTITY contract + AI.3: canonical_json (sorted keys, separators, Decimal-as-quantized-string, `...Z`, NaN/Inf forbidden → raise), uuid_v7 (RFC 9562), canonical_snapshot_id (single envelope; engine+contract_version+payload), governed_as_of_ms (max availability_time; missing → ValueError), hashes module (payload/source/content ids); identity-separation rule enforced (UUIDv7 never inside canonical payloads).
3. Store per Ch.4+Ch.5 DDL verbatim (all tables incl. `raw_observation`, `bootstrap_progress`, `evidence_event` 24 fields, CHECK constraints, WAL PRAGMAs, indices); append-only enforcement + correction lifecycle (AI.4 half: CLOSED immutable, CORRECTION_EVENT→SUPERSEDED, cascade hook); retention mechanics per AI.5; migration ledger table (own schema, logged per ADR-P2-003); catalog API (`catalog.get(feature_id, symbol, timeframe, as_of, lookback=1)`; statuses OK/MISSING/STALE/UNAVAILABLE/INVALID; future as_of→INVALID; unknown→INVALID; registration API for tiers; engines-never-SQL rule + the build-breaking lint test "get_ohlcv outside apex/data_catalog").
4. Quality per §2.1 (7 components incl. Q_oi weights AVAILABLE=1.0/STALE=0.5; Q_raw; Q_min(tf) copied table from §2.1 into `params/quality_weights_v1.yaml`; 4 independent vetoes; derived measures; dynamic re-evaluation; state machine; failure modes) + §2.2 numerical (`numerical.py`: two-tier ε, rounding/quantize per tables, division guards, signed-zero E-NUM-003) + §2.3 PIT (`pit.py`: as_of semantics, observation windows per consumer, MTF alignment states incl. INSUFFICIENT downgrade rule, multi-symbol scope, snapshot binding, parameter table).
5. Config: nine env names exactly (G16); APEX_ENV ∈ {PAPER,LIVE,RESEARCH,BACKTEST}; SHADOW rejected; secrets never logged/persisted (redaction helper exported for all layers); Toobit public client per Ch.16 wire rules (public endpoints only: time, exchangeInfo, klines 1000, depth, openInterest→MISSING-never-0, fundingRate |rate|≥0.001 alert-only non-gating; −1120 per-cell TF disable; token bucket + 3×backoff 1/2/4s; content_hash dedup); `apex/bus.py` per AI.8/AI.12 (in-process asyncio.Queue; bounded; P0 synchronous; order preserved; no loss on graceful stop); errors registry covering every Ch.7 row + WaveOutError + deterministic reason-code convention for all layers.
6. Features (former CP-2 scope, now yours): 74 entries per §3.12 template into tier modules with canonical ids `APEX.L00.*`, validity windows, decay `exp(-0.02·age)`, lineage, consumers; tier rules (ATOM never cached, MOLECULAR 5, ORGANISMIC 50+/LRU-100); universal guards (H<L→QX INVALID, H−L<ε→DEGRADED, open→CANDIDATE); §3.13 completeness enforced (74==74, F49 out-of-contract absent-by-design, F56 always UNAVAILABLE, unregistered-id build failure); `apex/engines/base.py` frozen contract (streaming on_closed_bar lifecycle, idempotent re-apply, correction/supersede hooks, evidence emission → 24-field DDL shape, Q-tag attach, versioned interface registry + degradation plumbing (I_*_v4 + Q2 fallback), WaveOutError gate helper) — base contains ZERO trading formulas.
7. Packaging: requirements.lock == the nine SBOM pins (Ch.1, verbatim); pyproject `requires-python>=3.10`, extras `tests=["pytest>=7,<9"]` ONLY (remove legacy pytest-asyncio unless genuinely needed → then DECISION_LOG it); README run skeleton per G16 (termux init, pinned install, test cmd, `.env.example` names-only, "Phase-2 progress: CP-1").
8. All engine-visible conventions exported: Q0–QX enum, availability states, environment enum, TF/symbol validators (E-VAL-021/022 semantics).

MANDATORY TESTS: every T-DC/T-PIT/T-ID/T-CL/T-OM/T-RS + T-DR-001 (feature-tier replay) + T-MON-001; registry completeness suite; cache-policy suites; catalog status matrix; canonical-serialization suite (Decimal quantize, NaN reject, key sort); uuid monotonic/format; snapshot single-envelope; params YAML equality vs §9.5 literals (incl. risk defaults 0.005/0.25/3%/6%/4/0.05/0.10/0.25/3/0.70, ISOLATED, ONE_WAY; setup weights sum=1: 0.16/0.12/0.10/0.08/0.12/0.10/0.06/0.06/0.08/0.06/0.04/0.02 + Q_min_setup 0.55; E11: K=9, θ_H 0.65, λ 0.94, hyst 3, α 0.1, delay 48, W 4320); lint tests (get_ohlcv; get_ohlcv-elsewhere; `import dotenv` absence; SHADOW keyword absence outside rejection code); salvage-gate test: `tests/integration/test_cp1_salvage_gate.py` fails while any §A01 salvage row is un-dispositioned.

GATE (boxes): tree files written & importable; DDL verbatim rows in MATRIX Part I (Result col); §3.12/3.13 complete; base.py API frozen & published in HANDOFF_CP1 §INTERFACES; salvage rows dispositioned (adopted rows have real proving tests in this repo); all mandatory suites green; handoff ≤400; STATUS checked; pushed.
DO NOT: implement engines, fabric, anything in CP-2..CP-6.

---

## CP-2 · AGENT-02 — Engines E01–E06 (salvage-conform E01–E04; fresh E05–E06)

ENTRY: CP-1 gate checked; HANDOFF_CP1 exists.
READ SET: PROTOCOL; this section; DECISION_LOG §A; `PHASE2_SALVAGE.md` §A02 rows; HANDOFF_CP1 §INTERFACES (then targeted signature reads of CP-1 modules only if a signature is unclear); blueprint: E01 1205–2789; E02 2790–3871; E03 3872–4954 (skip 4104–4158 body; import identity); E04 4955–5601; E05 5602–6859; E06 6860–8292; AI.2/AI.11 rows for E01–E06; §2.1 Q-tagging + §2.2 targeted; §9.5 directives 2,3,4,9,10; legacy clones limited to the five files listed in your salvage rows (read-only; diffing is your job, not importing).
WRITE SET: `apex/engines/e01_structure/…engine.py(+locals)`, `e02_liquidity/`, `e03_volume/`, `e04_volatility/`, `e05_fvg/`, `e06_orderblock/` (each `__init__.py + engine.py`; engine-local modules ONLY if that engine's §4 architecture demands — named after §4 subsections; logged); `tests/unit/test_e0[1-6]_*.py`; `tests/fixtures/e0[1-6]_FIX_*.json`; ⤷ registration of engine param defaults ONLY through CP-1 catalog/config APIs; control sections (HANDOFF_CP2 with §[STAGE-1 E01–E04] + §[STAGE-2 E05–E06], MATRIX CP-2, STATUS CP-2, DECISION_LOG CP-2).

SCOPE — per engine, §1–§10 exactly (shared law; specifics below):
§1 mission/boundaries (no scope creep); §2 vocabulary; §3 formulas verbatim incl. corrections & edge cases; §4 reference algorithm ported to production (streaming, idempotent, Decimal where required by §2.2, documented complexity); §5 objects/state-machines/events/JSON schemas/versioning `v4.0.0` + snapshot_id single envelope; §6 parameter tables wired as governed keys (no inline magic); §7 encyclopedia → docstring citations only; §8 validation battery FULLY coded (golden fixtures constructed from the §8.1 shape with re-derived values + computed real hashes; deterministic replay byte-identity; no-future-leak probe; ablation; Wilson-CI calibration harness (UNVERIFIED until data exists — harness is real, results honest); redundancy numeric threshold; serialization compat); §9 case studies illustrative → tests only after re-derivation (ADR-P2-007); §10 references cited.
Salvage duty (E01–E04): treat legacy engine files as DRAFT ONLY — the audit found E02 "Simplified VPIN", missing Merton jump-diffusion, missing DBSCAN O(n log n) clustering, missing salience proximity_HTF; E03 run_full façade + missing Phase 67 items (availability_time_ms fail-closed, governed as_of, structured last_error, volume_sma exposure per Phase 80) + Phase 79/88 TF registry alignment; E04 partial estimator set. Re-derive each against the cited blueprint ranges; adopt nothing without clause-level verification; record every legacy-file disposition in SALVAGE §A02.
Engine musts (condensed; blueprint text rules):
- E01: Williams k=2 frozen (dynamic k → WaveOutError); Gann single-fractal closure; pruning depth+age; multi-level BOS + absolute BreakMag; CHoCH four PIT conditions; retest/invalidation/expiry; 0.15×ATR HTF tolerance; S_struct OOS weights table; displacement+volume; stats & Q tagging.
- E02: Q0–QX per its §1.5 complete definition; hierarchical equal-level chain prevention; salience incl. proximity_HTF; 1-D DBSCAN `epsilon=θ_eq·ATR` O(n log n); sweep P1–P5 prerequisites; Merton jump-diffusion hit probability; Cont-OFI + BVC-VPIN exactly per §3.8 (LIVE depth is Wave-Out; implement the documented OHLCV-derived construction only); volume-profile LVN void; PIT reference table.
- E03: PIT as_of via `governed_as_of_ms`; availability_time_ms required; last_error structured classification with fail-closed None; t−1 reference-window rule is statistical-history not global PIT; volume_sma exported (Phase 80); 14-TF incl. 8h (Phase 79/88); zero-volume & OI-stale stresses survive DEGRADED-never-silent; profile complexity bounds per its §4/§5 notes.
- E04: ε=1e-12 (GC-D4); stable ATR per §2.2; full estimator battery per its §3/§4 (no adaptive ATR — WaveOutError hook).
- E05: min_width 0.2 frozen (CP-E05-001 0.25 NOT applied); negative-width fix; BodyRatio>0.1 classification; salience & premium; streaming idempotency; its §8.5 note (golden fixtures engine-canonical).
- E06: 0.55 frozen; zone definition per frozen text (not quarantined 0.65/0.68 or body-vs-range variants); I_Volume_v4 = aligned E03 evidence ONLY (no internal SMA; test-only exception forbidden in engine path — Phase 80/81); I_Volatility_v4 = aligned E04 ATR evidence (Phase 82); structure<4.0 → structural_event=None + Q2; full reference block §4.
Interface duty: HANDOFF_CP2 §INTERFACES publishes, per engine: class path, event types + schema pointers (v4.0.0), params keys, degradation branches coded, fixture paths, §8 battery command — complete enough that AGENT-03 never opens your files.

GATE: all six engines' §8 batteries green; T-E01-001 green; salvage rows dispositioned; interfaces published; handoff stages present (stage-1 pushed before stage-2 begins); STATUS; pushed.

---

## CP-3 · AGENT-03 — Engines E07–E12

ENTRY: CP-2 gate checked; HANDOFF_CP2 (both stage sections).
READ SET: PROTOCOL; this section; DECISION_LOG §A (esp. ADR-P2-008); HANDOFF_CP1 §INTERFACES + HANDOFF_CP2 §INTERFACES; blueprint: E07 8293–9085; E08 9086–9466; E09 9467–10275; E10 10276–11670; E11 11671–12732; E12 12733–13528 + its consumer notes 13520–13528; AI.2/AI.11 rows E07–E12; §9.5 (incl. `params/e11_params_v4.yaml` canonical values); §2.1/§2.2 targeted; §9.5-2/3/4/9/10; Ch.9 15019–15021 (GF_SC schema-compat assertions only — fixtures are CP-4's files).
WRITE SET: `apex/engines/{e07_rtm,e08_wyckoff,e09_trend,e10_momentum,e11_regime,e12_temporal}/`; `tests/unit/test_e0[7-9]_*`, `test_e1[0-2]_*`; `tests/fixtures/e0[7-9]_FIX_*.json`, `e1[0-2]_FIX_*.json`; control sections (HANDOFF_CP3 with §[STAGE-1 E07–E09] + §[STAGE-2 E10–E12] + final §CP-3-INTEGRATION-NOTES; MATRIX CP-3; STATUS CP-3; DECISION_LOG CP-3).
SCOPE: same shared engine law as CP-2 (no legacy salvage here — all six are fresh from blueprint; do NOT reuse the previous repo's absent engines).
Engine musts:
- E07: RTM/ICT combined reading per §2–§4 (breaker blocks, intent classification, streaming+idempotency block); consumes E12 windows via versioned interface — E12 is YOURS too (same slot): wire E07's time-sync input to real E12 output, with the documented QX degradation branch for E12-degraded retained for runtime faults.
- E08: calibrated probabilistic phase machine per its Ch.1 logic (8-phase vector, T_ij transitions, entropy scoring, explicit INVALIDATED/EXPIRED chain, calibrated Cause→Effect); encyclopedia ch.2–4 Wave-Out (reference-only citation); OI missing → volume-only PARTIAL non-blocking; golden 15-case §8.1 battery.
- E09: full trend framework per §3/§4 (V4 complete, PIT-safe); data-gap ≥10×ATR stress → explicit availability-state handling, degrade-never-fabricate.
- E10: momentum/divergence machinery; fixtures cover all four normative divergence kinds + CONVERGENCE + NONE; complexity claims (O(1) EMA/RMA, O(W) OLS, O(k) pivot) respected; DivergenceRecord transport per its §5 (SL-7/SL-8 channels).
- E11: **K=9 classes vs d=8 input dimensions — never conflate** (T-E11-K9 everywhere: logits, probabilities, transition matrices, entropy bounds, schemas, adapters); params load from `params/e11_params_v4.yaml` (θ_H 0.65 forces TRANSITION internally only; Dirichlet α 0.1; hysteresis 3; λ_ewma 0.94; delay 48; W_180d_H1 4320); consumes aligned v4 projections of E09/E10/E04/E03/E02 ONLY; missing/incompatible config → CONFIGURATION_INVALID / FAIL_CLOSED, never uniform/random fallback; next-regime forecast → WaveOutError; live-gate flag (research-only until owner approval per AI.12) implemented default-off.
- E12: UTC window canonicalization exactly per AI.2 rule 1–5 (UTC_W0..W3; derived cores; OVERLAP independent flag [12:30,16:00); ROLLOVER orthogonal event flag; DOW analytic-only; DST-ignore decree; versioned window registry); clock-drift>500ms → DEGRADED + entries-block signal (state emitted; enforcement is runtime layers'); 12-sample §8.1 battery + UTC boundary stability test (§8.8).
After both stages: append §CP-3-INTEGRATION-NOTES (≤40 lines): the merged 12-engine event-topic/version-registry map (from HANDOFF_CP2+CP3, deduplicated) — CP-4's assembly input.
GATE: six §8 batteries green; T-E11-K9; T-E12-Windows; integration notes present; handoff; STATUS; pushed.

---

## CP-4 · AGENT-04 — Context chain: Evidence Fabric → Pattern → Setup → Playbook → Arbitration

ENTRY: CP-3 gate checked. READ SET: PROTOCOL; this section; DECISION_LOG §A; HANDOFF_CP3 §CP-3-INTEGRATION-NOTES + §INTERFACES; HANDOFF_CP2 §INTERFACES (engine field lookups); blueprint: Ch.8 14672–14907; Ch.9 14908–15208 (AC.1–AC.5); Ch.10 15209–15476 (13 gates at 15213; AD.1–AD.8); Ch.11 15477–15859 (AE.1–AE.6; X.1–X.5, X.7 non-injectable metadata; X.8 two-step); Ch.12 15860–15971 (AF); AI.4 reread; §2.3 targeted (MTF/window consumption); §9.5 (setup-weights note); AI.11 consumer rows.
WRITE SET: `apex/fabric/{__init__,evidence,context,conflict}.py`; `apex/pattern/{__init__,detect,fibonacci}.py`; `apex/setup/{__init__,family_sf_fvg_sweep_rev,gates}.py`; `apex/playbook/{__init__,pb_fvg_sweep_rev_a}.py`; `tests/fixtures/gf_sc_01.json`, `gf_sc_02.json`; `tests/unit/test_{fabric,pattern,setup,playbook,arbitration}_*.py`; `tests/integration/test_cp4_context_chain.py`; control sections (HANDOFF_CP4; MATRIX CP-4; STATUS CP-4; DECISION_LOG CP-4).
SCOPE (all seven items of the Rev-1 CP-4 scope, renumbered here — text binding via CHECKPOINTS-history equivalence; summarized to its letter, the blueprint remains authority):
1. Evidence Fabric §8.0 (fabric schema; SL-14 lifecycle CANDIDATE→CONFIRMED→ACTIVE→{MITIGATED,INVALIDATED,EXPIRED,SUPERSEDED} with E03's created/emitted/consumed/expired(5×TF)/superseded crosswalk; read-only-for-engines by construction; lineage to raw).
2. Context Fabric + `context_confidence` combiner exactly (sigmoid(Σw·x); six terms; defaults .35/.20/.15/.15/.10/.05 normalized; solvency: data_trust<0.30 ∨ Q_raw<Q_min ⇒ 0 via FULL removal, never zero-substitute; thresholds 0.70/0.40 roles).
3. Conflict §8.1: four structured outputs only; permission-monotone; seven invariants as assertions; MTF §8.3 states; inter-symbol §8.2 (portfolio constraint only).
4. Pattern Ch.9: hierarchy/detector catalogue (legacy 20 rows locked); firing tolerances; AC governance (extension rows research-only; admission gates as registry validation); `fibonacci.py` in-repo computation (directive 15); GF_SC_01/02 CONSTRUCTED per 15019–15021 (author the bar arrays; hash after exists); GF_SC_03..12 schema-only asserts.
5. Setup Ch.10: thirteen gates exactly (Gate 11 = lineage/snapshot integrity only; Gate 7 H/ln(9)>0.85 ≠ θ_H); frozen weights from YAML (assert sum=1 & each value; Q_min_setup 0.55); conflict multiplier ×0.6; redundancy Pearson |ρ|>0.85 over last 48 OK pts, <20 skip, tie→higher engine id; relative-MTF index algorithm incl. vacuous pass + no-coarser-bar no-emit; universality across 140 cells; `SF_FVG_SWEEP_REV` = only Wave-In family; setup_candidate persistence via Ch.4 DDL).
6. Playbook Ch.11: `PB_FVG_SWEEP_REV_A` universal single instance; 16-field AE.1 template; BE/trail/time per X.1–X.3 with X.4 exit precedence; X.5 dynamic/governed with frozen bootstrap; X.7 owner-static flags non-injectable (metadata to CP-5/6 consumers); X.8 owner-absence two-step state.
7. Arbitration Ch.12: regime-window hard boundary (architectural filter, never score decrement); advisory composite ranking; NO-TRADE first-class; StrategyProposal output contract; never orders/capital/override (AF).
MANDATORY TESTS: T-DR-002; gate 1–13 boundary matrix; anti-monotonicity probes; thresholds table; MTF vacuous-pass; GF_SC fire tests (01 spring, 02 upthrust); registry governance; playbook exit-precedence suite; serialization compat for pattern_evidence/setup_candidate rows; end-to-end synthetic chain test that imports ZERO decision/risk modules (seam proof).
GATE: scope items 1–7 each traceable-row'd; tests green; HANDOFF_CP4 §INTERFACES lists every symbol CP-5 imports (setup candidate field-for-field vs DDL; gate result shape; conflict states; context accessor; regime-window gate input; playbook hooks; StrategyProposal shape; gf paths+hashes); STATUS; pushed.

---

## CP-5 · AGENT-05 — Runtime core → Control plane/Ops → Research/Governance (one role, up to 3 staged sessions)

ENTRY: CP-4 gate checked. The role runs STAGE-1 (runtime core; MUST complete before any other stage), then STAGE-2 (telegram/ops), then STAGE-3 (research/governance); each stage ends with a full exit protocol write into its HANDOFF_CP5 section; a session resumes at the first incomplete stage per the REMAINING WORK LEDGER.
READ SET (per stage; always: PROTOCOL, this section, DECISION_LOG §A, HANDOFF_CP4 §INTERFACES, HANDOFF_CP1 §INTERFACES):
- STAGE-1 blueprint: Ch.13 15972–16517 (incl. AG.0–AG.5); Ch.14 16518–16590; Ch.15 16591–16749; Ch.16 16750–16936; Ch.19 17357–17445; Ch.1 253–297, 337–355, 400–417; AI.7–AI.9 (18679–18906); §9.5 directives 9/11/12/14; Ch.7 E-EXEC/E-PIT rows; AI.11 E11/E12 fallback rows.
- STAGE-2: Ch.21 17562–18086; Ch.23 18221–18314; Ch.1 282–355; §2.5–§2.6 1135–1197; AI.9 reread; AI.13 G-RESTORE/G-PAPER rows; Ch.7 E-TELE rows.
- STAGE-3: Ch.17 16937–17022; Ch.18 17023–17356; Ch.20 17446–17561; Ch.11 15750–15859 (X.6–X.8); AG.4/AG.5 16462–16517 targeted; AI.12 Phase 7/9 + AI.13 G-ADAPTER/G-CAPACITY rows; Ch.1 370–399; §9.5-9.
WRITE SET: STAGE-1: `apex/forecast/{__init__,logistic}.py`; `apex/decision/{__init__,pipeline}.py`; `apex/risk/{__init__,kernel}.py`; `apex/execution/{__init__,fsm,toobit_adapter,toobit_map}.py`; `apex/ledger/{__init__,store}.py`; `apex/scheduler/{__init__,clock}.py`; store ⤷ migrations: `trade_plan` (verbatim Ch.16 DDL), `outcome` (Ch.4), `apex_risk_ladder_state` (ADR-P2-004); `tests/unit/test_{forecast,decision,risk,execution,ledger,scheduler}_*.py`; `tests/integration/test_cp5_runtime_chain.py`, `test_cp5_fail_closed.py`. STAGE-2: `apex/telegram/{__init__,control_plane,signaling}.py`; `apex/ops/{__init__,watchdog,backup}.py`; `scripts/deploy_termux.sh`, `scripts/restore_drill.sh`, `scripts/nfr_harness.py` (logged ADR-P2-005); ⤷ README final run section (extension point granted by CP-1). STAGE-3: `apex/research/{__init__,backtest,walkforward,montecarlo,stress,optimizer,bootstrap,promotion,packages,parameter_governance,liquidity_proxies,legacy_adapters}.py` (logged ADR-P2-005); `tests/unit/test_research_*.py`; `tests/integration/test_cp5_research.py`. All stages: control sections (HANDOFF_CP5 §[RUNTIME]/§[TELEGRAM-OPS]/§[RESEARCH]; MATRIX CP-5; STATUS CP-5 per stage; DECISION_LOG CP-5).
SCOPE: STAGE-1 = Rev-1 CP-5 items 1–8 in full (Forecast Ch.13 incl. bootstrap p_raw=0.5 PAPER/RESEARCH-only + Gates-10/12 LIVE block — never bypassed; U/C formulas; statistical paths; AG bindings registered-not-rederived; Decision EU units/ranking key `(-EU,-P,-C,U_sum,RR)`, eligibility conjunction, max_candidates 3; Risk canonical registry all-14 independently sufficient, sizing machine line-by-line incl. zero-guards, REDUCE 0.70, ratchet, breakers 10–12 from ledger, margin 60/40/20, CVaR advisory consumes stage-3 producer but NEVER replaces vetoes; Execution FSM=SL-6 exact + RECOVERY_REQUIRED, single-writer ledger queue, reconcile-first, idempotency SHA-256 chain key + TTL + duplicate-returns-original, order-type/TIF/retry table, Toobit adapter five ops + wire map verbatim (−1120, −1006/−1007/−1146/−1147→UNKNOWN no-resubmit, −1022 abort, −1003 backoff; forbidden ops raised at client boundary; leverage min(Y.2,owner,exchange); never 125x), trade_plan persistence, rollover + stop-gap handling; scheduler §9.5-14 (order, semaphore 4, HTF last-closed, E12 drift block, bounded pool); Economic gate Y.1–Y.4 state machine. STAGE-2 = Rev-1 CP-6a items 1–6 (screens/wizard/callbacks/busy-guard; signaling P0–P3 with idempotency key + TTL + limits 4096/1024/8×4 + bucket 20/20 3×backoff; aiogram 3.7.0; Agg charts in-memory; OWNER/USER-only; watchdog heartbeat/60s + 3-miss reduce-only/cancel-all via the five adapter ops + Gmail isolation module; alert policy table verbatim + dedup 30min exceptions; backup hourly+on-FSM-transition encrypted hash-verified + restore drill + T-RESTORE-001; startup reconciliation wiring; README completion). STAGE-3 = Rev-1 CP-6b items 1–8 (governance service classes/change-proposal/versioned packages/drift watch; YAML immutability watch; backtest via the CP-5 runtime code path — no parallel engine; walk-forward+stress §18.4 named cases; MC weekly CVaR_95 producer; W.1–W.9 dual optimizer incl. RED-LINE validator pre-paper + nightly window + continuous flag; W.6 bootstrap P1–P3 vs bootstrap_progress + DEEP config + pause/resume; Z.1–Z.9 promotion (Wilson gate exact, floor accumulation, shrinkage, SPRT auto-halt + owner-confirm demotion, UNPOOLED never promotable); AA 38-concept registry + 6 rejected absent + degrade-UNAVAILABLE rules; legacy adapter interfaces + conformance harness per ADR-P2-009 (real-legacy-data gate stays OPEN); T-PKG-001 idempotent version-locked injection).
MANDATORY TESTS per stage: STAGE-1: T-DR-003; T_VETO ×14 boundaries; T-VETO-SIZE; T-MON-002 + T_MONOTONE; T-LR-001..003; T_MATCH; T_RECONCILE; T_LEDGER; T_ADAPTER_SUBMIT/DUPLICATE/LOST_ACK (fake-HTTP test double; production = aiohttp); E-EXEC-001; RSK-ERR-506; FSM illegal-transition matrix; leverage table; scheduler suite; PAPER full-loop boot on fixture clock without Telegram. STAGE-2: T-FB-001..003; T-NFR-004; E-TELE-*; callback completeness introspection; watchdog heartbeat-loss; dedup; backup/restore tempdir drill; Agg-no-display; secrets-leak grep on message builders. STAGE-3: T-AD-001/002; T-PKG-001; red-line rejection suite; Z.8 worked example re-derived; determinism double-run; bootstrap pause/resume; CVaR boundary; registry-38 + rejected-absent greps; YAML-immutability watch; 10-day replay-equivalence harness (G-PAPER-001 semantics; honest labels).
GATE: each stage closes with handoff section + STATUS boxes + traceability + green suite + push; role completes only when all three stage sections are present and the last session writes HOW-TO-RUN covering the full boot (stages roll up); CP-5 box checked = all three.
DO NOT: refactor other roles' code (report defects to DECISION_LOG for CP-6); invent new handlers/screens/optimizer objectives/families.

---

## CP-6 · AGENT-06 — Independent integration audit & release verification (fresh context; run on reused account #1/#2)

ENTRY: CP-1..CP-5 all checked & pushed. Exception right: any unchecked predecessor gate ⇒ your first deliverable is a REOPEN report, no code.
READ SET: PROTOCOL; this section; DECISION_LOG (full); ALL HANDOFF_CP1..CP5 + STATUS + MATRIX Parts I–II; blueprint: Ch.24 18315–19211 FULL; §9.9 19212–19400; §9.5 20128–20247; Ch.7; then — uniquely to you — broad repository read rights (`apex/**`, `tests/**`, `params/**`, packaging, README, .gitignore) and targeted re-reads of any blueprint passage an audit finding needs.
WRITE SET: `tests/integration/test_cp6_release_gates.py` (unified gate runner); `PHASE2_FINAL_REPORT.md`; completion/fix rows in ALL control files' audit-owned sections (MATRIX Part I/II Results, STATUS CP-6 + reconciliation, DECISION_LOG §B/CP-7-triage); corrective patches ONLY (format `[CP-6][AGENT-06] fix AGENT-nn: <what>`); NEVER new subsystems, never redesigns (bigger findings ⇒ reopen the CP on the board with a 3-line finding and stop for owner).
SCOPE (the no-holes guarantee; each produces written evidence):
1. Tree conformance: every §9.5 file present, non-empty, importable; renames/deletions absent per `git log --name-status`; every additive file ↔ DECISION_LOG/SALVAGE row (incl. A01/A02 salvage dispositions and the legacy-absent-paths check: no `apex/candle_intelligence`, no pytest-asyncio unless logged).
2. Full-suite execution: `pytest tests/unit tests/integration -q` + your gate runner encoding AI.10's six fail-closed release rules + the cross-checkpoint invariants list (tail of this file).
3. Matrix completion: every AI.10 T-id and every AI.12 acceptance item ↔ real test + recorded result (Part II); Part I rows Result-filled; anything missing = owning-CP defect → reopen, or trivial-fix yourself.
4. Completeness & fidelity sweep (grep/AST, findings logged): zero TODO/FIXME/NotImplementedError/stub-`pass`; no mocks in production imports; deterministic time/random discipline; NaN/Inf guards; float-money audit; catalog-only access lint still green; duplicate identity-utility scan; Wave-Out keyword scan (withdraw, transfer, flashClose, reversePosition, hedge, CROSS, SHADOW, postgres, numba, networked bus) absent from runtime paths; requirements.lock == nine pins; env-name set exact; single ledger writer (one instantiation site); E06 no internal SMA/ATR; E11 K=9 everywhere + live-gate default-off; E12 windows canonical; single snapshot envelope; fixture hashes = real digests; params YAML == §9.5 literals; §9.5-11 numbers asserted in tests; bootstrap p=0.5 + Gates-10/12 LIVE block; leverage min() never 125; FSM == SL-6; `.gitignore` per ADR-P2-013; secrets scan across FULL git history (`.git log -p` grep): key-shaped material = CRITICAL finding + rotation notice per §2.5, never silent.
5. §9.9 Execution Readiness Checklists, checkbox-by-checkbox against real test results (owner-side Phase-0 items = OWNER-PENDING with the delivered harness named).
6. Gates ledger: each AI.13 G-* → harness path + sandbox execution label (`SANDBOX-PARTIAL` max) + owner procedure text; statuses only OPEN/CLOSED/BLOCKED per ADR-P2-009/010; LIVE_ELIGIBLE & PUBLIC_RELEASE_READY structurally OPEN.
7. DECISION_LOG triage: every OPEN issue CLOSED-with-evidence or ESCALATED-TO-OWNER verbatim; false-green CP = blunt reopen entry.
8. `PHASE2_FINAL_REPORT.md` assembled per PROMPT §18 structure (its nine sections) with the compliance declaration checked only where Parts I–III + executed tests prove; append the owner action list (the six external measurements + ECONOMIC_GATE checklist + escalations).
9. Exit: STATUS CP-6 boxes + CP-7 marker row prepared (owner checks CP-7); pull --rebase; push; ≤10-line chat report.
GATE: board reconciled; every test id dispositioned; report complete; corrective commits all `[CP-6]`; no feature diffs (`git diff --stat` proof: only tests/report/patches).

---

## Cross-checkpoint invariants (every agent asserts in their tests; CP-6 re-runs repo-wide)

1. `catalog.get` only; SQL confined to `apex/data_catalog/store`. 2. One uuid_v7, one canonical_json, one snapshot-id formula. 3. Fail-closed everywhere; unspecified ⇒ refuse+record. 4. Wave-Out list is law (G6). 5. Frozen numbers unretuned (§9.5-11, Ch.10 weights, θ_H, k=2, 0.2/0.55, caps, leverage 2/3/4/5). 6. Decimal/Text money; NaN/Inf never stored. 7. Ledger append-only, hash-chained, single writer. 8. No SHADOW/ADMIN/withdraw/transfer/MARKET-entry. 9. 140-cell universality + membership lints. 10. Same inputs+code_revision ⇒ byte-identical outputs.
