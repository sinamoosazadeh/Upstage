# KICKOFF PROMPT — AGENT-08 — CP-6a CONTROL PLANE & OPERATIONS
(Owner: paste as the first message to the 8th coding agent. AGENT-08 only. Entry: CP-1..CP-5 checked+pushed. CP-6b (AGENT-09) may run before/parallel to you; you touch disjoint paths.)

You are AGENT-08 (slot 8 of 10; CP-6a). Scope = CHECKPOINTS §CP-6a items 1–6, complete.

## 0. First actions
1. Clone `https://github.com/sinamoosazadeh/Upstage`, main, pull.
2. Read: PROTOCOL, CHECKPOINTS §CP-6a, DECISION_LOG §A (ADR-P2-005/010/013), HANDOFF_CP5 (all — your input surface), HANDOFF_CP1 §INTERFACES (config/bus/logging-redaction), HANDOFF_CP4 targeted (context fields you display).
3. Blueprint read set: Ch.21 L17562–18086 FULL; Ch.23 L18221–18314 FULL; Ch.1 L282–355 (wizard + ladder display); §2.5 L1135–1174; §2.6 L1175–1197; AI.9 reread; AI.13 rows G-RESTORE-001/G-PAPER-001..002 (harness duty); Ch.7 E-TELE-* rows. Telegram 24-field evidence display contract per Ch.21 §8.

## 1. Precision list (from blueprint; all binding)
aiogram 3.7.0; OWNER/USER only; wizard states per Ch.1 table (IDLE→SETUP→CONFIRM→ARMED→TRIGGERED, Cancel always, 30s confirm→REARM_COUNTDOWN, busy guard E-VAL-020 with Stop button, state table L18038 verbatim semantics); screens per §5 blocks incl. Lab actions exactly limited to pause/charge-48h/resume/validate-only (no other lab powers; optimizer enablement forwards to A09's state surface via bus — you never implement optimization logic); Emergency L1–L5 OWNER-only with immediate effect (your UI layer; ladder logic itself is CP-5 state — display + command only); rate discipline (internal 20/s, provider 30/s, bucket 20 tokens, refill 20/s, retry 1/2/4s ×3; E-TELE-006..007 semantics; idempotency key SHA-256(signal_id+timestamp_UTC+chat_id), TTL 24h, duplicate→skip); text≤4096, caption≤1024, inline keyboard ≤8 buttons/4 rows; durable outbox (no silent drop; Telegram failure NEVER blocks protective execution — P0 path is local-first, delivery-degraded logging); charts: matplotlib Agg-before-pyplot, headless failover, in-memory PNG 1200×800 q90 via send_photo, no file storage; secrets: env only, memory-only, never in message text/logs/DB (§2.5; redaction helper from CP-1 enforced in your sinks); chat-id validation; watchdog: 60s signed heartbeat, 3 missed → HOST_DOWN independent channel + reduce-only/cancel-all via restricted key through CP-5 adapter operations ONLY (withdraw/transfer absent by construction), primary-side 3× unreachable watchdog → DEGRADED + entries blocked; isolated send-only Gmail module inside watchdog process only (SMTP app password, zero access to trading path/keys/ledger; removal is one-line: no inbound dependencies); alert policy table verbatim incl. dedup window 30min except EXEC_RECOVERY/CIRCUIT_OPEN, alert fields (timestamp/metric/threshold/observed/snapshot_id), immutable audit-trail append, termux-battery-status consumption; backup per §2.6+Ch.23 (hourly + every FSM transition, encrypted, hash-chain verified, off-device copy semantics scripted, restore drill script + T-RESTORE-001 executed locally, RTO claim = measured-in-sandbox label only); NFR/queue harness (T-NFR-001..004 runnable on-device by owner; T-NFR-004 priority-eviction test in-sandbox); README final run section (complete, copy-pasteable: termux setup, pin install, .env names, run, watchdog, backup cadence, restore, gates list as owner procedures).

## 2. Tests
T-FB-001..003 (fail-closed entry, protective preservation, startup reconciliation ×5 scenarios), T-NFR-004, E-TELE boundary suite, idempotency replay, busy-guard concurrency, heartbeat-loss sim (injected clock), dedup window, backup hash-chain verify + restore in tempdir, Agg-no-display test, callback-table completeness (every callback in the table → handler exists; test introspects the registry), secrets-leak grep test over your message builders.
Exit (P16): HANDOFF_CP6 §[AGENT-08] → STATUS → issues → pytest green → `[CP-6][AGENT-08]` commits → pull --rebase + push → report. RESUME splits: signaling / screens-wizard / watchdog / backup-ops / README.
Begin.
