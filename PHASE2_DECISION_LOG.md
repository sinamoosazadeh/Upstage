# APEX_GEN5 — PHASE 2 DECISION LOG, CONTRADICTION REGISTER & PRE-ADJUDICATIONS

Purpose: the ONLY place where interpretation happens. Agents must (a) read the pre-adjudications before starting, (b) never re-litigate an ADR, (c) file new findings in their own CP section using the issue format, (d) implement the stated interim behavior. Silent resolution of contradictions is a PROTOCOL P13 violation.

Issue format (append under your CP heading):
```
[ISSUE-CPn-xxx] severity: CRITICAL|MAJOR|MINOR | status: OPEN|CLOSED|ESCALATED
  A: "quote" (APEX_GEN5.md Lnnn)
  B: "quote" (APEX_GEN5.md Lnnn)   # the conflicting passage / gap
  Rule applied: <which precedence clause resolved it, or "UNRESOLVED → fail-closed interim">
  Interim behavior: <exactly what the code does now>
  Needs from owner: <decision/measurement, or "none">
```

---

## A. Pre-adjudications ADR-P2-001..017 (binding for every stage executor; Rev-1: 2026-09-11; Rev-3 restated: 2026-09-12)

- **ADR-P2-001 — Repository target.** PROMPT.md's final block names `https://github.com/sinamoosazadeh/APEX_GEN5` as the START-EXECUTION repo. Owner decree (2026-09-12, Rev-3): all implementation happens in `https://github.com/sinamoosazadeh/Upstage`; the previous partial attempt/repo is abandoned and deleted by the owner — executors of this plan must not clone, read, or receive excerpts of it. Its only lasting value: capacity calibration (ADR-P2-016).
- **ADR-P2-002 — Test tooling.** Runtime deps stay exactly the Ch.1 SBOM (requirements.lock = the nine pins). pytest is allowed as a dev-only extra in pyproject (`tests = ["pytest>=7,<9"]`), never in requirements.lock, never imported by `apex/**` runtime code.
- **ADR-P2-003 — Tree = minimum + canonical.** §9.5 "create every file; do not rename" binds exact names/paths; additional files are permitted only where a blueprint chapter requires them (fixtures, tests, tier modules, ops/research additions, __init__.py), and each must appear in the agent's handoff DELIVERED table + a one-line note here under that agent's CP.
- **ADR-P2-004 — Emergency-ladder persistence.** Ch.1 L1–L5 + Ch.23 require persisted ladder state, but Ch.4 DDL has no such table. Resolution: CP-6 adds `apex_risk_ladder_state` (append-only revisions; single-writer via ledger queue) as a migration entry; frozen semantics = ratchet-only (RSK-ERR-506). No new operational states may be introduced beyond L1–L5 + the existing DEGRADED/RECOVERY_REQUIRED vocabulary.
- **ADR-P2-005 — Packages absent from the tree.** Research/ops support modules (backtest/optimizer/promotion; deploy scripts) are AI.12 Phase-8/9 deliverables with no tree entries: created as `apex/research/**`, `scripts/**` (new paths only — not renames), logged here by CP-8 (research) / CP-7 (ops helpers). The normative tree files remain the contract surface.
- **ADR-P2-006 — §3.12 "module layout" vs flat tree.** Tree wins for its named files (`contracts.py`, `catalog.py`, `store/sqlite_store.py`, `ingest/toobit_public.py`); the tier packages (`atomic/`, `molecular/`, `organismic/`, `math/`, `performance/`) are added UNDER `apex/data_catalog/` exactly as §3.12 names them. No duplicate catalog module: registration flows through the single `catalog.py` public API.
- **ADR-P2-007 — Illustrative hashes/values.** E01 §8.1 FIX_001 hash `e3b0c442…` is the empty-string SHA-256 (a placeholder of record); all engines' §9 case-study numbers are illustrative per §9.5-2. Agents construct fixtures, re-derive expected values from formulas, and compute REAL hashes after fixture creation (no-hash rule). Never copy a document hash or case value into code as truth.
- **ADR-P2-008 — E11 YAML vs §6 tension.** `params/e11_params_v4.yaml` values are §9.5-canonical (K:9, θ_H:0.65, λ_ewma:0.94, hyst 3, dirichlet 0.1, delay 48, W=4320). Engine §6 tables govern formula internals/sensitivity. If CP-7 finds a real numeric conflict between §6 and the YAML, the YAML value loads at runtime, the conflict is filed [ISSUE-CP3-*] and the engine chapter's formula semantics are otherwise untouched (preamble rule).
- **ADR-P2-009 — v2/v3 adapter conformance without legacy data.** This repo contains no v2/v3 code or datasets; G-ADAPTER-001's "100 legacy samples" cannot be sourced without fabrication. Resolution: CP-8 implements the adapter INTERFACES (AI.2 adapter column, read-only, no state mutation) + a conformance harness operating on any user-provided legacy export + synthetic v4-derived self-checks. The real-data gate remains OPEN/UNVERIFIED for the owner (recorded, never faked).
- **ADR-P2-010 — NFR/device measurements.** T-NFR-001..004 and G-CAPACITY/G-TOOBIT/G-PAPER require the owner's device/live market. Agents deliver executable harnesses and run what is runnable in-sandbox; results in-sandbox are labeled `SANDBOX-PARTIAL`; `TARGET-DEVICE` rows stay OPEN until owner measurement (AI.13 status vocabulary: OPEN/CLOSED/BLOCKED only).
- **ADR-P2-011 — Dangling "Chapter 5, SL-N" citations.** §9.9/R.3 cite an older "Chapter 5 (SL-0..SL-15)" contract registry. Resolution: each SL-n contract's content IS defined in its owning runtime chapter — SL-1 Evidence/Context Fabric (Ch.8), SL-2 conflict/monotonicity (Ch.8 §8.1), SL-3 forecast (Ch.13), SL-4 capital ceiling (Ch.19 + Ch.15), SL-5 Canonical Risk Veto Registry (Ch.15), SL-6 execution FSM/adapter/ledger (Ch.16), SL-7 divergence transport (Ch.8/9), SL-8 MTF (Ch.8 §8.3), SL-9 semantic levels Feature→…→Strategy (Ch.6/8/9/10/11/12 ladder), SL-10 evidence-event bus (Ch.4 + AI.12), SL-11 redundancy (Ch.8/§10 redundancy rule), SL-12 parameter governance (Ch.17), SL-13 shared validation battery (Ch.18 §18.4), SL-14 evidence lifecycle/quality resolution ladder (Ch.8 §8.0 + Ch.2 §2.1), SL-15 case-study discipline (Ch.16 §/§9.5-2). Never invent content to fill a dangling SL reference; cite the owning chapter.
- **ADR-P2-012 — Single-branch baton chain.** All slots push to `main` with pull-rebase; adjacent stages keep disjoint WRITE SETs so rebase never conflicts except on control files (append-merge rule P14). No feature branches are used; history linear-ish by discipline. If the owner prefers branch-per-agent + PR review, it is an operational variant only — path ownership and handoff duties are unchanged.
- **ADR-P2-013 — Runtime data & hygiene.** `.gitignore` (created by CP-1): `data/`, `*.sqlite3*`, `.env`, `__pycache__/`, `.pytest_cache/`, chart artifacts (none expected — in-memory rule), `dist/`, `build/`. DBs and `.env` never enter git; nothing else may be added to `.gitignore` by later agents without a log note.
- **ADR-P2-014 — Fixture canonicality split.** Engine §8 golden fixtures (FIX_*) are engine-canonical; §9 case studies are illustrative; GF_SC_01/02 shapes are Ch.9-canonical but their bar arrays must be constructed by CP-6 (source has none — PHASE 79/C-3 record); GF_SC_03..12 exist only as schema assertions (Wave-Out). Tests must assert shape+semantics for 03..12 and full behavior for 01/02.
- **ADR-P2-015 — From zero.** No code of the prior partial attempt is imported, referenced, or re-derived 'for comparison'; `PHASE2_SALVAGE.md` and the Rev-2 salvage law are WITHDRAWN and deleted. Everything is built fresh from the frozen blueprint. Forbidden defect classes (learned from the abandoned attempt, listed here once so nobody re-invents them): silent simplification ('Simplified X'), placeholder constants, non-canonical DDL table names, duplicate identity generators, off-tree directories, env-only config bypassing §9.5 params YAMLs, zero-test delivery.
- **ADR-P2-016 — Capacity model → 8 stages; no audit phase inside the plan.** Measured: one context window of the solo attempt read ~5–7k spec lines and emitted ~6.7k mostly-thin lines before exhaustion (stopped ~E03/E04 of twelve engines, tests absent). Full-fidelity work costs more per line (tests, fixtures, §8 batteries), so each stage here is sized at ~6–11k work-lines (read+write) — clearly under the measured ceiling — with the OVERFLOW ledger as escape valve (G19/P18). Stage count 8 is derived (total work ÷ per-window capacity), not chosen for elegance; compressing to 6 stages would demand ~13k-line stages = the exact profile that died. A separate external review/bugfix phase is the owner's later decision; this plan ends at CP-8 closeout.
- **ADR-P2-017 — Engine grouping = dependencies + reading load (3/3/3/3).** CP-2 E01–E03, CP-3 E04–E06, CP-4 E07–E09, CP-5 E10–E12: every cross-engine consumption stays same-stage-or-earlier (E06→E01–E05; E07 codes its 'E12 unavailable → degraded' branch at CP-4, integration test lands at CP-5 when E12 exists; E10→E01/E04; E11→E09/E10 same stage; E08→E01/E02). No engine is ever split mid-chapter.

## B. Open issues (executors append under their CP; owner reads this on every verification)

### CP-1
- [ISSUE-CP1-001] severity: RESOLVED-BY-DECREE | A: PROMPT.md final block names the old repo as start target | B: this plan's sole repo is Upstage. Rule: ADR-P2-001 — old repo abandoned/deleted; nothing is fetched from it. Informational only.
- [ISSUE-CP1-002] severity: MINOR | status: CLOSED | A: Ch.7 L14664 "OI lag -> QUARANTINED … Q_oi AVAILABLE=1.0 STALE=0.5" | B: §2.1 Q_oi table scores STALE (lag ≤ 5·threshold) as 0.5. Rule applied: §2.1 Q_oi table semantics (GLOBAL CONTRACTS precedence). Interim behavior: veto_OI_lag fires only at lag > 5×threshold (DEGRADED band) or INVALID; STALE is scored, not vetoed. Needs from owner: none.
- [ISSUE-CP1-003] severity: MINOR | status: CLOSED | A: §3.12 uniform template "Unit: ratio Range: [0,10]" on every feature | B: §2.2 core formulas produce price diffs (range, body_size), booleans (is_bullish), and a k-indexed dict (return_k). Rule applied: §2.2 is the formula authority for features it defines; §3.12 template metadata is retained verbatim in contracts. Needs from owner: none.
- [ISSUE-CP1-004] severity: MINOR | status: OPEN | A: §3.12 names formulas for F22–30 (k_*/theta_*), F40 GARCH, F42–44 (theta_body/vol/invalid) | B: no formula math in CP-1's assigned ranges (E02/E03 chapters own the k/theta/GARCH semantics; Ch.6 Feature Catalogue is not assigned to any stage). Rule applied: P13 fail-closed interim. Interim behavior: features registered with complete contracts; compute returns UNAVAILABLE with a deterministic reason — never a guessed formula. Needs from owner: none (CP-2/CP-3 supply implementations from their chapters).
- [ISSUE-CP1-005] severity: MINOR | status: OPEN | A: §3.12 ORGN CTXT features take (O,H,L,C,V,OI,regime,temporal_window,MTF) | B: regime fabric (CP-6), E12 temporal windows (CP-5), HTF evidence (CP-2..CP-5) do not exist at CP-1. Rule applied: AI.6 missing-data permission model. Interim behavior: context-gated features return UNAVAILABLE (gates stay open, nothing fires) until their producers exist. Needs from owner: none (CP-5/CP-6 wire them).
- [ISSUE-CP1-006] severity: MINOR | status: CLOSED | A: §3.12 F20 VolRatio and F41 volume_ratio are separate slots | B: §2.2 defines one volume_ratio formula (V/max(SMA_20(V) on t−1, ε), 8 digits). Rule applied: §2.2 formula authority. Interim behavior: F41 = exact §2.2 8-digit formula; F20 = the plain ratio with the same PIT discipline (SMA on t−1). Mapping disclosed, never silently merged. Needs from owner: none.
- [ISSUE-CP1-007] severity: MINOR | status: CLOSED | A: §2.3 algorithm uses tf_duration[tf] | B: no tf_duration table exists in the blueprint. Rule applied: deterministic documented interim. Interim behavior: unambiguous minute-multiples for 1m..1w; 1mo = 2592000 s (30-day exchange interval; Toobit wire 1mo→1M). Needs from owner: none.
- [ISSUE-CP1-008] severity: MINOR | status: CLOSED | A: params/*.yaml must be loaded at runtime | B: the Ch.1 SBOM (nine pins) contains no YAML library; no tenth runtime dep may be added. Rule applied: G16/P5 fail-closed. Interim behavior: strict stdlib parser for the frozen "APEX params subset" (block/flow maps+sequences, comments, quoted/plain scalars) in apex/config.py; anything outside the subset raises ValueError. Needs from owner: none.
- [ISSUE-CP1-009] severity: MINOR | status: CLOSED | A: §9.5 shows universe_v1.yaml with four keys | B: Ch.5 requires tick_size/quantity_step/min_notional/exchange_max_leverage as governed parameter values. Rule applied: ADR-P2-003 (additive where a blueprint chapter requires). Interim behavior: four additive key maps in universe_v1.yaml, logged here and in the handoff DELIVERED. Needs from owner: none.
- [ISSUE-CP1-010] severity: MINOR | status: CLOSED | A: F74 §3.12 text "rejection ratio >= theta_sweep" | B: theta_sweep is itself feature F29 (E02-owned parameter, unavailable at CP-1) and "rejection ratio" is not numerically defined. Rule applied: §3.12 F74 text + fail-closed on missing parameters. Interim behavior: rejection ratio = (close-back-inside depth)/(penetration depth); theta_sweep consumed from the compute context when a producer provides it, else the gate is skipped with a recorded reason; 5-candle block, rolling 20-bar extreme, VolumeZ ≥ 2, 3-block cooldown all implemented as written. Needs from owner: none (E02/CP-2 re-asserts).
- [ISSUE-CP1-011] severity: MINOR | status: CLOSED | A: features reference indicator formulas (SMA/EMA/RMA/Wilder/ATR/TR/VWAP/OBV/RSI/z/OLS/Hurst/Parkinson/Garman-Klass/Rogers-Satchell) | B: Ch.6 Feature Catalogue per-feature code is not assigned to any stage. Rule applied: §3.12 registry + §2.2 formulas are CP-1's authority; indicators implemented per the standard definitions the frozen text itself uses. Needs from owner: none (engine stages re-assert against their chapters).
- [ISSUE-CP1-012] severity: MINOR | status: CLOSED | A: AI.4 correction model: original SUPERSEDED, new record appended | B: Ch.4 candle_status enum contains both SUPERSEDED and CORRECTED. Interim behavior: original market_observation row → SUPERSEDED; corrected row → CORRECTED; raw_observation stays append-only; lineage in raw_revision + retention_event audit rows. Needs from owner: none.
- [ISSUE-CP1-013] severity: MINOR | status: CLOSED | A: G16 forbids pip install without the lock | B: sandbox PEP 668 blocks system pip. Rule applied: dev-toolchain only; runtime deps remain the nine locked pins. Interim behavior: tests run under a venv outside the repo (/home/user/apex-venv); on-device the frozen Termux SBOM governs. Environment note, not a deviation. Needs from owner: none.
- [ISSUE-CP1-014] severity: MINOR | status: CLOSED | A: PHASE2_MASTER_PLAN / ADR-P2-012: work directly on branch main | B: this executor session is Arena-tied to branch arena/01a09373-upstage (branched from main@bc91ed4); pushing other branches is outside the session's authority. Interim behavior: the full CP-1 commit chain is pushed to the session branch only, rebased on origin/main before push; ADR-P2-012 main-branch discipline applies when the owner merges. Needs from owner: merge arena/01a09373-upstage → main.
### CP-2
- [ISSUE-CP2-001] severity: MINOR | status: CLOSED | A: E01 §5.3 market-state machine fourth transition source line reads `AUTC_W2 --no BOS in expiry--> RANGE` | B: the machine's four named states are BULL/BEAR/RANGE/TRANSITION; no state "AUTC_W2" exists anywhere. Rule applied: the garbled token sits exactly where TRANSITION belongs (state #4) and the transition (no BOS in expiry → RANGE) is otherwise fully specified. Interim behavior: `AUTC_W2` read as `TRANSITION`; EXPIRY transitions only TRANSITION→RANGE (strict transition table); all other states reach RANGE only via the specified BOS/CHoCH paths. Needs from owner: none.
- [ISSUE-CP2-002] severity: MINOR | status: CLOSED | A: E01 §4.6 pseudo-code enumerates eligible levels most-recent-first when assigning level_index | B: §3.6 ("L_1 is the nearest opposing swing and L_k the farthest"), §8.1 FIX_004 (swings 635.5@5 and 640@8, break 641.5 → expected BM_L1=1.428 at level 635.5, BM_L2=0.357 at 640 — price adjacency), and §9 Step 5 all require price-adjacency order. Rule applied: ADR-P2-014 fixtures canonical. Interim behavior: level_index assigned in price-adjacency order among broken levels (bullish: ascending; bearish: descending); the candidate stack is still built most-recent-first per §4.6 (max_levels=3). Needs from owner: none.
- [ISSUE-CP2-003] severity: MINOR | status: CLOSED | A: E01 §4.3 Gann pseudo-code (run-of-3 marker) cannot produce §8.1 FIX_002 (lows 99,98,97,97.1 → exactly [LOW@2, price 97]), and §4.4's index-±2 dedup swallows FIX_009's three same-family swings (631.2@5, 632.4@7, 631.8@8 → expected 2 pruned). B: §9 case study (lows 618,621.5,624.1 → Gann LOW at C1) and the §3.4 GSH formula (requires SWING_prev=LOW) pin the semantics. Rule applied: fixtures + case study + §3.4 formula over §4 pseudo-code. Interim behavior: (a) Gann swings mark completed-leg extremes; a Gann LOW fires on the first higher low after falling lows; a Gann HIGH requires SWING_prev=LOW (so alternation opens with a LOW, matching both FIX_002 and §9); (b) dedup merges only cross-method (Williams+Gann) same-type ±2-bar overlaps — its stated purpose — while same-family neighbors flow to the depth/redundancy pruner; (c) Gann final confirmation (§3.4: Williams overlap ±2 bars same-type OR depth ≥ 1.2) applied at prune stage (FIX_002 tests raw detection); (d) FIX_009's stated reason label "redundancy>0.9" is a scenario name — the count 2 is reproduced by depth pruning (0.286, 0.143 < θ_depth 0.8), which the fixture's own inputs determine; no redundancy value is fabricated. Needs from owner: none.
- [ISSUE-CP2-004] severity: MINOR | status: CLOSED | A: E01 §4.8 pseudo-code accepts a retest when |L−P| or |H−P| ≤ κ·ATR (wick touch) | B: §3.11 defines retest as |C_{t'}−P| ≤ κ_ret·ATR (close-based), and §8.1 FIX_007 expects EV_STR_013_INVALIDATION for a candle whose high touches the level (|H−P|=0.5 ≤ tol) but closes 4.5 below it. Rule applied: §3 corrected-formula authority + fixture canonical. Interim behavior: retest is close-based per §3.11; wick-touch retests are not recognized (the wick case is covered by EV_STR_012 Wick Rejection). Needs from owner: none.
- [ISSUE-CP2-005] severity: MINOR | status: CLOSED | A: E01 §3.2 GapType defines BREAKAWAY as |G|>2·ATR ∧ Disp>1.5 ∧ VR>1.2 | B: §4.5 algorithm gates BREAKAWAY on |gap|>2·at ∧ (H−L)/at>1.5 with no VR term, and §8.1 FIX_008 supplies no volume-SMA basis (VR not computable). Rule applied: §4 algorithm + fixture decidability. Interim behavior: §4.5 rule implemented; the §3.2 VR>1.2 condition is recorded as gap metadata (vr field) when computable, never as a gate. Needs from owner: none.
- [ISSUE-CP2-006] severity: MINOR | status: OPEN | A: each engine chapter carries a §6 governed parameter table | B: §9.5's normative tree enumerates exactly six params YAMLs (the e11_params_v4.yaml precedent is the only engine file), and Params (apex.config, CP-1 frozen) loads only those six names; a seventh file would require patching frozen config.py. Rule applied: §9.5 tree + consumed-never-patched law. Interim behavior: each engine package carries its §6 table as a frozen in-code default dict (blueprint literals, single source, no runtime retuning; `get_params()` rejects unknown keys); tick_size loads from universe_v1.yaml (ISSUE-CP1-009 additive keys). Related: E01 §3.3's dynamic Williams k (k = max(2,⌊α·ATR_ratio⌋), α unspecified) is Wave-Out per §9.5-9/GC-D — k is fixed at 2 and any dynamic-k request raises WaveOutError("dynamic_williams_k"). Needs from owner: none (CP-8 closeout may reconcile a 7th YAML with a config.py change under its own authority).
- [ISSUE-CP2-007] severity: MINOR | status: CLOSED | A: EngineBase.compute() is a sync abstract whose docstring says "implementations read via self.feature() only", but self.feature()/Catalog.get are async (uncallable from sync code), and the engine chapters' algorithms consume raw closed-candle windows which catalog.get does not expose | B: engines must obtain a candle window lawfully. Rule applied: P19 frozen-contract consumption + fail-closed. Interim behavior: compute() takes its window from context["window"] (the §4.10 streaming model — the data plane supplies the per-candle window; single event loop) or from a synchronous WindowProvider (the public apex.data_catalog.catalog.WindowProvider protocol) passed as context["provider"]; an async provider inside a running loop without a context window raises ValueError("MISSING_WINDOW_CONTEXT_QX") — deterministic fail-closed; engines never SQL the store (Ch.5 lint rule). CP-7's scheduler is the intended context supplier. Needs from owner: none.
- [ISSUE-CP2-008] severity: MINOR | status: CLOSED | A: §8.1 FIX_010 states expected_bias 0.6 for states {1D: BULL, 1W: BULL, 4H: RANGE} with weights {1D: 0.6, 1W: 0.4} | B: §3.10's formula gives 0.6·(+1)+0.4·(+1) = 1.0 for those inputs, and the §9 Step-9 walkthrough computes Bias = 1.0 > 0.5 → BULL for the same weights. Rule applied: §3.10 formula + §9 walkthrough over the fixture's scalar. Interim behavior: the engine computes 1.0 (label BULL); the test asserts the formula value; the fixture's 0.6 is recorded as internally inconsistent. Needs from owner: none.
- [ISSUE-CP2-009] severity: MINOR | status: CLOSED | A: §2.1 state machine says "PRUNED (older than theta_maxAge, 100 bars)" (implemented as F31 = 100 by CP-1) | B: E01 §6 default theta_maxAge = 120 and §4.4 pseudo-code uses 120. Rule applied: scope separation. Interim behavior: F31 (catalog feature) keeps §2.1's 100; E01's internal swing pruning uses its own §6 default 120 — two different scopes, both verbatim. Needs from owner: none.
- [ISSUE-CP2-010] severity: MINOR | status: CLOSED | A: E02 §3.5 defines the DBSCAN pool epsilon as θ_eq·ATR·κ | B: §7 Ch.2 examples and §8.1 GF_LIQ_003 ([100, 100.05, 100.09], ATR 0.2, θ 0.15, κ 1.0 → ONE pool, 3 members, weight > 2.0; the weight 2.2487 reproduces only with eps = 0.06 = 2·θ·ATR·κ) require the §3.3 D_max scale. Rule applied: ADR-P2-014 fixtures canonical. Interim behavior: the pool clustering radius passed to dbscan_1d_optimal is 2·θ_eq·ATR·κ (the equal-level D_max scale, κ-calibrated); §3.5's bare formula is recorded as the half-radius naming. Needs from owner: none.
- [ISSUE-CP2-011] severity: MINOR | status: CLOSED | A: E02 §4 pseudo-code computes the LVN threshold as the bin-count 15th percentile of bin volumes and gates on max(inside) ≤ thresh | B: that gate rejects §8.1 GF_LIQ_007's expected void (inside bins 200/250/300/400, bin-percentile 200). §2 defines the Void via "the aggregated volume inside the region is ≤ the 15th percentile of the profile's volume". Rule applied: §2 definition + fixture canonical. Interim behavior: lvn_threshold = (lvn_percentile/100) × total profile volume; the void condition is sum(inside) ≤ threshold (GF_LIQ_007: 1150 ≤ 1822.5 ✓; §7 Ch.4 example: 800 ≤ 1650 ✓); max_vol_inside recorded as metadata. Needs from owner: none.
- [ISSUE-CP2-012] severity: MINOR | status: CLOSED | A: E02 §4's merton_hit_probability uses the ATR-normalized distance directly as d (giving p ≈ 0 for GF_LIQ_012 and contradicting §9 Step 10) | B: §9 Step 10 converts distance to log/return units first (d_log = distance_price/price ≈ 0.00539 → p_brown 0.949, p_jump 0.787), and GF_LIQ_012 requires p_hit > brownian. Rule applied: §9 worked math + fixture canonical. Interim behavior: d = distance_price/price when price is given (exact §9 conversion), else the ATR proxy d = distance_atr·σ_ret (one ATR ≈ one σ_ret return); both reproduce §9 and GF_LIQ_012 (1.0 > 0.9203). Needs from owner: none.
- [ISSUE-CP2-013] severity: MINOR | status: CLOSED | A: E02 §7 Ch.4's OFI example computes e_a = −5 when the ask price moves UP (101→102) | B: §3.8's formula and §4's code both give e_a = +q_a on a price-up move, and §8.1 GF_LIQ_008 expects OFI = 2−5 = −3 (only the formula's sign reproduces it). Rule applied: §3.8 formula + fixture canonical. Interim behavior: Cont et al. (2013) signs as in §3.8/§4; the §7 example's sign is recorded as erroneous. Needs from owner: none.
- [ISSUE-CP2-014] severity: MINOR | status: CLOSED | A: E03 §8.1 fixture "VWAP_TypicalPrice" states expected_vwAP 96.555 for bars (100,90,95,1000) and (101,92,100,2000) | B: §3.2's formula gives (95·1000+97.667·2000)/3000 = 96.778 for those bars (96.555 is only reachable if bar 2's high were 100), while the §7 Ch.2 three-bar example (VWAP 97.29) IS exactly reproducible. Rule applied: §3.2 formula authority; no value is copied. Interim behavior: the test asserts the formula value 96.778 and separately reproduces the §7 example's 97.29; the fixture's scalar is recorded as internally inconsistent. Needs from owner: none.
- [ISSUE-CP2-015] severity: MINOR | status: CLOSED | A: E03 §8.5 states z = 2.1, p < 0.05 for the VR-filtered continuation split; E01/E02/E03 §8.6 correlation thresholds (≤0.15, ≤0.85, 0.3–0.8, <0.85, <0.3) are stated as universal gates | B: the z is not reproducible from the stated counts (34 BOS / 21 continued / 15 VR-filtered; the closest consistent split 15/21 vs 6/13 gives z = 1.47; the Wilson CI [0.50, 0.86] IS exactly reproducible with p̂=15/21, n=21), and the §8.6 thresholds were measured on real market data (no real OHLCV dataset exists in the sandbox; none may be fabricated). Rule applied: ADR-P2-007 (re-derive, never copy) + no fabricated statistics. Interim behavior: every §8.6 gate is an executed check on deterministic synthetic windows (documented seeds) where the property holds — E01↔E02 r = 0.015 ≤ 0.15 (seed 31), VWAP_dev/OBVZ r = −0.04 < 0.3 (seed 61), plus the E02-internal salience/instances band; the §8.5 test asserts the reproducible Wilson CI and records the non-reproducible z. Real-data verification belongs to the research stage's ADR-P2-005 harness. Needs from owner: none.
- [ISSUE-CP2-016] severity: MINOR | status: OPEN | A: ISSUE-CP1-004 expects CP-2/CP-3 to "supply implementations from their chapters" for the k_*/theta_* catalog slots (F22–30, F40, F42–44) | B: the E01/E02/E03 chapters define these names only as governed engine parameters (θ_eq = 0.15, θ_depth = 0.8, θ_maxAge, sweep thresholds) — §3.12's `formula_k_disp(O,H,L,C,V,ATR)` etc. remain undefined placeholder names in every assigned range, and the catalog is consumed-never-patched. Rule applied: fail-closed + consumed-never-patched. Interim behavior: the engines carry their chapter's parameter values in-package (E01_DEFAULTS/E02_DEFAULTS/E03_DEFAULTS, ISSUE-CP2-006) and consume/produce features only through public catalog surfaces; the catalog ATOM slots stay UNAVAILABLE (correct fail-closed behavior for undefined formulas); F74's theta_sweep context pathway (ISSUE-CP1-010) remains available for a future producer — E02's own sweep detection is the chapter's five-prerequisite engine model, a different surface from F74's 5-candle-block feature. Needs from owner: none (CP-3/CP-5 may close the remaining slots if their chapters define the formulas; else CP-8 dispositions).
### CP-3

- [ISSUE-CP3-001] severity: MINOR | status: CLOSED | A: E04 §4 pseudo-code gates the Ljung–Box result through coarse p-value step functions | B: §3.10/§3.11 define the exact statistics p = 1 − F_χ²(Q) (χ², df=m) and the two-sided t-CDF p-value. Rule applied: §3 formula authority over §4 pseudo-code (P8 formula precedence). Interim behavior: exact CDFs implemented stdlib-only (regularized incomplete gamma via math.lgamma for χ²; incomplete beta for t; no scipy — SBOM law G16); the §4 step functions are recorded as illustrative. Needs from owner: none.
- [ISSUE-CP3-002] severity: MINOR | status: CLOSED | A: E05 §7 Ch.1-11 documents a historical max/min FVG zone formula | B: that formula produces NEGATIVE-width zones on the blueprint's own fixture bars (max(H0,H1)=123 > min(L1,L2)=120.1) while the ICT zone [H_{t−2}, L_t] is valid ([122.4, 124.6]). Rule applied: §2/§3 main implementation + fixture canonical. Interim behavior: the frozen ICT zone is the only detection formula; Gate_C is a numerical safety net that records NEGATIVE_WIDTH without fabricating a zone; the test asserts the abandoned formula is negative to pin the rationale. Needs from owner: none.
- [ISSUE-CP3-003] severity: MINOR | status: CLOSED | A: E04 §8/§4 quote Ljung–Box critical values as hard thresholds | B: with the exact χ² CDF the m=20 criticals are 31.410 (p=0.05) and 37.566 (p=0.01); the engine compares the exact p-value, so any table rounding is non-normative. Rule applied: §3.10 exact statistic. Interim behavior: ljung_box returns (Q, exact p); tests pin Q=5.056934/p=0.079781 for the §8.1 fixture and both criticals. Needs from owner: none.
- [ISSUE-CP3-004] severity: MINOR | status: CLOSED | A: E05 §5.2 text says FVGs expire at "age > max" | B: the §8.1 FRESHNESS_DECAY_EXPIRY fixture (age 96 = max_age, freshness 0.25 > 0.15) is expected expired=true, which only the boundary-inclusive rule reproduces. Rule applied: ADR-P2-014 fixtures canonical. Interim behavior: expiry fires at age_bars ≥ max_age_bars OR freshness < fresh_thr; the engine comment cites this issue. Needs from owner: none.
- [ISSUE-CP3-005] severity: MINOR | status: CLOSED | A: E05 §3.6 gates INVERSE classification on VolRatio > 1.2 | B: during Q1 warmup VolRatio is undefined (the §4 contract call raises INSUFFICIENT_HISTORY_Q1), yet the §8.1 INVERSE fixture is canonical at 3 bars. Rule applied: §3.4 conservative warmup + fixture canonical. Interim behavior: when VolRatio is not computable the INVERSE gate is evaluated on structure/trend alone (the VR term stands as satisfied-by-absence), recorded in the classifier comment; with computable VR the > 1.2 gate applies normally. Needs from owner: none.
- [ISSUE-CP3-006] severity: MINOR | status: CLOSED | A: E06 §5.2's final row describes a MITIGATED → MITIGATION_BLOCK research transition | B: §1.6 permits only Q3/Q4 as SETUP inputs and Q4 requires OOS validation that runtime never fabricates (§8.5). Rule applied: §1.6 quality gate + no-fabricated-statistics. Interim behavior: reactivated MITIGATION_BLOCK objects are emitted at Q2 with fate ACTIVE (context-only), never implicitly elevated; the engine comment cites this issue. Needs from owner: none.
- [ISSUE-CP3-007] severity: MINOR | status: CLOSED | A: E05 §3.4's VolRatio term requires SMA(V,20) history | B: the §8.1 golden fixtures are canonical at 3 bars, where the §4 contract call raises INSUFFICIENT_HISTORY_Q1 and blocking zone creation would contradict "Classified → Q2". Rule applied: §3.4 conservative warmup rule + fixture canonical. Interim behavior: the streaming engine's process_bar applies the conservative rule (VolRatio stays 0, its w_v share is NOT reallocated, salience computed with vr_override=0.0) so creation proceeds; direct contract calls still raise INSUFFICIENT_HISTORY_Q1 (fail-closed preserved). Needs from owner: none.
- [ISSUE-CP3-008] severity: MINOR | status: CLOSED | A: E04 §8.1 regime fixture omits the RangeZ input needed by its own expected regime label | B: §3.9's banding requires (hv, quantiles, RZ); without an explicit RZ the label is underdetermined. Rule applied: ADR-P2-007 re-derive, never copy. Interim behavior: the golden fixture carries an explicit RZ and the re-derived label; the doc fixture's omission is recorded in the fixture note. Related: the §7 YZ worked example is only reproducible with its own approximate intermediates (RS²=3.24e-4, k=0.1018 ⇒ n=3 for a 2-bar input); the fixture asserts the engine value 0.0176164 (tol 5e-5) with the divergence noted. Needs from owner: none.
- [ISSUE-CP3-009] severity: MINOR | status: CLOSED | A: several §8.1 fixture bars fail the same chapter's earlier gates (E05 REJECTION bars: H0=101 ≮ L2=100.5; E05 INVERSE bars: H0=112 ≮ L2=105 ∧ L0=108 ≯ H2=111) | B: no three-candle FVG exists on those bars, so the classification branch cannot execute on them. Rule applied: ADR-P2-007 + formula authority. Interim behavior: tests assert the re-derived formula values (WR=0.8727 etc.) on the fixture bars and exercise the branch on Gate_B-passing crafted shapes with identical geometry; each affected fixture carries a doc_inconsistency note. Needs from owner: none.
- [ISSUE-CP3-010] severity: MINOR | status: CLOSED | A: E05 §8.1 fixture "OVERLAP_MERGE_IOU" states expected IoU 0.75 | B: the fixture's own zones ([100,102], [100.5,102.5]) give inter=1.5, union=2.5 → IoU = 0.6 exactly; at the default threshold 0.7 they do NOT merge. Rule applied: ADR-P2-007 re-derive. Interim behavior: fixture asserts IoU 0.6; merge mechanics (bounds [100,102.5], S=max×1.1) are exercised via the governed threshold parameter and the containment rule. Needs from owner: none.
- [ISSUE-CP3-011] severity: MINOR | status: CLOSED | A: E06 §8.1 fixture 10 says expected_touch=false although the bar geometrically overlaps the zone ([100,101] inside [100,102]) | B: §2.8's directional rule makes a wrong-side penetration an Invalidation candidate, NOT mitigation. Rule applied: §2.8 semantics + fixture canonical. Interim behavior: directional_mitigation returns (touch=True, side_ok=False, depth>0); the effective-touch (touch ∧ side_ok) is False and no state change results; the test pins both the geometric and effective readings. Needs from owner: none.
- [ISSUE-CP3-012] severity: MINOR | status: CLOSED | A: E06 §8.1 fixture 2 labels a DOWN OB with vol_ratio 1.1 as Q2 | B: §1.6's ladder defines Q2 = Q1 + VolRatio ≥ 1.3; at 1.1 the normative quality is Q1 (the same rule fixture 8 applies for V=0 → Q1). Rule applied: §1.6 ladder authority; fixture kept verbatim. Interim behavior: the engine's detection branch yields Q1 when vr < θ_vol regardless of structure; the fixture carries a doc_inconsistency note and the test asserts the ladder outcome. Needs from owner: none.
- [ISSUE-CP3-013] severity: MINOR | status: CLOSED | A: EvidenceEvent comments say resolution_class ∈ Q0..QX | B: the frozen validator accepts exactly Q0..Q5 (contracts.py, consumed-never-patched). Rule applied: consumed-never-patched + CP-2 precedent (E02 maps out-of-range tags to a valid class). Interim behavior: E06 emits quality Q0..Q5 verbatim and maps terminal QX states to Q1 with validity=DEGRADED carrying the QX meaning (E05 expired zones stay out of the emission set). Foundation untouched. Needs from owner: none (CP-8 may reconcile the comment with the validator).
### CP-4

- [ISSUE-CP4-001] severity: MINOR | status: CLOSED | A: E07 §2 defines the Accumulation range condition with a close-dispersion term σ(C)/ATR_l < 0.6 | B: §3.2 lists only ATR10/ATR100 < 0.75 ∧ VolRatio < 0.9 ∧ (High−Low) < 2.5·ATR_l, omitting σ. Rule applied: §2 definition + both geometric terms (a range needs a tight *and* a low-dispersion window; the two are not equivalent because C ∈ [L,H] only bounds σ by the HL range). Interim behavior: `atr_ratio_detect` evaluates all four terms and reports `sigma_ratio`; the σ term is isolated in the battery by relaxing the ratio/HL thresholds through the governed parameters (tests/unit/test_e07_rtm.py::TestRangeDetection::test_sigma_term_is_enforced_issue_cp4_001). Needs from owner: none.
- [ISSUE-CP4-002] severity: MINOR | status: CLOSED | A: E07 §3.2 edge case: V = 0 ⇒ VolRatio unavailable | B: §4's range gate contains the shortcut `or recent_vol == 0`, which converts missing volume evidence into a *confirmed* low-volume range authorization. Rule applied: §3.2 edge case + fail-closed (P19); absent evidence is never a substitute value. Interim behavior: V = 0 ⇒ `VOL_UNAVAILABLE_QX`, `range_authorized = False`, no synthetic VolRatio; the §4 shortcut is not applied. Needs from owner: none.
- [ISSUE-CP4-003] severity: MINOR | status: CLOSED | A: E07 §3.2 mentions a single ATR-multiple gap rule | B: §2/§3.2 give two distinct thresholds with different effects — a 3·ATR gap *resets* the range window, a 5·ATR gap *flags* `gap_edge` on the bundle. Rule applied: both thresholds, distinct semantics. Interim behavior: `atr_ratio_detect` reports `gap_reset` and `gap_edge` separately; a reset restarts the range window rather than vetoing it. Needs from owner: none.
- [ISSUE-CP4-004] severity: MINOR | status: CLOSED | A: E07 §4 pseudo-code mints `bid` with `uuid.uuid4().hex[:12]` | B: the GLOBAL IDENTITY contract + P8 permit exactly one in-tree generator (UUIDv7, operational identity only), and §5.1 requires `^bnd_[a-f0-9]{12}$`. Rule applied: identity single-source + schema. Interim behavior: `new_bundle_id()` takes 12 hex chars from the UUIDv7 **random** field (bits 62–111 = 48 random bits); the leading 48-bit timestamp was rejected because every bundle created inside the same millisecond would collide. `bid` never enters the canonical snapshot payload. Needs from owner: none.
- [ISSUE-CP4-005] severity: INFO | status: OPEN-UNTIL-CP-5 | A: E07 §3.6 names E12 (`E12_Temporal_Context.Contract v4.0.0`) the temporal authority for the UTC activity windows | B: E12 is delivered at CP-5, so at CP-4 the authority does not exist in-tree. Rule applied: CP-4 stage rule + P19 (never fabricate an authority). Interim behavior: with no provider (or a provider that raises) E07 evaluates its own NON-authoritative duplicate registry, returns `source = "E07_LOCAL_NON_AUTHORITATIVE"`, `degraded = True`, `degraded_reason = E12_UNAVAILABLE_DEGRADED_QX`, and the Q5 overlap gate stays off; a conformant `temporal_window(ts_ms)` clears the degradation and makes Q5 reachable. CP-5 re-tests **both** modes (E07↔E12 integration is scheduled there, not here). Needs from owner: none — CP-5 inherits the two-mode test.
- [ISSUE-CP4-006] severity: MINOR | status: CLOSED | A: E07 §4's quality cascade returns Q5 on `Integrity ≥ 0.9 ∧ 0.95 ≤ conf` | B: §3.7's Q5 definition is conjunctive (it additionally requires the UTC overlap flag), and the §9 case study claims Q5 for a bundle that does not carry it. Rule applied: §3.7 conjunctive gates cap the §4 cascade. Interim behavior: `quality_class_caps` decrements the class until every gate of that class holds; the §9 case is Q4, asserted as re-derived. Needs from owner: none.
- [ISSUE-CP4-007] severity: MINOR | status: CLOSED | A: E07 §3.5's CHAIN.v1 weight list gives [sweep 1.5, choch 1.0, fvg 0.8, vol 0.7] = 4.0 | B: the same section states Σw = 4.8 for a five-component chain that includes `retest`. Rule applied: the stated total is authoritative ⇒ `retest = 0.8` (the FVG weight). Interim behavior: CHAIN_DEFS carries retest at 0.8, Σ = 4.8, Integrity = 4.8/(4.8+ε) on the golden fixture. Needs from owner: none.
- [ISSUE-CP4-008] / [ISSUE-CP4-009] — **withdrawn before commit** (superseded during the build; no code, fixture or test carries either number). The numbering is left gapped so the remaining ids stay stable across the engine docstrings, fixtures and this log.
- [ISSUE-CP4-010] severity: MINOR | status: CLOSED | A: E08 §4's `detect_sc` call site passes `(H−L)/ATR` as RangeZ | B: §3.1 defines RangeZ as the z-score of the bar range against the PIT window (`(x − SMA)/max(SD, ε)`), not an ATR ratio. Rule applied: §3 formula authority (P8). Interim behavior: `range_z` is the z-score; `volume_z` likewise; the §4 call site is recorded as illustrative. Needs from owner: none.
- [ISSUE-CP4-011] severity: MINOR | status: CLOSED | A: E08 §3.2 states `score_position ∈ [0,1]` | B: the stated expression `2·sigmoid(ClosePos − 0.5)` reaches 2.0 at ClosePos = 1. Rule applied: the declared domain is normative. Interim behavior: the score is clamped to [0,1] (the clamp is a no-op for ClosePos ≤ 0.73). Needs from owner: none.
- [ISSUE-CP4-012] severity: MINOR | status: CLOSED | A: E08 §3.2 fully specifies the score matrix only for the ACCUMULATION column | B: the other seven phase columns are under-specified, and a naive zero-fill would push them to zero probability. Rule applied: no invented behaviour — unspecified cells take the neutral 0.5 with provenance `UNSPECIFIED_NEUTRAL`, and are still appended (a missing cell makes the matrix ragged and every downstream softmax fails closed on shape). Interim behavior: `phase_score_matrix` returns `(scores, provenance[phase][component])`; DISTRIBUTION/RE-DISTRIBUTION mirror the EVR sign per the chapter's own note. Needs from owner: none.
- [ISSUE-CP4-013] severity: MINOR | status: CLOSED | A: E08 §0/§3.2 declare 8 phases with θ_H = 0.85 nat | B: §9's worked example claims softmax([0.76,0.55,0.12]) = [0.71,0.18,0.11] with H = 0.52; recomputed it is [0.428,0.347,0.226] with H = 1.066, and with 8 phases Σw = 1 the entropy is 2.076 ⇒ AMBIGUOUS. Rule applied: ADR-P2-007 (re-derive, never copy); §0/§3.2 normative, §9 illustrative. Interim behavior: the battery asserts the recomputation and pins the divergence in the fixture note; the §8 phase-vector fixture records `fate = AMBIGUOUS`. Needs from owner: none (a 3-phase restriction would change the contract; not applied).
- [ISSUE-CP4-014] severity: MINOR | status: CLOSED | A: E08 §3.2's Dirichlet transition matrix uses K = 9 | B: `PHASES` has 8 members, so `T_ij = (N_ij + α)/(N_i + Kα)` rows sum to 8/9 at N = 0 and approach 8.8/8.9 with counts — never 1. Rule applied: implement the stated formula verbatim; the inconsistency is recorded, not silently repaired. Interim behavior: `transition_matrix` exposes `K` as a governed parameter; the battery pins both row sums. Needs from owner: confirm whether K should be 8 (CP-8 triage).
- [ISSUE-CP4-015] severity: MINOR | status: CLOSED | A: E09 §4's ADX early-return path references `di_plus`/`di_minus` before assignment when `dx_list` is empty | B: that path raises NameError instead of returning a warm-up result. Rule applied: canonical Wilder recursion with bound outputs. Interim behavior: `compute_adx_wilder` always returns `{adx, di_plus, di_minus, dx, warmup, warmup_2n}`; on warm-up the values are 0.0 with the flags set. Needs from owner: none.
- [ISSUE-CP4-016] severity: MINOR | status: CLOSED | A: E09 §2 defines the Hurst term as `min(H, 1.2)` | B: §3.8/§4 read `H if 0 ≤ H ≤ 1.2 else 1.0 if H > 1.2 else 0`. Rule applied: the §4 executable form (identical for H ≤ 1.2; both clipped to [0, 1.5]). Interim behavior: `compute_trend_quality` implements the §4 form and pins both readings in the battery. Needs from owner: none.
- [ISSUE-CP4-017] severity: MINOR | status: CLOSED | A: E09 §8.4 says "correlation > 0.85 ⇒ redundant" | B: §8.6 says "> 0.9 … threshold 0.85" in the same sentence. Rule applied: the stated threshold value 0.85 (it appears in both clauses). Interim behavior: `redundancy_corr_th = 0.85`, governed. Needs from owner: none.
- [ISSUE-CP4-018] severity: MINOR | status: CLOSED | A: E09 §3.5 states ADX is not trustworthy before `t ≥ 2n` (NaN until then) | B: the §4 warm-up flag is raised at `n`, so a value between `n` and `2n` was presented as usable. Rule applied: §3.5 boundary. Interim behavior: `compute_adx_wilder` returns `warmup` (t < n) **and** `warmup_2n` (t < 2n); either flag degrades the scale with `ADX_WARMUP_QX` instead of publishing an untrustworthy ADX. Needs from owner: none.
- [ISSUE-CP4-019] severity: MINOR | status: CLOSED | A: E09 §8.1 FIX_03 (sideways) expects `MICRO_dir = 0` | B: on the fixture's own 5 bars §3.2/§4 give slope_z = 1.0742 (β 1.6671e-05 / SE_NW 1.5519e-05), pos = 0.0267, seq = 0 ⇒ z = 0.3827, which exceeds the §4 deadband 0.05 ⇒ direction +1. Rule applied: formula authority (ADR-P2-007); the fixture keeps the doc's expectation (0) *and* the re-derived value (+1). Interim behavior: the sideways diagnosis is carried by the engine's own §5.4 EV_TRD_007 (|H−0.5| < 0.1 ∧ ADX < 20 ∧ R² < 0.3 all hold: H = 0.5, ADX = 0, R² = 0.0192), which the battery asserts. Needs from owner: confirm whether the deadband should apply to z or to strength (CP-8 triage).
- [ISSUE-CP4-020] severity: MINOR | status: CLOSED | A: E09 §8.1 FIX_01 expects `Q_min = Q2` on a 5-bar window | B: §3.5 needs ≥ 2n directional samples for any ADX, so at 5 bars ADX = 0 (warm-up) and Q = R²·(ADX/100)·H = 0 ⇒ Q0. Rule applied: ADR-P2-007 re-derive. Interim behavior: the fixture asserts the re-derived Q0 with `warmup_2n = True` and records the divergence; every other FIX_01 expectation (dir +1, seq 1.0, slope_z ≥ 1.0, evidence 3) holds as written. Needs from owner: none.
- [ISSUE-CP4-021] severity: MINOR | status: CLOSED | A: E07 §3.1 defines `WeightCoverage = Σ_{i∈O} w_i / Σ_i w_i` (no ε) and `Integrity = Σ w_i s_i / (Σ w_i + ε)` (with ε) | B: §4's pseudo-code reuses one ε-inflated `total_w` for both, so a complete chain reported coverage 0.999999998 instead of 1.0. Rule applied: §3 formula authority (ISSUE-CP3-001 precedent). Interim behavior: Integrity keeps the ε denominator; coverage is the exact ratio. Needs from owner: none.
- [ISSUE-CP4-022] severity: MINOR | status: CLOSED | A: E08 §4 re-derives the range boundary from the trailing window on every bar | B: §5's state machine is one-way (NO_RANGE → RANGE_DETECTED → SC → … → MARKUP) with only INVALIDATED and EXPIRED as exits, and an SOS breakout necessarily widens the trailing High−Low beyond 1.5·ATR — re-deriving therefore discards the range exactly when the cycle advances. Rule applied: §5 state-machine authority. Interim behavior: the boundary is latched on first detection and aged thereafter; INVALIDATED/EXPIRED release the latch (and clear `low_sc`, which would otherwise re-invalidate every later bar) so a fresh cycle can start. Needs from owner: none.
- [ISSUE-CP4-023] severity: MINOR | status: CLOSED | A: E09 §5.1/§5.2 declare 18 TrendScale keys and 6 TrendStack keys as required | B: the driver published only the subset the stack reads (`scale`, `as_of`, `contract_version` and the stack's `stack`/`as_of` were absent). Rule applied: §5 schema authority — the validated object is published whole. Interim behavior: `TrendScale.to_dict()` / the payload carry every required key; `snapshot_id` is derived from `to_canonical()` only, so publishing the extras cannot move it. Needs from owner: none.
- [ISSUE-CP4-024] severity: MINOR | status: CLOSED | A: E08 §5's `cycle_state` JSON has no degradation key | B: a cycle computed from undefined evidence (NaN EVR/VR) must stay observable — a computed-but-unpublished flag is a silent failure. Rule applied: fail-closed observability; the wire schema is not altered. Interim behavior: `degraded`/`degraded_reason` are published as operational extras (like `idempotency_key`) and never enter the canonical snapshot payload; the EvidenceEvent's `validity` reflects the same state. Needs from owner: none.
- [ISSUE-CP4-025] severity: MINOR | status: CLOSED | A: E07 §8.1's UTC fixture expects `which = [UTC_W1, UTC_W2, UTC_W1_W2_OVERLAP]` at 14:00Z | B: §3.6 defines UTC_W1 = 07:00–12:30 and UTC_W2 = 12:30–21:00 — disjoint, so 14:00 belongs to UTC_W2 alone. Rule applied: §3.6 window definitions + geometric truth. Interim behavior: the engine emits the true membership plus the derived overlap tag (`is_overlap = True`, 12:30–16:00); the fixture records both readings. Needs from owner: none.
### CP-5
- [ISSUE-CP5-001] severity: MINOR | status: CLOSED | A: E10 §6 gives θ_acc unit "z" and §9 reads `a_z = a_t/σ_v` | B: §4 pseudocode compares the RAW acceleration `a_t` against the threshold. Rule applied: §6/§9 unit authority. Interim behavior: acceleration is normalized to z-scale before thresholding. Needs from owner: none.
- [ISSUE-CP5-002] severity: MINOR | status: CLOSED | A: E10 §3.5 "Final" clauses conjoin the MvZ (momentum-vs-zoom) context | B: §2/§4/§9 define the detectors without it and MvZ is optional context. Rule applied: §3.5 governs WHEN MvZ context is available; pure-state detectors evaluate the core condition otherwise (GF05/GF06 shape). Interim behavior: absent MvZ ⇒ no downgrade; incompatible MvZ ⇒ −1 strength adjustment. Needs from owner: none.
- [ISSUE-CP5-003] severity: MINOR | status: CLOSED | A: E10 §6 `reference_mode` default "velocity" | B: §4/§8.1/§9 all use the RSI auxiliary. Rule applied: executable-sections authority. Interim behavior: default "RSI"; all five enum modes wired and governed. Needs from owner: confirm the intended default (CP-8 triage).
- [ISSUE-CP5-004] severity: MINOR | status: CLOSED | A: E10 §5.1 schema requires `param_hash` | B: §4 pseudocode emits `param_fingerprint`. Rule applied: schema wins. Interim behavior: the state carries `param_hash` (12-hex sha256 over the canonical param dict). Needs from owner: none.
- [ISSUE-CP5-005] severity: MINOR | status: CLOSED | A: E10 §5.1 interval enum lists 6 timeframes | B: §9.5-8 universality freezes a 14-TF universe. Rule applied: the frozen universe governs; the schema enum is recorded as a doc divergence. Interim behavior: any universe TF accepted; unknown interval fails closed. Needs from owner: none.
- [ISSUE-CP5-006] severity: MINOR | status: CLOSED | A: EV_MOM_007 fires on "low D_mag" — unquantified | B: §7-Ch3-D9 says "small D_mag (<0.2) should be filtered out". Rule applied: the 0.2 threshold is derived from the encyclopedia clause. Interim behavior: D_mag < 0.2 ⇒ filtered + EV_MOM_007. Needs from owner: none.
- [ISSUE-CP5-007] severity: MINOR | status: CLOSED | A: E10 §3.4 disagreement ⇒ Q2 is missing from the §4 code; the ROC index is off by one; duplicate calls rebuild state | B: §3.4/§2 formulas govern; duplicates return the cached state (§4 text). Rule applied: formula authority (ADR-P2-007). Interim behavior: all three fixed in the engine; battery pins each. Needs from owner: none.
- [ISSUE-CP5-008] severity: MINOR | status: CLOSED | A: E10 impulse_score has no specified formula | B: a deterministic derived normalization is required (no stubs). Rule applied: fail-closed determinism. Interim behavior: `impulse_score = min(1, |Mz| / (2·θ_imp))` while an impulse is ACTIVE, else 0.0; pinned by the battery. Needs from owner: confirm the normalization (CP-8 triage).
- [ISSUE-CP5-009] severity: MINOR | status: CLOSED | A: E11 §3.1 Method-A prose gives x=0.5 for a degenerate window (max−min<ε) | B: §4 pseudocode raises INVALID_E11_HISTORY. Rule applied: fail-closed pseudocode governs (also for Method-B: a constant window raises rather than returning sigmoid(0)); the ε-regularized σ of the §3.1 formula is kept for the division. Interim behavior: degenerate/short/non-finite history ⇒ ValueError. Needs from owner: none.
- [ISSUE-CP5-010] severity: MINOR | status: CLOSED | A: E11 §9 case study compares H_norm = H/ln9 against θ_H (0.73 > 0.65) | B: §4 pseudocode, §6 (θ_H unit: nat) and §8.1 GF_01 (expected_H_lt 0.7 with expected_Q Q4, which the cascade only grants for raw H < 0.65) compare RAW entropy in nats. Rule applied: pseudocode governs. Interim behavior: θ_H thresholds raw nats; H_norm is exposed as a wrapper-level diagnostic only (§5.1 forbids extra state keys). Needs from owner: none.
- [ISSUE-CP5-011] severity: MINOR | status: CLOSED | A: E11 §3.8 gap adjustment (expansion→1, structure_quality×0.7) is absent from the §4 pseudocode | B: GF_09 requires it. Rule applied: both honored — the adjustment is implemented in the streaming wrapper post-normalization. Interim behavior: gap candle ⇒ expansion := 1, sq ×= 0.7 after the norms; battery pins GF_09. Needs from owner: none.
- [ISSUE-CP5-012] severity: MINOR | status: CLOSED | A: E11 §3.8 V=0 ⇒ Q1 with x6 moving-average fill | B: GF_10 shows participation_raw=None ⇒ Q0 CONFIGURATION_INVALID. Rule applied: a missing/None required input key always fails closed (Q0, pseudocode); an explicit zero-volume candle (v==0 with participation_raw present or fillable) takes the §3.8 path. Interim behavior: fill + Q1 cap + EV_RGM_006 for v==0; Q0 for missing keys. Needs from owner: none.
- [ISSUE-CP5-013] severity: MINOR | status: CLOSED | A: EV_RGM_007 is missing from the E11 §5.4 event table | B: the §4 pseudocode emits it for CONFIGURATION_INVALID / FAIL_CLOSED. Rule applied: pseudocode governs. Interim behavior: the catalog carries EV_RGM_001–007; GF_15 pins the fail-closed emission (no probabilities). Needs from owner: none.
- [ISSUE-CP5-014] severity: MINOR | status: CLOSED | A: E11 μ0/Σ0/prev_mom initialization is unspecified | B: deterministic PIT-safe seeds are required. Rule applied: chosen μ0 = 0.5·1 (neutral center), Σ0 = 0.05·I (the §9 case-study covariance diagonal scale) + 1e-6·I regularization, prev_mom = 0.5 (the NEUTRAL stationary point, matching case-study candle 1 x8=0.5). Interim behavior: engine defaults; overridable via run_engine. Needs from owner: none.
- [ISSUE-CP5-015] severity: MINOR | status: CLOSED | A: E11 §8.1 GF_01 expected_vector.momentum_state = 0.62 | B: not reproducible from the Method-C recursion with any neutral seed (0.94·0.5 + 0.06·1 = 0.53). Rule applied: ADR-P2-007 re-derivation; the document value is logged, never used. Interim behavior: fixture asserts 0.53. Needs from owner: none.
- [ISSUE-CP5-016] severity: MINOR | status: CLOSED | A: E11 §8.1 GF_01 expected_state TREND_EXPANSION | B: contradicts its OWN expected_vector through the §3.2 tree (expansion 0.76 ≥ 0.6 ∧ participation 0.86 ≥ 0.4 fire EXPANSION first). Rule applied: ADR-P2-007. Interim behavior: re-derived fixture keeps the document raw inputs but a participation window yielding x_part < 0.4, so the expected state is reachable and asserted. Needs from owner: none.
- [ISSUE-CP5-017] severity: MINOR | status: CLOSED | A: the E11 §4 pseudocode's EV_RGM_003 guard (hist_raw[-1] != confirmed_state under status CONFIRMED) is provably dead under its own hysteresis_manager | B: the §5.4 event table and the §9 case study (candle 6 emits EV003) require the event. Rule applied: event table + case study govern. Interim behavior: EV_RGM_003 fires on the confirming candle where the CONFIRMED label actually changes (last_confirmed tracking); GF_11 pins EV_RGM_002@[2,3] / EV_RGM_003@[4]. Needs from owner: none.
- [ISSUE-CP5-018] severity: MINOR | status: CLOSED | A: E11 §8.1 GF_05 expects CHOP ∧ Q3 | B: unreachable — Q3 requires H ≥ 0.65 (⇒ TRANSITION in the tree) or Tur ≥ 15.5 (⇒ CRISIS). Rule applied: ADR-P2-007. Interim behavior: re-derived as CHOP ∧ Q4 with the document inputs kept. Needs from owner: none.
- [ISSUE-CP5-019] severity: MINOR | status: CLOSED | A: the E11 §4 pseudocode hysteresis_manager's trailing else confirms a new label after only 2 consecutive raw labels | B: §2 says "3 consecutive candles" and the pseudocode's own first branch agrees. Rule applied: §2 governs; a continuation branch was added (new == confirmed_prev ⇒ CONFIRMED without a streak, so an unchanged label never re-arms SUSPECTED). Interim behavior: the engine tracks the last CONFIRMED label so SUSPECTED candles retain it. Needs from owner: none.
- [ISSUE-CP5-020] severity: MINOR | status: CLOSED | A: E12 §8.1 GF10 expects `rollover_active=true` | B: §3.1 makes rollover an exchange-configured event with NO universal CME schedule and the frozen default configuration is empty (⇒ never active). Rule applied: config authority — `rollover_windows` is injected runtime configuration ({"weekdays", "start_h", "end_h"}). Interim behavior: GF10 verified with an explicit Saturday [00:00,01:00) configuration; default [] ⇒ rollover_active=False always. Needs from owner: provide the canonical Toobit rollover configuration when one exists (CP-8 triage).
- [ISSUE-CP5-021] severity: MINOR | status: CLOSED | A: E12 §8.1 GF01 (2024-03-15, a Friday) expects `expected_daytype="WEEKDAY"` | B: the §4 pseudocode returns FRIDAY for weekday()==4 and GF11 (same date) expects FRIDAY. Rule applied: pseudocode governs (ADR-P2-007). Interim behavior: GF01 re-derived to FRIDAY. Needs from owner: none.
- [ISSUE-CP5-022] severity: MINOR | status: CLOSED | A: E12 §8.1 GF09 expects `gap=true` | B: the §3.1 formula (O_t > H_{t−1}·1.1 or O_t < L_{t−1}·0.9) gives 71000 ≤ 77000 ⇒ no gap. Rule applied: formula authority (ADR-P2-007). Interim behavior: GF09 re-derived to gap=false; the gap law is separately proven with synthetic >10%/<−10% data. Needs from owner: confirm whether the 1.1/0.9 multipliers are intended (a 10% gap threshold is extreme for crypto) (CP-8 triage).
- [ISSUE-CP5-023] severity: MINOR | status: CLOSED | A: E12 §8.1 GF10 (00:30 UTC) expects `expected_temporal_window="UTC_W3"` | B: σ(00:30) ∈ [00:00,07:00) ⇒ UTC_W0 per §3.5/§4. Rule applied: registry authority (ADR-P2-007). Interim behavior: GF10 re-derived to UTC_W0. Needs from owner: none.
- [ISSUE-CP5-024] severity: MINOR | status: CLOSED | A: E12 §4.1 `fff_seasonal` carries a vestigial `use_sqrt` parameter | B: §3.2/§11(a) forbid the biased absolute-value estimator ("never"). Rule applied: fail-closed instead of silently ignoring the flag. Interim behavior: `use_sqrt=True` raises FFF_ABS_ESTIMATOR_FORBIDDEN; the squared-magnitude estimator is the only implementation. Needs from owner: none.
- [ISSUE-CP5-025] severity: MINOR | status: CLOSED | A: E12 §4.2 imports `scipy.stats` (kruskal, spearmanr) | B: scipy is NOT among the nine SBOM pins (requirements.lock) and adding a runtime dep is forbidden. Rule applied: in-tree implementation (E04 precedent; no cross-engine import — P3). Interim behavior: Kruskal–Wallis with tie correction + χ² survival via an in-tree regularized incomplete gamma (stdlib math only), Spearman ρ as Pearson on mid-ranks; χ² pinned against known quantiles (3.841/df1, 7.815/df3, 9.8/df3≈0.0204). Needs from owner: none.
- [ISSUE-CP5-026] severity: MINOR | status: CLOSED | A: E12 §8.1 GF05 states `VR: 2.5` | B: a single candle cannot yield a VolRatio (§2: VR = V_t / mean(V_window, 180d)) — window-volume history is required. Rule applied: ADR-P2-007 deterministic re-derivation. Interim behavior: GF05 verified by injecting the canonical window mean volume 8000 ⇒ 20000/8000 = 2.5, with y_vol = [2.5 > 1.5] = True. Needs from owner: none.
- [ISSUE-CP5-027] severity: MINOR | status: CLOSED | A: E12 §4 `on_candle` emits a minimal state | B: §5.2 TemporalContextState_v4 requires `version`, `tod_state{hour_utc,minute,weekday,tod_bin,utc_window_progress}` and `rollover_active`. Rule applied: §5.2 schema is the canonical artifact — the engine state merges both (pseudocode keys ⊂ schema keys) and the hashed §5.1 payload binds the complete deterministic state minus snapshot_id. Interim behavior: validate_state_schema enforces §5.2; canonical_json NaN-guard sanitizes Q0 input payloads (NaN ts ⇒ None inside the hashed input, reason INVALID_TS). Needs from owner: none.
### CP-6
- [ISSUE-CP6-001] severity: MINOR | status: CLOSED | A: Ch.8 §8.1/§8.2 describe ``redundancy_penalty`` with "operational value 0.25" | B: Ch.10 §10.1 Gate 4 fires at ``redundancy_penalty > 0.3`` and the frozen ``params/setup_weights_v1.yaml`` (CP-1) carries 0.3. Rule applied: the governed YAML is the single numeric authority (params-win precedence); the 0.25 prose value is documented, never used. Interim behavior: Gate 4 reads redundancy_penalty (0.3) from the YAML; the ρ>0.85 halving (0.50×) is a separate law, asserted independently. Needs from owner: none.
- [ISSUE-CP6-002] severity: MAJOR | status: CLOSED | A: the stage mandate carries evidence decay 0.02/bar, context decay 0.05/bar and the context band grid 0.70/0.50/0.30 | B: APEX_GEN5 Ch.8 defines propagation 0.70/0.40 (collapse when ``data_trust < 0.30``), Q-window λ=0.1 and forecast λ=0.1; the 0.05/0.02 pair and the three-band grid do NOT appear in the frozen chapter text (grep over Ch.8 found no occurrence). Rule applied: blueprint normative (STEP-3 law; G4 no silent deviation) — both law-sets coexist: §8.0 propagation table stays the adjudicator, the prompt numbers were admitted as governed additive keys in ``params/setup_weights_v1.yaml`` and are asserted FROM the YAML. Interim behavior: decay 0.05/0.02 drive the freshness factors of the setup-score combiner only; ``band_of`` (0.70/0.50/0.30) is a classification label that grants nothing; propagation threshold remains 0.70/0.40. Needs from owner: confirm or retire the prompt numbers (CP-8 triage).
- [ISSUE-CP6-003] severity: MINOR | status: CLOSED | A: Ch.11 AE.5 instantiated playbook: BE at 1.0R, trail_after 1.5R, trail 1.0×ATR, max_hold 16 bars | B: generic X.1–X.3 tables: staged exits 0.8/1.0/1.2 R, trail_after 1.0R, regime-dependent 2.0/2.5/3.0×ATR, max_hold 96/60/40/20 per TF class. Rule applied: the playbook-specific instantiation is the more specific law (Ch.11 preamble). Interim behavior: PB_FVG_SWEEP_REV_A literal values are asserted by the playbook battery and pinned in the record's ``fallback_source: FROZEN_BOOTSTRAP_X2`` field; the generic table is retained only as fallback metadata, never applied silently. Needs from owner: none.
- [ISSUE-CP6-004] severity: MINOR | status: CLOSED | A: Ch.15 veto table rows 10–12 cite error code ``CIRCUIT_OPEN (Alert Policy)`` | B: ``CIRCUIT_OPEN`` is not defined in the frozen Ch.7 registry (``apex/errors.py`` ERROR_REGISTRY, CP-1) and codes may not be invented (G6). Rule applied: frozen registry is the sole code authority. Interim behavior: registry rows for vetoes 10–12 carry ``error_code=None`` with the Ch.15 policy name kept in the veto line; ``veto_definition`` builds the registry string through ``get_error_code`` without KeyError. Needs from owner: decide whether CIRCUIT_OPEN joins the registry in a later phase (CP-8 triage).
### CP-7
- [ISSUE-CP7-001] severity: MAJOR | status: CLOSED | A: the CP-7 stage RULES require the wire map to carry the business codes −1120/−1021/−1022/−1003/−2026 | B: APEX_GEN5.md defines −1120 (Ch.16 L16877 interval disabled), −1022 and −1003 (Ch.16 L16861–16863 reject/abort class) but **−1021 and −2026 appear nowhere in the frozen blueprint** (grep over the whole file: zero occurrences), and codes may not be invented (G6). Rule applied: never invent semantics — an unlisted code is UNMAPPED ⇒ outcome UNKNOWN ⇒ RECOVERY_REQUIRED + reconcile-before-action (Ch.16 L16861–16863), never a silent retry and never a guessed classification. Interim behavior: `classify_business_code(-1021/-2026)` returns `UNMAPPED_CODE` with `outcome="UNKNOWN"`, `classification="RECONCILE"`, `resubmit=False`; pinned by `tests/unit/test_toobit_map.py::TestBusinessCodeClassification` and by conformance cases C-16/C-17 in `tests/fixtures/toobit_adapter_conformance.json` (hash-locked). Needs from owner: confirm the intended venue semantics of −1021 and −2026 (CP-8 triage) — if either is a benign class, the mapping row can be added without touching the FSM.
- [ISSUE-CP7-002] severity: MINOR | status: CLOSED | A: Ch.16 L16852 + AI.3 freeze the adapter surface at **exactly five operations** | B: the merged wire block L16884–16895 freezes **eleven signed paths** and forbids any unlisted path, while Ch.23 L18247–18249 (RECONCILING needs working orders + recent fills), Ch.21 §5.7 (CANCEL_ALL up to 12) and Ch.16 L16892–16895 (ISOLATED/ONE_WAY, leverage, commissionRate) each need a path that is not one of the five operation endpoints. Rule applied: the OPERATION surface stays five; every auxiliary path is reachable only **inside** one named operation and is never exposed as a sixth operation. Interim behavior: `OPERATION_ENDPOINT` (5 rows) + `OPERATION_AUXILIARY_PATHS` (batchOrders→cancel_order; openOrders+userTrades→query_order_state; leverage+marginType+commissionRate→query_account_margin_health; submit_order/query_open_positions none), asserted by `tests/unit/test_toobit_map.py::TestFiveOperations` and `tests/unit/test_toobit_adapter.py::TestFiveOperationsLaw` (incl. the intersection rule and the no-unlisted-path guard). Needs from owner: none.
- [ISSUE-CP7-003] severity: MINOR | status: CLOSED | A: P2/G4 — parameter values live ONLY in `params/*.yaml`; code loads, never hardcodes | B: CP-7's WRITE SET excludes `params/*.yaml`, and several CP-7 constants have **no YAML home** (Ch.17 L16993–17011 heartbeat/backup/clock-drift/timeout defaults, the Ch.21 §10 Telegram parameter table, the Ch.21 §5 control-plane literals, the Ch.16 L16802 order-type table). Rule applied: adding keys to a CP-1-owned YAML would be an out-of-scope edit, so the literals are frozen **verbatim in the owning module with the blueprint line cite** and asserted literal-by-literal by tests (the value is still single-source: one constant, one test, one cite). Interim behavior: `apex/telegram/signaling.py::TELEGRAM_PARAMS`, `apex/telegram/control_plane.py` frozen literals, `apex/execution/fsm.py` timeout constants, `apex/execution/toobit_map.py` order table — each with a checked view asserted in `tests/unit/test_telegram_signaling.py::TestParameterTable`, `test_telegram_control_plane.py::TestFrozenLiterals`, `test_execution_fsm.py::TestTimeouts`, `test_toobit_map.py::TestSidesAndOrderDefaults`. Needs from owner: decide at CP-8 triage whether these move into a params file (a test would then read them from YAML instead of the module).
- [ISSUE-CP7-004] severity: MINOR | status: CLOSED | A: Ch.17 L16995 `clock_drift_tolerance = 5 s` (static/architecture) and Ch.23 L18244–18245 "drift beyond the governed tolerance blocks new trades" | B: AI.7 L18723–18724 requires NTP sync **within ±100 ms** and sets **E12 DEGRADED + trading paused when drift > 500 ms**. Rule applied: the three numbers govern three different things and all three are kept — ±100 ms is the NTP *sync target* (`NTP_SYNC_TARGET_SECONDS`, reported, not gated), 500 ms is the E12/trading-pause threshold, 5 s is the boot SELF_TEST tolerance whose breach blocks new trades. Interim behavior: `StartupReconciliation._check_clock_sync` returns PASS ≤0.5 s, DEGRADED >0.5 s (E12 degraded, time-based gates QX, trading paused), DEGRADED >5 s (new trades blocked, the boot matrix's explicit `DRIFT_BEYOND_TOLERANCE → DEGRADED` edge), and **UNAVAILABLE when drift cannot be measured** (never assumed synchronized); `drift_blocks_new_trades` uses the stricter 500 ms; the scheduler blocks a cell at either threshold. Asserted by `tests/unit/test_execution_fsm.py::TestBootMachine`, `tests/unit/test_scheduler_clock.py::TestDrift` + `TestDriftBlock`, `tests/integration/test_cp7_paper_loop.py::TestDriftBlocksTheLoop`. Needs from owner: none.
- [ISSUE-CP7-005] severity: MINOR | status: CLOSED | A: Ch.16 L16802 governed order table: "Stop order | STOP, GTC" and "Target | LIMIT, GTC" | B: the wire block L16899 fixes `type=STOP` with `priceType=MARKET` for the protective stop and forbids `type=MARKET` outright, but is silent on the stop's `timeInForce`; `reduceOnly` is required for protective legs by Ch.15/Ch.16 semantics yet not spelled out per leg. Rule applied: the more specific wire block governs the **wire fields** (type/priceType), the governed order table governs the **TIF**, and both protective legs are `reduceOnly=true` (they may only reduce, never add). Interim behavior: entry = LIMIT/IOC/priceType INPUT; stop = STOP + priceType MARKET + timeInForce GTC + reduceOnly true; target = LIMIT + GTC + reduceOnly true; aggressive flatten after IOC-0 = LIMIT + priceType MARKET (never `type=MARKET`). Asserted by `tests/unit/test_execution_fsm.py::TestProtection`, `tests/unit/test_toobit_map.py::TestSidesAndOrderDefaults` and conformance cases in `tests/fixtures/toobit_adapter_conformance.json`. Needs from owner: none.
- [ISSUE-CP7-006] severity: MINOR | status: CLOSED | A: Ch.21 §10 frozen table: chart **1200×800 px, PNG, quality 90** | B: the Ch.21 §14 illustrative snippet (L18062–18082) calls `savefig(dpi=150)` with a `quality=90` keyword that matplotlib's PNG writer does not accept (PNG is lossless — there is no encoder quality knob). Rule applied: the §10 parameter table is normative for the artifact; the snippet is illustrative. Interim behavior: `render_chart()` produces exactly 1200×800 px in memory (figsize 12×8 at dpi 100), Agg backend only, never written to the filesystem; `quality: 90` is carried in the message metadata and the IHDR width/height are asserted from the produced bytes (`tests/unit/test_telegram_signaling.py::TestCharts`). Needs from owner: none.
- [ISSUE-CP7-007] severity: MINOR | status: CLOSED | A: Ch.21 §5.1 pseudo-render header reads "APEX — Full Unlock — PAPER 10,000 USDT — **UTC+3:30**" | B: Ch.21 §5.6 mandates UTC for internal processing **and display** ("local-device timezone is never used"), and Y.3 L17437 bans local offsets/DST conversion from normative runtime logic. Rule applied: the normative settings law wins over an illustrative render. Interim behavior: every screen header and timestamp is UTC (`TIMEZONE_DISPLAY = "UTC"`), and `tests/unit/test_telegram_control_plane.py::TestScreens` asserts the string "UTC+3:30" never appears. Needs from owner: none (a device-local convenience display, if ever wanted, must stay non-normative).
- [ISSUE-CP7-008] severity: MINOR | status: CLOSED | A: the frozen Ch.5 ledger DDL (L14507–14522) carries `CHECK(typeof(price)='text')`, `CHECK(typeof(quantity)='text')`, `CHECK(typeof(fee)='text')` | B: those CHECKs **reject SQL NULL**, while Ch.16 events legitimately carry absent money (a transition with no fill, a cancel with no fee, an outcome with no slippage). Rule applied: the CP-1 DDL is untouchable and NULL must not be treated as legal; the store boundary encodes "absent" as the empty TEXT `''` and maps it back to `None` on read. Interim behavior: `LedgerEntry.to_row()` / `_row_to_entry()` in `apex/ledger/store.py`; money that IS present stays a Decimal TEXT string (never a float, never a NUMERIC column). Asserted by `tests/unit/test_ledger_store.py::TestMoneyBoundary` and `tests/integration/test_cp7_paper_loop.py::TestCleanLoop::test_money_is_text_decimal_at_the_store_boundary` (`typeof(...)='text'` for every ledger row). Needs from owner: decide at CP-8 triage whether the DDL should permit NULL for absent money (that would need a CP-1-owned migration).
- [ISSUE-CP7-009] severity: MINOR | status: CLOSED | A: Ch.21 §10 caps an inline keyboard at **8 buttons / 4 rows** | B: Ch.21 §5.1 shows **eight domain buttons plus the global Back/Home controls** on every screen (10 buttons / 5 rows), and §5.1 also requires the global controls on every screen. Rule applied: the §10 budget governs the **domain** buttons; the two global controls are rendered as their own final row so Back/Home is always reachable; overflow beyond the budget is truncated and **reported** (E-TELE-005 + Q2_DEGRADED), never silently dropped. Interim behavior: `ControlPlane.render()` emits `inline_keyboard = domain rows + global row` with `domain_button_count ≤ 8`, `domain_row_count ≤ 4`, `keyboard_truncated`/`keyboard_error_code` surfaced; asserted by `tests/unit/test_telegram_control_plane.py::TestScreens` (every screen) and `tests/unit/test_telegram_signaling.py::TestFormatting`. Needs from owner: none.
### CP-8 (closeout + triage ledger — every still-open issue gets a final disposition here)
- [ISSUE-CP8-001] severity: MAJOR | status: CLOSED | A: Ch.17 §17.1's governed-default table lists `budget_per_trade = 1.0 %` and `k_attn = 0.05` | B: the frozen `params/risk_defaults_v1.yaml` (CP-1, §9.5-10/§9.5-11 FROZEN_BOOTSTRAP) carries `budget_per_trade = 0.005` and `k_attn = 0.25`; the two numbers cannot both be the live value. Rule applied: §9.5-10 (params live ONLY in `params/*.yaml`; code loads, never hardcodes) + §9.5-11 FROZEN_BOOTSTRAP + G4/G5 (no silent deviation, no late redesign) — the YAML is the runtime authority and CP-8 never rewrites it. Interim behavior: `apex.research.governance.GOVERNED_DEFAULTS` records the blueprint's own number for every §17.1 row and compares it with the frozen YAML (`assert_yaml_consistency()`), reporting each mismatch as a `doc_inconsistency` note instead of reconciling it (`tests/unit/test_research_governance.py::TestRegistry::test_yaml_backed_values_match_or_are_recorded`); the runtime keeps the YAML values. Needs from owner: confirm that §9.5-11's freeze supersedes the Ch.17 §17.1 example numbers (recommendation: yes — §9.5-11 is the freeze statement; the alternative is an owner-ordered YAML revision, which no executor may perform).
- [ISSUE-CP8-002] severity: INFO | status: CLOSED | A: Ch.20 AA.7-2 lists performance/PARALLELIZATION work for the proxy layer | B: Numba is not one of the nine frozen SBOM pins and is an explicit §9.5-9 Wave-Out item ("Numba (not in SBOM)"). Rule applied: Wave-Out + G6 — the proxy layer must stay pure-Python/pandas and may never import it. Interim behavior: `apex.research.proxies.NUMBA_USED = False` with the Wave-Out citation in the module docstring; the registry asserts the incremental (O(1)) law instead (`INCREMENTAL_ONLY = True`), and `tests/unit/test_research_proxies.py` fails if the module ever gains a numba import. Needs from owner: none.
- [ISSUE-CP8-003] severity: MINOR | status: CLOSED | A: [ISSUE-CP7-001] (CLOSED) states that the −1021/−2026 UNMAPPED semantics are "pinned by … conformance cases C-16/C-17 in `tests/fixtures/toobit_adapter_conformance.json` (hash-locked)" | B: the fixture's C-16 is `query_open_positions` and C-17 `query_account_margin_health`; the codes are actually carried by **C-09 (−1021)** and **C-10 (−2026)** (verified this session against the hash-locked fixture, `cases_sha256 cd1ba348…29c17`, and independently re-run by `scripts/run_adapter_conformance.py` 19/19 PASS). Rule applied: citations are never silently rewritten (G13) — the predecessor entry stands and the correction is filed here. Interim behavior: the mapping rule itself is unchanged (UNMAPPED ⇒ UNKNOWN ⇒ RECOVERY_REQUIRED, no silent retry, no invented classification) and remains covered by `tests/unit/test_toobit_map.py::TestBusinessCodeClassification` + cases C-09/C-10. Needs from owner: none (citation-only).
- [ISSUE-CP8-004] severity: MINOR | status: CLOSED | A: §9.5-12 fixes the runtime environment at **nine names** | B: `apex/config.py::_load_env` additionally reads `APEX_DOTENV_PATH` (a tenth name) to locate the `.env` file; it is documented in the CP-7 README run block and in `PHASE2_HANDOFF_CP7.md` §HOW-TO-RUN and is a **path pointer only** — it can never carry a credential, and the no-shadow rule keeps real process env authoritative. Rule applied: FROZEN config surface + G4 (no late redesign of a CP-1-owned module for a cosmetic name count) with full disclosure instead of a silent change. Interim behavior: the name is not used to read any secret; the nine-name contract is still asserted (`tests/unit/test_config.py`), and `apex.ops.watchdog.watchdog_summary()["env_names_added"] == []` proves the CP-8 ops plane introduced no new name. Needs from owner: decide whether the `.env` path pointer must be removed from the shipped config (recommendation: keep — it is documented, secret-free and test-covered; removing it would break the documented run block).
- [ISSUE-CP8-005] severity: INFO | status: CLOSED | A: P13 demands a final disposition for every still-open issue before closure | B: five predecessor issues were still `OPEN`/`OPEN-UNTIL-CP-5` at CP-8 entry (ISSUE-CP1-004, ISSUE-CP1-005, ISSUE-CP2-006, ISSUE-CP2-016, ISSUE-CP4-005). Rule applied: CP-8 owns the closeout ledger; predecessor entries stay immutable and the dispositions are recorded here. Interim behavior (dispositions): **ISSUE-CP1-004 → CLOSED-with-evidence** — CP-2/CP-3 supplied the chapter formulas (E02/E03 parameter tables + engines) and the remaining §3.12 `k_*/theta_*`/GARCH catalogue slots stay UNAVAILABLE by design (never a guessed formula); evidence `tests/unit/test_catalog.py::TestRegistryCompleteness`, `tests/unit/test_e02_liquidity.py`, `tests/unit/test_e03_volume.py`. **ISSUE-CP1-005 → CLOSED-with-evidence** — the producers named as missing (E12 temporal windows CP-5, regime/fabric CP-6) now exist in-tree and the AI.6 permission model keeps context-gated features UNAVAILABLE only while their data is absent; evidence `tests/unit/test_e12_temporal.py::TestTE12Windows`, `tests/unit/test_fabric_context.py`, `tests/unit/test_catalog.py::TestTierCacheRules`. **ISSUE-CP2-006 → CLOSED-by-CP-8-decision** (the issue itself delegates the disposition to CP-8): the normative tree's six `params/*.yaml` files stay the only YAML set — no seventh file and no patch to frozen `apex/config.py`; each engine's in-package §6 table remains the single source, asserted literal-by-literal by that engine's `TestParameters`. **ISSUE-CP2-016 → CLOSED-by-CP-8-decision**: the undefined `formula_k_*`/`theta_*` catalogue slots stay UNAVAILABLE (fail-closed), F74's `theta_sweep` pathway stays available for a future producer, and no formula is invented; evidence `tests/unit/test_catalog.py` + E01/E02/E03 `TestParameters`. **ISSUE-CP4-005 → CLOSED-with-evidence**: CP-5 re-tested both E07 modes and the E12 authority now exists (`tests/unit/test_e07_rtm.py::TestUTCWindows`, `tests/integration/test_cp5_engines.py::TestE07E12BothModes`). Needs from owner: none.
- [ISSUE-CP8-006] severity: INFO | status: ESCALATED-TO-OWNER | A: the CP-6/CP-7 triage backlog carries owner-only questions | B: no executor can define venue semantics, a new registry code, a DDL change or the retirement of prompt-supplied numbers. Rule applied: P13 — owner decisions are escalated verbatim, never answered by an agent. Verbatim escalations: **ISSUE-CP6-002** "Needs from owner: confirm or retire the prompt numbers (CP-8 triage)." — CP-8 reading: both law-sets stay, the §8.0 propagation table (0.70/0.40) is the adjudicator and the prompt numbers (0.05/0.02 decay, 0.70/0.50/0.30 band labels) are governed additive YAML keys granting nothing (`tests/unit/test_fabric_context.py::TestFrozenNumbers`). **ISSUE-CP6-004** "Needs from owner: decide whether CIRCUIT_OPEN joins the registry in a later phase (CP-8 triage)." — CP-8 reading: keep `error_code=None` on vetoes 10–12; adding a code to frozen `apex/errors.py` is a CP-1-owned registry change (`tests/unit/test_risk_kernel.py::TestVetoRegistry`). **ISSUE-CP7-001** "Needs from owner: confirm the intended venue semantics of −1021 and −2026 …" — CP-8 reading: keep UNMAPPED⇒UNKNOWN⇒RECOVERY_REQUIRED (see ISSUE-CP8-003 for the citation correction). **ISSUE-CP7-003** "Needs from owner: decide at CP-8 triage whether these mo[dules]…" — CP-8 reading: keep the frozen verbatim literals in the owning modules (single source + cite + literal test each); copy them into a YAML only on an owner-ordered `params/*.yaml` revision, which is out of every executor's write set. **ISSUE-CP7-008** "Needs from owner: decide at CP-8 triage whether the DDL should permit NULL for absent money (that would need a CP-1-owned migration)." — CP-8 reading: keep the empty-TEXT encoding; a DDL migration is an owner decision after go-live consideration (`tests/unit/test_ledger_store.py::TestMoneyBoundary`). **AI.13 external gates** (G-TOOBIT-*, G-TARGET-DEVICE-*, G-CAPACITY-*, G-ADAPTER-*, G-RESTORE-*, G-PAPER-*, G-RISK-*, G-FALLBACK-*): all remain OPEN/UNVERIFIED — the CP-8 harnesses exist and are recorded in `PHASE2_FINAL_REPORT.md` §7 with their owner procedures. Needs from owner: the decisions listed above; no code change is blocked on them.

### CP-9 (operational wiring: long-run bootstrap command + Telegram receive gateway + 24/7 PAPER loop)
- [ISSUE-CP9-001] severity: MAJOR | status: CLOSED | A: `SQLiteStore.get_window`'s own docstring — "CLOSED observations with open_time <= as_of, ascending, last `bars` rows (PIT-safe by construction)" | B: the SQL was `ORDER BY open_time ASC LIMIT ?`, i.e. it returned the OLDEST `bars` rows at or before `as_of` — every engine would have received an ancient window instead of the most recent one (PIT-safe but the wrong set). Rule applied: the docstring is the contract (Ch.4 window provider; engines never SQL) and the pilot run needs the recent window. Interim behavior: the inner query takes the newest `bars` rows (`ORDER BY open_time DESC LIMIT ?`) and the outer one restores ascending order; asserted by `tests/integration/test_store_integration.py::TestWindowIsTheLatestBars` (3 tests: last-bars order, as_of boundary, short history). Needs from owner: none.
- [ISSUE-CP9-002] severity: MINOR | status: CLOSED | A: W.6 Phase 1 — "insert closed bars into `raw_observation`; checkpoint `bootstrap_progress`" | B: the frozen Ch.5 DDL (CP-1, `sqlite_store.CH5_DDL`) already owns a table named `bootstrap_progress` (symbol/timeframe/phase P1..P3) in the SAME database file (`Config().sqlite_path`), while the research runner's checkpoint store needs a cell-keyed cursor table — one name cannot serve both (`CREATE TABLE IF NOT EXISTS` would silently bind to whichever opened first and the other store's queries would then fail). Rule applied: the frozen Ch.5 DDL is untouchable; the research plane takes a distinct name (ADR-P2-003 additive migration rule). Interim behavior: the research store's table is `research_bootstrap_progress` (migration `M201_research_bootstrap_progress`), documented in the module docstring; the Ch.5 table keeps its frozen name and columns; asserted by `tests/unit/test_research_checkpoints.py` + `tests/unit/test_ops_bootstrap_service.py` (resume path). Needs from owner: none.
- [ISSUE-CP9-003] severity: MAJOR | status: CLOSED | A: Ch.16 L16814–16840 `trade_plan` DDL (21 columns, "no frozen column is removed") | B: `apex/ops/paper_loop.py::TRADE_PLAN_FIELDS` listed 23 columns — it added `leverage` and `owner_leverage_cap`, which the frozen table does not carry — so every `PlanQueue` lookup died with `sqlite3.OperationalError: no such column: leverage` and no cell could ever pass the gates stage. Rule applied: the frozen DDL is the schema authority; the runtime may not invent columns. Interim behavior: `TRADE_PLAN_FIELDS` aliases `apex.ledger.store.TRADE_PLAN_COLUMNS` (single source of truth, cannot drift again); `leverage`/`owner_leverage_cap` stay execution-side `TradePlan` fields only; asserted by `tests/integration/test_ops_paper_loop.py` (whole chain + `PLAN_ALREADY_MATERIALIZED` guard). Needs from owner: none.
- [ISSUE-CP9-004] severity: MAJOR | status: CLOSED | A: Ch.4 frozen DDL — `outcome.setup_id TEXT, FOREIGN KEY(setup_id) REFERENCES setup_candidate(setup_id)` | B: a governed plan can reach the execution stage with a `setup_id` that has no `setup_candidate` row (the setup engine's write is a separate producer); the FK then fails at CLOSE time — after the position was opened, i.e. an untradeable, unbookable position. Rule applied: fail-closed earliest — refuse before any venue contact. Interim behavior: the gates stage checks `setup_candidate` and refuses with the named `NO_SETUP_ROW_FOR_PLAN` (no order, no `trade_plan` row); asserted by `tests/integration/test_ops_paper_loop.py::test_a_plan_without_its_setup_row_is_refused`. Needs from owner: none.
- [ISSUE-CP9-005] severity: MINOR | status: CLOSED | A: Ch.4 DDL (L14397) — `open_interest TEXT CHECK(typeof(open_interest)='text')` in `market_observation` | B: Phase-1 bootstrap ingests OI-less bars (the public kline feed carries OI only in a separate series), and SQL NULL violates the CHECK while T-DC-004 forbids substituting 0. Rule applied: the frozen DDL + T-DC-004 (never a fake number) with the canonical availability label. Interim behavior: `market_observation.open_interest` stores `MISSING`/`INVALID` (text label, never a number) and `raw_observation` keeps the nullable pair (`oi NULL` + `oi_state`); `_row_to_obs` maps the label back to `oi=None`; a value-less state other than MISSING/INVALID is refused with `ValueError OI_STATE_WITHOUT_VALUE`; asserted by `tests/integration/test_store_integration.py` (4 OI tests, incl. "never 0"). Needs from owner: none.
- [ISSUE-CP9-006] severity: INFO | status: CLOSED | A: SL-5 → SL-6 — the frozen plan seam (Ch.16: "the Execution FSM is the only path to Toobit"; the governed plan is produced by the fabric → setup → forecast → risk → decision chain) | B: the engine → fabric → setup → forecast → risk → plan BRIDGE was the remaining separate increment; the production `serve` composition root still had no provider and every cell halted as `NO_PLAN_PROVIDER`. Rule applied: declared seam + named refusals (never a stub), single ledger writer, T_MONOTONE, all 14 vetoes before sizing, PAPER-only execution, and no live adapter path. Interim behavior: `apex/ops/plan_bridge.py::PaperPlanBridge` now consumes persisted ACTIVE evidence plus governed derived inputs and E11 IC inputs/history/classifier tensors/regime context, assembles the Evidence Fabric, runs pattern/setup, all setup gates, forecast, Risk Kernel/veto order, decision and materializes the setup row before returning the frozen `PaperRuntime` plan shape; missing context/evidence fails closed (`ENGINE_CONTEXT_UNAVAILABLE`, `PERSISTED_EVIDENCE_INCOMPLETE`, `E11_CONTEXT_INCOMPLETE`/`E11_CONTEXT_INVALID`, `PAPER_ONLY_EXECUTION`). `scripts/run_apex.py serve` registers this bridge as `PaperRuntime.plan_provider` through a store-backed context source, so the ingested store produces real per-cell PAPER plans. The fixture-clock bootstrap-style store double proves plan → gates → FSM → fill → protection → exit → outcome → reconcile, including deterministic bridge replay, with `tests/unit/test_plan_bridge.py` refusal coverage; no live adapter path was added. Needs from owner: none.
- [ISSUE-CP9-007] severity: MAJOR | status: CLOSED-with-evidence | A: W.6 Phase 1 — "checkpoint bootstrap_progress" (APEX_GEN5.md L17101) + Ch.5 DDL bootstrap_progress L14575 (symbol/timeframe/phase P1..P3, cursor_open_time TEXT, status PENDING/RUNNING/DONE/PAUSED/ERROR, bars_written, last_error, updated_at, PK(symbol,timeframe,phase)) | B: the CP-9 wiring initially wrote only to research_bootstrap_progress (M201) — the canonical Ch.5 table stayed empty, so a resume consulting the frozen DDL or an owner query would see no progress and re-wind Phase 1, violating W.6 resume-never-rewind. Rule applied: frozen DDL + W.6 wording — the canonical table is the resume authority; the research table is the cell cursor but must mirror (ADR-P2-003 additive, ISSUE-CP9-002). Interim behavior: CanonicalMirroredCheckpoints delegates every save_bootstrap to research_bootstrap_progress and mirrors to bootstrap_progress via _canonical_upsert(INSERT … ON CONFLICT(symbol,timeframe,phase) DO UPDATE cursor_open_time=MAX, status=excluded.status, bars_written=bootstrap_progress.bars_written+excluded.bars_written, last_error=COALESCE(excluded.last_error,bootstrap_progress.last_error), updated_at=excluded + commit) + mirror_bootstrap_progress(6× retry on 'locked' sleep 0.05·2^k, phase int→'P1', SKIPPED→ERROR+PHASE1_VERIFICATION_SKIPPED, fixed-width ISO _ms_to_iso/_iso_to_ms/_utc_now_iso, CANONICAL_PROGRESS_STATUS IN_PROGRESS→RUNNING PAUSED→PAUSED COMPLETE→DONE SKIPPED→ERROR), BootstrapService.open() constructs it as runner's store when self._store is not None else pure delegation; asserted by tests/unit/test_ops_bootstrap_service.py::test_complete_run_mirrors_canonical_done_and_cursor (phase P1 status DONE bars_written≥3 ISO cursor equality) + test_budget_paused_mirrors_canonical_running_and_cursor (status RUNNING|PAUSED cursor advanced). Needs from owner: none.

### CP-10 (hotfix: Toobit public response-shape unwrap + named error-code fail-closed)
- [ISSUE-CP10-001] severity: MAJOR | status: CLOSED-with-evidence | A: `apex/data_catalog/ingest/toobit_public.py` (CP-1 wire ingest) assumed a wrapped dict envelope `{"code": ..., "data": ...}` across public endpoints (`get_klines`, `get_depth`, `get_open_interest`, `get_funding_rate`), with `get_klines` executing `rows = body.get("data", body)`. | B: Live probe on the owner's device (HTTP 200) revealed that `GET /quote/v1/klines` returns a bare JSON array `[...]` and `GET /quote/v1/openInterest` returns a bare JSON object `{...}` without a `{"code": ..., "data": ...}` envelope; the initial bootstrap pilot crashed at `rows = body.get("data", body)` with `AttributeError: 'list' object has no attribute 'get'`. Furthermore, an HTTP-200 error body such as `{"code": -1003, "msg": "Too many requests"}` collapsed into empty rows `[]` (marking the cell COMPLETE with zero bars, violating frozen law W.6: −1003 backoff, never a skip) or an unexpected-shape error losing the `-1003` marker required by `bootstrap_service`. Rule applied: Patching frozen CP-1 code authorized under the ISSUE-CP9-001 bug-fix precedent; W.6 (rate-limit backoff, never a skip; every refusal NAMED); client choke-point unwrap: bare list is data, dict with non-zero code raises `ToobitPublicError` preserving numeric code and msg (so `RATE_LIMIT_MARKERS` "-1003" triggers backoff and non-rate-limit errors fail closed), dict with "data" unwraps data, bare dict without "data" or error code is data; legitimate HTTP 200 `[]` preserved as end-of-history; response-shape documented in `toobit_public.py` docstring without altering frozen `params/*.yaml`. Interim behavior: Implemented `unwrap_toobit_response` at the `_get` choke point in `apex/data_catalog/ingest/toobit_public.py`; updated `get_klines`, `get_depth`, `get_open_interest`, and `get_funding_rate` to consume unwrapped responses while keeping tolerant row parsing (lists and dicts); asserted that `-1003` on HTTP 200 raises `ToobitPublicError` with `-1003` in message and that `ToobitKlineSource` in `bootstrap_service` maps it to backoff (`{"code": -1003, "rows": [], "next_cursor_ms": None}`); asserted that non-zero error codes fail closed (`BootstrapError("FETCH_FAILED")`) and never yield empty rows; asserted verbatim real device probe fixtures; evidence `tests/unit/test_toobit_public.py` (33 passed: 13 baseline + 20 hotfix tests covering bare arrays, wrapped dicts, probe fixtures, -1003 backoff seam, non-zero code fail-closed). Needs from owner: none.

### CP-11 (hotfix: venue-adaptive backfill source for tail-aligned Toobit)
- [ISSUE-CP11-001] severity: CRITICAL | status: CLOSED-with-evidence | A: Frozen W.6 law (`apex/research/bootstrap.py`, untouchable) — empty page = cell complete, forward cursor, resume-never-rewind — was designed for a **head-aligned** venue that returns the FIRST `limit` bars at/after `startTime`. | B: Owner-measured live probe on the real Toobit venue (2026-09-16, owner device) established that public klines are **TAIL-ALIGNED**: for any `[startTime, endTime]` window the venue returns the LAST `limit` bars at/before `endTime` (`startTime` ignored). Verified: probe `2020→2020+7d` returned `[]`; probe `2020→now` returned the most recent 1000 bars. Retention per interval is a rolling ~3500 bars — and `1d` simply ends at the exchange's own listing (~2021-12-05). Measured oldest values: `1d 2021-12-05` · `4h 2025-02-09` · `1h 2026-04-23` · `15m 2026-08-10` · `5m 2026-09-03` · `1m 2026-09-13`. Against this venue the FIRST forward page from `DEEP_START=2020-01-01` lands in a dead window → every cell COMPLETE's with ZERO bars (owner pilot reproduced: `completed=1 pages=0 bars=0`). Rule applied: frozen runner/research/`bootstrap.py` law may not be edited (W.6); the adaptation belongs in the wiring layer `apex/ops/bootstrap_service.py` (CP-9/CP-10 file family); ISSUE-CP9-001 bug-fix precedent for owner-authorized hotfix of operational wiring; W.6 empty-page completion preserved as the honest end-of-history signal *after* the walk; resume-never-rewind preserved via cursor filtering + store `content_hash` dedup; −1003/429 backoff never a skip; non-rate-limit errors named fail-closed; `--max-pages` counts runner-facing pages only. Interim behavior: `ToobitKlineSource` becomes venue-adaptive per `(symbol, timeframe)` — on the runner's first page call the source walks the venue BACKWARD from `end_ms` (`endTime` stepping to `oldest_open − 1`), collecting raw rows, dedup-guarded (stop when a page re-returns its own first row; stop on any empty page), then serves the runner ASCENDING chunks of `PAGE_LIMIT` bars filtering out any row with `open_time < cursor`; subsequent page calls keep serving the next ascending chunk; when the walked history is exhausted, return the empty-page shape exactly as today (`next_cursor_ms=end_ms`) so the frozen law completes the cell honestly. Inside the walk, −1003/429 → bounded exponential backoff retries (`WALK_BACKOFF_SECONDS=(5,15,60)` injected by `BootstrapService`; default empty surfaces −1003 to the runner for the CP-10 seam); anything else → `BootstrapError("FETCH_FAILED")` (durable cursor stays valid). Resume: after a restart with a forward cursor, already-collected bars are not re-served (cursor filter = speed net; store content_hash = safety net). Evidence: `tests/unit/test_ops_bootstrap_service.py` — `TestTailAlignedSource` (full-walk ascending; empty-first-page no longer zero bars; budget on facing pages; backoff inside walk; dedup-stop; resume-with-cursor; resume-across-restart; non-rate-limit fail-closed) + service pilot-regression `test_tail_aligned_pilot_regression_completes_with_bars`; full suite twice = 2661 passed / 0 failed. Needs from owner: none (OWNER-CHECKED board box left unchecked for owner stamp).

### CP-12 (hotfix: venue OHLC-hygienic backfill source)
- [ISSUE-CP12-001] severity: MAJOR | status: CLOSED-with-evidence | A: W.6 Phase-1 law + the frozen store DDL (`APEX_GEN5.md` §W.6 L17101 "−1003 backoff, do not skip"; `apex/data_catalog/store/sqlite_store.py` `raw_observation` CHECK `CAST(high_price AS REAL)>=max(CAST(open_price AS REAL),CAST(close_price AS REAL)) AND CAST(low_price AS REAL)<=min(...) AND CAST(high_price AS REAL)>=CAST(low_price AS REAL)`) — the raw store is venue-faithful and its DDL is frozen, so an impossible bar must never be written and never be rewritten. | B: the owner's first long bootstrap run after CP-11 advanced ~25 cells / 76,366 clean bars, then died named-fail-closed at `BTCUSDT:1d` with `IntegrityError: CHECK constraint failed: CAST(high_price AS REAL)>=max(open,close) AND low<=min(open,close) AND high>=low`. The owner's read-only venue probe (2026-09-16, walking every interval backward exactly like `ToobitKlineSource` does) measured violations per interval: `1m`=0, `5m`=0, `15m`=0, `1h`=0, `4h`=0, `1d`=2 of 1747 rows — Toobit's legacy Huobi-era 1d candles. Verbatim poison rows (BTCUSDT, 1d): `[1673395200000,"17440.25","17995.03","17551.51","17942.82","1630.364436157643853256",0,"28460372.187496166962904175",710123,"0","0"]` (2023-01-11: low 17551.51 > open 17440.25) and `[1677196800000,"23940.6","23193.77","23183.93","23185.29","1077.358111833780842094",0,"25386566.469458050508191797",4573900,"0","0"]` (2023-02-24: high 23193.77 < open 23940.6). | Rule applied: no frozen file may be edited (P13/G13 — `apex/research/bootstrap.py`, `apex/data_catalog/**` incl. `toobit_public.py` and the store DDL, `params/*.yaml`, `APEX_GEN5.md`, `PROMPT.md`, migrations); repairing/clipping a venue value is fabrication (forbidden by the no-fabrication law, ADR-P2-015) and silently skipping the row violates W.6's never-skip law, so between the two forbidden options the sanctioned behavior is the one CP-10/CP-11 established for owner-measured venue defects: adapt in the wiring layer (`apex/ops/bootstrap_service.py`, CP-9/CP-10/CP-11 file family) with observable evidence, keep the frozen law byte-untouched, and file the finding here rather than resolving it silently (P13). | Interim behavior: `ToobitKlineSource` validates at the single boundary where a walked kline row is appended into the per-cell history — so both fresh walks and cursor-filtered resume passes pass through it — requiring `open/high/low/close` numeric-parseable AND `high>=max(open,close)` AND `low<=min(open,close)` AND `high>=low` (the frozen DDL law, clause for clause); a failing row is DROPPED before it can be served, so it never reaches the store and the CHECK can no longer abort a run. Drops are never silent: `invalid_dropped` counts them per source, `invalid_by_cell` per cell, up to 20 offenders per cell are retained as `(symbol, timeframe, open_time_ms, repr(row)[:160])` with named reasons (`LOW_ABOVE_MIN_OPEN_CLOSE`, `HIGH_BELOW_MAX_OPEN_CLOSE`, `HIGH_BELOW_LOW`, `OHLC_NOT_PARSEABLE:<field>`), the cell-complete wiring print carries `dropped=<n>` plus the offenders/reasons summary, and the status mirror carries `invalid_bars_dropped` so `scripts/run_apex.py status` (offline, read-only) surfaces it. Volume/quote-volume/trade-count fields are never a drop reason (the frozen client's tolerant parse of them is unchanged — a non-decimal volume still surfaces as the client's own named failure, never as a drop). MUST-NOT-CHANGE semantics preserved and asserted: the empty-page shape (`next_cursor_ms=end_ms`) stays the only completion signal, an all-invalid venue page drops its rows but neither ends the walk nor completes the cell (its open times still count as "seen", so the walk keeps stepping `endTime` to `oldest_open − 1`), ascending `PAGE_LIMIT` serve, `open_time < cursor` resume filter, `--max-pages` counts runner-facing pages only, −1003/429 walk-internal bounded backoff with `WALK_BACKOFF_SECONDS`, the CP-10 seam (default empty backoff surfaces −1003 to the runner), non-rate-limit errors remain `BootstrapError("FETCH_FAILED")`, completed-cell stamps and resume-never-rewind unchanged. | Evidence: `tests/unit/test_ops_bootstrap_service.py` — `TestOhlcLaw` (verbatim rows judged in observation/list/dict shape; a 625-case table proving the verdict equals the frozen DDL predicate; unparseable OHLC named per field; rows without OHLC not judged) + `TestVenueDataHygiene` (both verbatim poison rows dropped with their valid neighbours ingested and `dropped=2` visible; an all-poison page that neither ends the walk nor completes the cell; an all-poison cell completing with zero bars and 20-of-25 retained offenders; hostile volume/trades kept; poison never re-served across a resume or a restart re-walk; −1003 inside a poisoned walk backs off and never skips; a −1120-class error still fails closed as `FETCH_FAILED`) + `TestVenueDataHygieneService` (real frozen store: `IntegrityError` unreachable, cell `COMPLETE` at 48 bars with `invalid_bars_dropped=2` in status and run result, cell-complete wiring print carries `dropped=2` + offenders, clean cell reports `dropped=0`, budget stop keeps the evidence, offline status reports 0, and the untouched DDL CHECK still refuses the verbatim poison bar); targeted 49 passed (30 baseline + 19 CP-12), full suite twice 2680 passed / 0 failed. Needs from owner: confirm the interim drop-with-evidence policy for `1d` (and any further interval the probe later implicates) — either accept it and the frozen DDL stays as-is, or decree the venue-faithful alternative (store the bar, gate it at the QC layer), which needs a frozen-file change and is therefore NOT implemented here.
  OWNER DECISION (2026-09-17): ISSUE-CP12-001 interim policy ACCEPTED — OHLC-impossible venue bars are DROPPED with observable evidence in the wiring layer; the frozen raw_observation DDL CHECK stays as-is. Device evidence from the first complete harvest (2026-09-16, 140/140 cells, 421,303 raw bars, cells_with_data=140): the 1d series of all ten Core-10 SWAP symbols are contiguous and duplicate-free except ETHUSDT 2023-01-19, where the venue bar for ETH-SWAP-USDT is open=1511.44 high=1564.69 low=1513.44 close=1551.44 (LOW_ABOVE_MIN_OPEN_CLOSE) — dropped, neighbours 2023-01-18 and 2023-01-20 ingested. Correction of record: the two probe rows of 2023-01-11 / 2023-02-24 quoted in this issue belong to the SPOT pair BTCUSDT, not to the harvested BTC-SWAP-USDT (whose 1d bars for those dates are sane), and the pre-CP-12 IntegrityError abort therefore occurred at cell 26 ETHUSDT:1d on the 2023-01-19 bar, not at BTCUSDT:1d. Open follow-ups filed separately: (a) the last bar of every cell was stored as CLOSED while still open (45 confirmed mismatches vs the venue's closed bar, evidence data/partial_bar_evidence_20260916T223404Z.json); (b) availability_time is 1970-01-01 for every row (venue close_time=0); (c) per-cell drop evidence is not visible in run_apex.py output without Telegram and not persisted for offline status.
### CP-13 (hotfix: closed-bar law + frontier resume + partial-bar repair + drop-evidence persistence)
- [ISSUE-CP13-001] severity: MAJOR | status: CLOSED-with-evidence | A: W.6 Phase-1 law ("insert closed bars into `raw_observation`", APEX_GEN5.md §W.6 L17101) + the frozen client's CLOSED labelling (every parsed kline row carries `status=CLOSED`) + the frozen runner's empty-page completion law (`cursor=end_ms`, resume-never-rewind) — a bar stored while still open is never revisited by re-running bootstrap (the cursor is already past it). | B: the venue's tail-aligned `/quote/v1/klines` returns the CURRENTLY OPEN candle as its last row, and the pre-CP-13 wiring served it: owner measurement 2026-09-16 — 91 stored bars differ from the venue's final closed bar, every one with `created_at` strictly earlier than its bar close (the CP12-001 OWNER DECISION files 45 confirmed mismatches with evidence `data/partial_bar_evidence_20260916T223404Z.json`, open follow-up (a)). Each was kept as an immutable CLOSED row — a permanent partial bar no re-run can heal. | Rule applied: no frozen file may be edited (P13/G13 — the same frozen set as CP-12: `apex/research/bootstrap.py`, `apex/data_catalog/**` incl. `toobit_public.py` and the store DDL, `params/*.yaml`, `APEX_GEN5.md`, `PROMPT.md`, migrations); the serve decision is wiring-owned, so the close law, the resume fix and the repair path all live in the wiring layer with observable evidence, and the stored partials are healed only through the frozen correction path (never delete, never update-in-place — the `IMMUTABLE_RAW_STORE` trigger). | Interim behavior: (i) SERVE LAW — a walked row is served only when `close_time_ms(open) <= end_ms` (wiring-layer `close_time_ms` over all 14 timeframes; `1mo` is the next calendar month in UTC, never the 30-day approximation, across year boundaries and leap Februaries); the still-open bar is excluded (never counted, never stored) and reported as `open_excluded=1` with its `open_time`, then served exactly once after it closes. The boundary is the SERVE boundary in `__call__`, not the CP-12 append boundary — openness is temporal (it depends on the run's `end_ms`) while the history stays the venue's retained series; the cached walk is re-walked whenever `end_ms` advances, so a stale open-bar snapshot is never served as closed. (ii) CURSOR-TRAP FIX — the serve filter no longer trusts the durable cursor (filtering on `cursor <= open_time` would hole the newly closed bar forever while `next_cursor_ms == cursor` is frozen-impossible): it serves `open_time > store_frontier AND open_time > served_upto AND close_time <= end_ms`, where the frontier is `MAX(as_of)` read once per run and handed via `set_frontiers` (strict `>`: a stored bar, partial or not, is never re-served) and `served_upto` is the within-process high-water mark; `next_cursor_ms = max(close_time_ms(last_served), cursor + 1)` (calendar-aware for `1mo`) is strictly greater than the incoming cursor in every branch, so `CURSOR_NOT_ADVANCING` is unreachable and the empty page stays the only completion signal. (iii) GOVERNED REPAIR — new `apex/ops/partial_bar_repair.py` + `repair-partial` CLI (dry-run default; LIVE `endTime=close-1` first with open-equality, F6a/F6b `--evidence` fallback with a Decimal-equal store gate, the frozen parse + the CP-12 law judging the replacement, `store.correct_raw` with `actor=OPS_REPAIR_CP13`, `VERIFIED_CLOSED`/`CORRECTED`/`UNREPAIRABLE_VENUE_WINDOW_PASSED`/`REFUSED_*` verdicts, idempotent, exit 0 iff nothing is left unrepairable or refused). | Evidence: `tests/unit/test_ops_bootstrap_service.py` — `TestCloseTimeMs` (all 14 timeframes, Dec→Jan, leap/non-leap February, unaligned opens, unknown-timeframe fail-closed) + `TestClosedBarLaw` (open bar excluded-then-once across restart and same-process runs, seconds-later no-op COMPLETE, 1001-bar `--max-pages` frontier resume, `1mo` year boundary, the 31-day next-cursor trap) + `TestCursorAlwaysAdvances` (fresh / newly-closed / nothing-new / budget branches) + `tests/unit/test_ops_partial_bar_repair.py` (20: B1 detection incl. the 5 s skew boundary, B2 LIVE-first + F6a/F6b fallback incl. the open-mismatch and store-mismatch gates, B3 `correct_raw` lineage + idempotent re-apply, B4 untouched-unrepairable, B5 named refusals + CLI exit mapping); targeted 71 + 20 passed, full suite twice 2722 passed / 0 failed. Needs from owner: accept the serve law + frontier resume as the permanent Phase-1 behavior; run `repair-partial` with owner evidence for the stored partials whose venue window has passed (LIVE heals the rest automatically).
- [ISSUE-CP13-002] severity: MINOR | status: FILED (frozen — read, not changed) | A: `raw_observation.availability_time` is expected to carry the bar's venue close instant (`max_availability_time` is the freshness signal). | B: the venue sends `0` in every kline row's close_time slot (byte-visible in the verbatim probe rows, index 6) and the frozen client maps it faithfully, so `availability_time` is `1970-01-01` for every row and `MAX(availability_time)` is 1970 by venue fact, not by bug (open follow-up (b) of the CP12-001 OWNER DECISION). | Rule applied: P13/G13 — the client mapping and `max_availability_time` are frozen; a wiring-layer derivation would be a second source of truth for a venue-stamped field. | Interim behavior: none — filed, not compensated; the repair path reuses the same frozen values (a correction row also carries 1970, so MAX is unchanged — see the B4 direct-readers audit in `partial_bar_repair.py`). | Evidence: read-only (frozen `toobit_public.py` row mapping + `sqlite_store.max_availability_time`); no test pins a derived value because none is derived. Needs from owner: decree whether a wiring-layer availability derivation is wanted at all — it would need a frozen-file change and is therefore NOT implemented here.
- [ISSUE-CP13-003] severity: MINOR | status: CLOSED | A: the CP-12 drop-evidence contract (a drop is never silent: per-cell print + status mirror). | B: without Telegram only the last three notifications reached the screen, so per-cell `dropped=<n> offenders=…` prints were lost (F5), and the evidence was process-local (a fresh `status` after a restart reported 0 for completed cells) — open follow-up (c) of the CP12-001 OWNER DECISION. | Rule applied: the same frozen set as CP-12 (the research checkpoint store already accepts and stores `payload_json` — read, not changed). | Interim behavior: (i) `run_apex.py bootstrap` prints EVERY `CELL_COMPLETE` line carrying evidence (`dropped>0` or `open_excluded>0`) plus a final `invalid_bars_dropped=<n> open_bars_excluded=<m>` line on both the success and the resumable-stop paths, and `status` prints both counters; (ii) `CanonicalMirroredCheckpoints.save_bootstrap` merges `invalid_bars_dropped`/`invalid_reasons` into the COMPLETE payload from the current walk (max-preserved when retention slid an old poison bar out of the window, or when no walk happened this run) and carries the keys forward untouched on non-COMPLETE saves — required because the frozen runner re-saves `IN_PROGRESS` with `payload=None` on every re-run, even for an already-complete cell, immediately before its COMPLETE save (a same-end re-run would otherwise wipe the evidence); (iii) `status()` sums durable COMPLETE payloads + the live source's counters for incomplete cells only (never both for one cell), while `open_bars_excluded` stays temporal per-run, never persisted. | Evidence: `TestDropEvidencePersistence` (COMPLETE payload content, offline `status` durable sum, budget-stop live-only reporting with nothing persisted, same-end re-run preservation, the evidence-line detector + print-every test); targeted 71 passed, full suite twice 2722 passed / 0 failed. Needs from owner: none.
- Correction of record (F2): the `1970-01-01` `availability_time` on every row is NOT a parse/store defect — the venue sends `close_time=0` in every kline row (byte-visible at index 6 of both verbatim probe rows) and the frozen parse/store preserve it faithfully; `MAX(availability_time)=1970` is therefore the venue's own fact. Filed as ISSUE-CP13-002 (frozen, not changed); no wiring compensation.
- ADR-P2-003 note (CP-13): two additive files, both required by the hotfix scope and both listed in `PHASE2_HANDOFF_CP9.md` §`HANDOFF_CP13_HOTFIX` DELIVERED: `apex/ops/partial_bar_repair.py` (governed repair module — Ch.18 W.6 ops addition) + `tests/unit/test_ops_partial_bar_repair.py` (its 20-case battery).

### CP-13.1 (hotfix: close-time-vs-now guard for repair-partial — still-open candidates skipped)
- [ISSUE-CP13-004] severity: MAJOR | status: CLOSED-with-evidence | A: CP-13 B1 detection law — `find_candidates` in `apex/ops/partial_bar_repair.py` selects market `CLOSED` rows with `created_at < close_time_ms(open) + SKEW_MARGIN_SECONDS*1000` (the 5 s margin errs toward checking; a candidate that turns out identical is a harmless no-op). | B: the selection has NO guard that the bar has actually closed. Device evidence (owner dry-run 2026-09-17T04:33:03Z, report `data/repair_partial_report_20260917T043303Z.json`, kept off-repo): `candidates=163 corrected=163 unrepairable=0 refused=0`, all replacements `VENUE_LIVE`, but **20 of the 163 candidates are the CURRENT still-open 1w and 1mo bars of all 10 symbols** (1w open 2026-09-15T00:00Z closes 2026-09-22T00:00Z; 1mo open 2026-09-01T00:00Z closes 2026-10-01T00:00Z). For those, `fetch_live_bar` returns the venue's still-open candle, so an `--apply` run would replace a partial bar with another partial bar and would do so again on every subsequent run. `--apply` has NOT been run by the owner and must remain unused until this fix is merged. ROOT CAUSE: spec omission — the CP-13 repair spec stated the detection law (created-before-close+skew) and the correction path but never a close-time-vs-now guard; the wiring implemented the spec faithfully. | Rule applied: fail-closed (a bar whose replacement cannot yet be final is never fetched, never written); the allowed-set law (only `apex/ops/partial_bar_repair.py`, the `repair-partial` summary/row print in `scripts/run_apex.py`, the repair test file and the four control docs touched; `apex/research/bootstrap.py`, `apex/data_catalog/**`, store DDL, `params/*.yaml`, `APEX_GEN5.md`, `PROMPT.md` byte-untouched); single wall-clock seam (`now_ms`, injectable; `None` → `int(time.time()*1000)` resolved once inside `run_repair` — the only clock read of the repair path; production and tests share it). | Interim behavior: `run_repair(..., now_ms=None)` — a candidate whose `close_ms > now_ms - SKEW_MARGIN_SECONDS*1000` (strict `>`) is NOT repaired and NO live fetch is made for it; it still appears in the report's `candidates` list with verdict `SKIPPED_STILL_OPEN` (new constant `VERDICT_SKIPPED_STILL_OPEN`), `replacement` null and `closes_at` (ISO-8601 UTC millisecond string of `close_ms`, via the wiring `_ms_to_iso`), counted under the new counts bucket `skipped_still_open` (added when ≥1; never counted as unrepairable or refused). Exit-code semantics unchanged: `repair-partial` returns READY (0) iff `unrepairable == 0` and `refused == 0`; skipped rows never degrade the exit code. The printed summary carries `skipped_still_open=N`; the per-row print for a skipped row shows `verdict=SKIPPED_STILL_OPEN closes_at=<iso>`. `CONTRACT_VERSION` 4.0.0 → 4.1.0 (backward-compatible minor: one additive verdict, one additive counts bucket, one additive parameter). `find_candidates` / `repair_one` / `report_filename` / the frozen `correct_raw` usage are byte-untouched. | Evidence: `tests/unit/test_ops_partial_bar_repair.py` — NEW `TestSkippedStillOpen` (6 tests, appended; all 20 CP-13 tests untouched and green): (a) a still-open 1w candidate with injected `now_ms` before its close is reported `SKIPPED_STILL_OPEN` with `closes_at` set, `counts.skipped_still_open == 1`, `unrepairable == 0`, `refused == 0`, and the stub client records ZERO `get_klines` calls; (b) the same row with `now_ms >= close_ms + skew` is processed normally (`VERIFIED_CLOSED`, `endTime = close-1`, limit 5); (c) a close 4 s in the future, 3 s before now and 4 999 ms before now are all treated as still open (the 5 s skew window), the exact boundary `now_ms == close_ms + 5000` is NOT skipped (strict `>`); (d) with `--apply` semantics a skipped row triggers NO `correct_raw` write (raw count AND content unchanged, `raw_revision` empty, market row still `CLOSED`); (e) a mixed run of 1 skipped (1w) + 1 corrected (1h) + 1 verified (1h) yields `candidates=3 verified=1 corrected=1 unrepairable=0 refused=0 skipped_still_open=1` and the report JSON round-trips through `json.dumps`; (f) the `run_apex.py` CLI summary line includes `skipped_still_open=1` and the exit code is READY (0) when only skipped rows exist, with `fetch_live_bar` patched to raise on any call. Targeted: `tests/unit/test_ops_partial_bar_repair.py` = 26 passed (20 baseline + 6), `tests/unit/test_ops_bootstrap_service.py` = 71 passed (untouched); full suite twice = **2728 passed / 0 failed** (was 2722; +6, no decrease). Projected device read of the unchanged command after this fix: the 2026-09-17T04:33:03Z dry-run reports the same 163 candidates with the 20 still-open rows listed `SKIPPED_STILL_OPEN` with `closes_at` (`2026-09-22T00:00:00.000Z` 1w / `2026-10-01T00:00:00.000Z` 1mo), exactly 0 live fetches for them, `skipped_still_open=20`, exit 0 — to be confirmed by the owner per `HANDOFF_CP13.1_HOTFIX` DEVICE-RUNBOOK. Needs from owner: none (OWNER-CHECKED board box left unchecked for owner stamp).
- OWNER DECISION (2026-09-17): ISSUE-CP13-004 verified on the target phone with the merged CP-13.1 code (main b831161). git pull fast-forward (7 files), targeted pytest 97 passed; dry-run: candidates=163 corrected=143 unrepairable=0 refused=0 skipped_still_open=20 (report 20260917T060210Z); --apply: corrected=143 (report 20260917T060558Z); re-run without --apply: candidates=20 corrected=0 skipped_still_open=20 (report 20260917T060651Z); raw_revision=143, raw_observation=426520 (append-only, no row deleted or updated). Correction of record: the current 1w bar opened 2026-09-14T00:00Z and closes 2026-09-21T00:00Z (venue weeks start Monday) — the 09-15/09-22 dates quoted in ISSUE-CP13-004, HANDOFF_CP13.1_HOTFIX and the PR text are a documentation slip in the spec; the code computes closes_at correctly and printed 2026-09-21T00:00:00.000Z on device. Remaining: 10 × 1w repairable after 2026-09-21T00:00:05Z, 10 × 1mo after 2026-10-01T00:00:05Z, same --apply command. Reports kept off-repo in ~/apex_evidence.

### OWNER DECISIONS — PAPER go-live programme (2026-09-17, binding)
Recorded verbatim by the owner after a full gap review of main @ 9da8566. These decisions supersede the "separate increment" / "owner procedure" deferrals of PHASE2_FINAL_REPORT §7 and HANDOFF_BRIDGE §REMAINING WORK. Every executor from Session A onward reads this block before editing.
- D1 PAPER execution mode = in-process PAPER simulator transport: the ToobitAdapter keeps its five-operation surface; in APEX_ENV=PAPER the transport is a deterministic simulator that fills against store prices with the frozen cost model (fee 0.02 %, alpha_spread slippage), sends NO packet to the venue, and drives the same FSM/ledger/Telegram path as LIVE. No Toobit demo account. A tiny-balance real-venue canary is a later, separate owner decision.
- D2 APEX_GEN5.md MAY be patched, in the owning sections only, for the items in this block; every patch is logged as one Appendix AJ traceability row and the new sha256 is recorded on the checkpoint board. No engine formula body is rewritten.
- D3 E11 classifier weights W(9x8), b(9): trained deterministically on the harvested store per E11 §3.3 ("logistic regression over delayed labels", labels from the §3.2 rule tree, 48-candle delay), fixed seed, persisted as a hashed parameter artifact under params/ (additive file, ADR-P2-003). Never zeros, never random at runtime.
- D4 PAPER account inputs come from a new params/paper_account_v1.yaml (capital_usdt: 10000) and from the ledger (exposure, realized daily/weekly loss, consecutive losses, margin health = simulated). PAPER and LIVE are separate everywhere they are measured or displayed (reports, backtest results, exports, balances); the Telegram control plane's paper_balance is fed from this YAML, never a second literal.
- D5 `serve` performs a per-cycle catch-up for due cells through the CP-13 ToobitKlineSource frontier rule (closed bars only) before the engines run — one process, one writer.
- D6 AI.13 sequencing: G-PAPER-001 is satisfied by the W.6 Phase-2 deterministic double run over the whole local store (byte-identical hashes, zero exceptions); G-PAPER-002 is measured passively during the first 5 days of normal PAPER use (5-min drift/uptime snapshots) and is an exit gate towards LIVE, not an entry gate to PAPER; G-TOOBIT-*, G-CAPACITY-*, G-RESTORE-001, G-RISK-001, G-FALLBACK-001, G-LEDGER-001 move to the pre-LIVE checklist; G-ADAPTER-001 is N/A (no legacy export exists). Nothing is deleted; the order changes.
- D7 W.6 Phase 2 (frozen-default replay + health check) is built as a CLI and run once before PAPER; Phase 3 (optimizer) starts after PAPER is running.
- D8 params/risk_defaults_v1.yaml stays authoritative (budget_per_trade 0.005, k_attn 0.25); Ch.17 numbers remain a documented divergence.
- D9 CIRCUIT_OPEN joins the Ch.7 error registry (doc + apex/errors.py) so vetoes 10-12 carry a code.
- D10 -1021/-2026 stay UNMAPPED -> RECONCILE. D11 module-frozen literals stay. D12 empty-TEXT-for-absent-money stays. D13 APEX_DOTENV_PATH stays. D15 CP-6 prompt numbers stay as governed additive keys. D16 Toobit remains the only history source for now.
- D14 Freshness: because the venue sends close_time=0, staleness for the freshness veto and Q_fresh is derived in the wiring layer from close_time_ms(open_time, timeframe) and the store receipt time; the frozen availability_time column is not reinterpreted.
- D17 Secrets (bot token, chat id, venue key/secret) live ONLY in the phone's .env (git-ignored); they are never committed, never pasted in chat, never printed by any command. D18 repository set to PRIVATE now. D19 24/7 host = the owner's phone (Termux + proot, Termux:Boot). D20 watchdog chat = owner chat.
- Programme: Session A = APEX_GEN5.md patch (D2/D9/D14 + design text for D1/D3/D4/D5/D6/D7). CP-14 = engine-context producer + E11 training + live catch-up + PAPER account inputs + freshness. CP-15 = PAPER simulator transport + Phase-2 replay CLI (G-PAPER-001). Then device go-live.

---

## C. Session A — DESIGN PATCH (2026-09-17; doc-only; the owner D1–D20 block above is untouched)

### ADR-SESSION-A (binding notes for CP-14 / CP-15; Session A created no code, no tests, no YAML, no new files)
- **ADR-SA-001 — PAPER capital is 10000.0.** The executor brief draft said `capital_usdt: 1000.0`; owner D4 says `10000`, corroborated by the control-plane constructor default `"10000"` (`apex/telegram/control_plane.py` L458). Owner decision wins: P7 specifies `capital_usdt: 10000.0`.
- **ADR-SA-002 — D9 is split: doc now, code later.** P6 adds `CIRCUIT_OPEN` to the Ch.7 registry; the matching `apex/errors.py` one-liner is deferred to CP-14 (Session A scope is doc-only per its brief).
- **ADR-SA-003 — Named-but-uncreated files.** `params/paper_account_v1.yaml` and `params/e11_classifier_v1.yaml` are named by P7/P2 as CP-14 additive files under ADR-P2-003; Session A created neither. (Session-A F2 2026-09-17: renamed from `e11_classifier_v4.json` — YAML-only parameter surface per ISSUE-CP2-006.)
- **ADR-SA-004 — No risk-threshold fork.** Per D8, `params/risk_defaults_v1.yaml` stays authoritative for `budget_per_trade`/`k_attn`/loss limits; P7's paper-account YAML holds only `capital_usdt` + simulated-margin inputs and references risk_defaults, never duplicates it.

### ISSUEs filed by Session A (doc-vs-code conflicts the patch had to resolve; doc line numbers are pre-patch)
- [ISSUE-SESSION-A-001] severity: MAJOR | status: CLOSED(doc-side; code follow-up = CP-14)
  A: "on each TF close, ingest→quality→features→engines→setup→gates→risk→decision→execution for **that** (symbol,TF)" (APEX_GEN5.md §9.5-14, L20246)
  B: `_stage_features`/`_stage_engines` return `DECLARED_SKIP signal_source=DECISION_BRIDGE`, and `setup` halts `NO_PLAN_PROVIDER` without a plan (apex/ops/paper_loop.py L340-364, L381-394)  # the code cannot satisfy the nine-stage law until the producer exists
  Rule applied: D2/D5 — P1 specifies the CP-14 engine-context producer contract (per-close catch-up, 38+23 keys, store seam).
  Interim behavior: loop still skips/halts exactly as before; no code changed this session.
  Needs from owner: none (CP-14 builds the producer).
- [ISSUE-SESSION-A-002] severity: MAJOR | status: CLOSED(doc-side; code follow-up = CP-14)
  A: "using initial weights trained with logistic regression over 6 months of delayed labels" (APEX_GEN5.md E11 §3.3, L11831)  # no artifact, no procedure, no seed
  B: `_validate_e11_context` hard-requires `classifier_W` shape (9,8) + `classifier_b` shape (9,) (apex/ops/plan_bridge.py L432-457); engine raises `CONFIGURATION_INVALID` without W/b (apex/engines/e11_regime/engine.py L409)
  Rule applied: D3 — P2 names `params/e11_classifier_v1.yaml` + the deterministic training procedure + degenerate-class refusal (renamed Session-A F2 2026-09-17).
  Interim behavior: bridge still raises `E11_CONTEXT_INVALID` until CP-14 trains the artifact.
  Needs from owner: none (CP-14 trains per P2).
- [ISSUE-SESSION-A-003] severity: MINOR | status: CLOSED(doc-side; code follow-up = CP-14 one-liner)
  A: vetoes 10–12 error-code column reads "CIRCUIT_OPEN (Alert Policy)" (APEX_GEN5.md Ch.15, L16659-16661)
  B: the Ch.7 registry (L14643-14671) has no CIRCUIT_OPEN row, and `apex/errors.py` ERROR_REGISTRY lacks the key  # vetoes cite a code that exists nowhere normative
  Rule applied: D9 — P6 adds the Ch.7 row; veto semantics stay Ch.15-owned.
  Interim behavior: doc resolved; `apex/errors.py` still lacks the key until CP-14.
  Needs from owner: none.
- [ISSUE-SESSION-A-004] severity: MAJOR | status: CLOSED
  A: "PAPER orders require `APEX_ALLOW_SIGNED=1`" (APEX_GEN5.md W.6 Phase 2, L17093)  # reads as authorizing signed orders in PAPER
  B: D1 — PAPER is the simulator; no venue packet is ever constructed in PAPER.
  Rule applied: D1/D2 — P3 reconciles: the flag permits the PAPER loop to run; it never authorizes a packet.
  Interim behavior: doc resolved; CP-15 builds the simulator transport.
  Needs from owner: none.
- [ISSUE-SESSION-A-005] severity: MINOR | status: CLOSED
  A: E01 output "consumed by E02 Liquidity, E09 Trend, and E11 Regime" + consumers list incl. `E02_Liquidity` (APEX_GEN5.md L1296, L1327)
  B: "Dependencies: none (zero inter-engine dependency)" (APEX_GEN5.md E02, L2792)
  Rule applied: each engine's own Dependencies declaration governs its inputs (P1 runtime order runs E02 on CLOSED OHLCV only).
  Interim behavior: doc resolved; no code impact.
  Needs from owner: none.
- [ISSUE-SESSION-A-006] severity: MINOR | status: CLOSED(doc-side; code follow-up = CP-14)
  A: D4 — control-plane `paper_balance` is fed from the YAML, never a second literal.
  B: `paper_balance: Any = "10000"` constructor default (apex/telegram/control_plane.py L458)
  Rule applied: D4 — P7 names `params/paper_account_v1.yaml` authoritative; the constructor default stands only as a pre-YAML display fallback.
  Interim behavior: doc resolved; CP-14 wires the YAML feed.
  Needs from owner: none.
- [ISSUE-SESSION-A-007] severity: MAJOR | status: CLOSED(doc-side; code follow-up = CP-14)
  A: bridge probes `get_bridge_context → get_engine_context → read_bridge_context → read_engine_context → bridge_contexts`, absence ⇒ `ENGINE_CONTEXT_UNAVAILABLE` (apex/ops/plan_bridge.py L539-545)
  B: `SQLiteStore` public methods (`open/close/db/ingest_raw/correct_raw/get_window/max_availability_time/insert_snapshot/insert_evidence/write_manifest/retention_purge/table_names`; apex/data_catalog/store/sqlite_store.py L342-667) include no `get_bridge_context`
  Rule applied: D2/D5 — P1 names the explicitly named store reader as the CP-14 interface; until then the producer assembles context through the existing public methods.
  Interim behavior: bridge still raises until CP-14 implements the reader.
  Needs from owner: none.

## D. CP-14 — engine context and PAPER prerequisites (2026-09-17)

- [ISSUE-CP14-001] severity: CRITICAL | status: ESCALATED
  A: E11 Session-A P2 requires the initial classifier to be trained on labels from the exact Section 3.2 rule tree, finalized by the 48-candle structural confirmation rule (APEX_GEN5.md L11831, L11851-11865).
  B: Section 2 defines H exclusively from softmax(W X + b) (L11744-11747); Section 3.2 uses H for its second-priority TRANSITION branch (L11810-11812); E11 explicitly says it never labels without entropy (L11728). No initial W/b or alternative source of training-label entropy is specified. The existing engine checks W/b before computing entropy and calling the rule tree (apex/engines/e11_regime/engine.py L876-890). ISSUE-CP5-014 authorizes Gaussian-state/momentum initialization only, not classifier initialization. Thus the first artifact depends on labels that themselves depend on an existing classifier.
  Rule applied: Section 9.5-7 No-Guess; D3; exact P2 training law. A fixed optimizer seed alone does not authorize a substitute label oracle.
  Interim behavior: implementation of the first-training label source is paused pending owner adjudication; no zero-entropy, random-label, fixture, or omitted-TRANSITION fallback is introduced. Existing runtime fail-closed behavior remains unchanged. This is not classified as a measured degenerate-class refusal: no lawful label histogram has yet been constructed.
  Needs from owner: specify the authoritative initial entropy/classifier source for training labels, or authorize a precise deterministic bootstrap-label procedure (including how TRANSITION obtains members). Such a decision must explicitly resolve the circular dependency; no trained artifact is committed.

- [ISSUE-CP14-002] severity: MAJOR | status: ESCALATED
  A: P1 item 4 requires staleness_seconds = max(0, receipt_time - derived_close_time) and freshness_ok if and only if staleness_seconds <= freshness_sla_seconds (APEX_GEN5.md L20498-20508; D14). P1 item 1 also forbids engine computation until catch-up for that close completes (L20389-20391).
  B: CP-14 G4 requires every catch-up failure to make freshness_ok false through G5. A transient fetch failure can occur while the last stored close is still within its SLA, including a first-cycle fetch immediately after a successful external bootstrap. Then the exact G5 predicate is true, but G4 demands false. Changing the timestamp or increasing staleness to force failure would fabricate the G5 measurement.
  Rule applied: No-Guess and fail-closed; preserve the exact measurement and do not silently replace an iff with a conjunction.
  Interim behavior: catch-up/freshness integration is paused pending owner adjudication; no receipt/availability timestamp is changed. Recommended resolution is to retain the exact G5 freshness predicate, expose catch_up_ok separately, and refuse engine/plan evaluation on catch-up failure regardless of freshness_ok.
  Needs from owner: authorize the separate catch-up admission failure (recommended), or explicitly amend freshness_ok to include catch_up_ok while preserving the unmodified measured staleness_seconds. Either choice resolves a conflict in the executor acceptance contract, not an engine formula.

### OWNER ANSWERS to CP-14 issues (2026-09-17)

D21 — ISSUE-CP14-001, first-training labels for E11 (bootstrap rule, normative): (a) The training procedure is self-contained: it NEVER depends on a previously trained artifact, so the same store always reproduces the same artifact. (b) Training labels use the §3.2 rule tree with the entropy branch REMOVED (branch 2, "H_t ≥ θ_H → TRANSITION", is not evaluable before W/b exist and is skipped); the other eight branches keep their exact order and thresholds: CRISIS (Turbulence ≥ 15.5 or x_vol ≥ 0.85, Turbulence from the §3.6/§3.7 EWMA state, which needs no classifier) → EXPANSION → TREND_EXPANSION → TREND_CONTRACTION → TREND → COMPRESSION → CHOP → RANGE. Call this r_t^rule0. (c) Delayed finalisation per §3.4 is applied as written: r_t^final = r_t^rule0 if confirmed by BOS/CHoCH within [t+1, t+48]; otherwise the §3.4 "AMBIGUOUS" outcome IS the training label TRANSITION (TRANSITION is defined by the document as high uncertainty; unconfirmed structure is its observable proxy before a classifier exists). This is the only source of TRANSITION members in training; no synthetic members, no reweighting. (d) Training consumes only bars whose t+48 has closed inside the store (PIT: ≤ t−48 relative to the window end); the optimizer is multinomial logistic regression (the document's "initial weights trained with logistic regression") with the fixed recorded seed; K stays 9; the degenerate-class refusal applies to all nine classes including TRANSITION. (e) Runtime is unchanged: the full §3.2 tree including the entropy branch runs with the trained W/b.

D21(f) in full: Document patch — add a short paragraph with the lead-in "Session-CP-14 (2026-09-17; D21 — first-training label rule)" inside E11 §3.3, directly after the Session-A P2 training-procedure paragraph, stating D21 (a)–(e) in prose; add one Appendix AJ row for it; no formula is rewritten.

D22 — ISSUE-CP14-002, catch-up failure vs freshness (owner decision 2026-09-17, binding, record verbatim): freshness_ok is defined ONLY by the D14 law — freshness_ok ⇔ staleness_seconds ≤ freshness_sla_seconds, measured on the bars actually present in the store. A catch-up failure never rewrites freshness. It is recorded separately: the cycle JSON catch_up.failures[] entry carries the cell, the error code and the frontier it tried from, and the cell receives a per-cycle named status CATCH_UP_FAILED which the plan stage treats as blocking for that cell in that cycle (the cell is skipped with that status and no plan is produced for it); all other cells proceed normally; the next cycle retries the catch-up. Add a test for the case "catch-up failed but data still within SLA": freshness_ok stays true, cell status CATCH_UP_FAILED, no plan produced. Reflect this in the G4/G5 tests and in the HANDOFF_CP14 interface notes. In APEX_GEN5.md §9.5 item 14, the P1 wording "the cell is marked stale" is to be read as this named status, not as a freshness override — add a one-line clarification there under the same "Session-CP-14 (2026-09-17)" lead-in.

- ISSUE-CP14-001 / ISSUE-CP14-002 resolution: CLOSED(design-side by owner D21/D22 above); code and tests remain pending. The transport truncated the first response within D21(f); the complete D21(f) supplied in the continuation is recorded above. The complete substantive D22 was received; no missing trailing administrative fragment is treated as authority.

- [ISSUE-CP14-003] severity: MAJOR | status: CLOSED-with-regression
  A: SQLiteStore.insert_snapshot accepts a quality_state mapping and reads its min_q/weighted_q fields (apex/data_catalog/store/sqlite_store.py L559-573).
  B: The same mapping is passed directly as SQLite bind parameter 9; test_cp14_insert_snapshot_accepts_quality_mapping failed before the fix with sqlite3.ProgrammingError: Error binding parameter 9: type 'dict' is not supported. Passing a JSON string instead fails the method's mapping .get calls. Omitting quality entirely avoids the bind error but cannot store the producer's required quality state.
  Rule applied: owner CP-14 single-method-body exception for the uncallable canonical snapshot payload; no DDL/schema/other-method change.
  Interim behavior: serialize the quality_state mapping with canonical_json at the bind boundary; retain all columns and the numeric min/weighted projections. Regression test reads the mapping and scalar values back.
  Needs from owner: none; evidence is tests/integration/test_store_integration.py::test_cp14_insert_snapshot_accepts_quality_mapping (pre-fix failure recorded above).

### ADR-CP14 (under ADR-P2-003)
- **ADR-CP14-001 — Governed additive paths.** `apex/ops/engine_context.py` owns the engine-context wiring and classifier/account boundary; `tests/unit/test_engine_context.py` is its battery under the existing normative tests/unit directory; `tests/fixtures/e11_classifier_v1.yaml` is a small test-only classifier artifact. Section 9.5 now names these paths with the Session-CP-14 lead-in and Appendix AJ records the addition. `params/paper_account_v1.yaml` was already mandated by P7/ADR-SA-003. The trained runtime `params/e11_classifier_v1.yaml` remained absent and unignored at that checkpoint; ADR-CP14-021 now requires the runtime path to stay gitignored. No CLI fixture fallback is authorized.
- **ADR-CP14-002 — CIRCUIT_OPEN registry mapping.** Ch.7 and ErrorCode have code/meaning/handling fields, no severity column or severity attribute. Add the exact three Ch.7 fields without inventing a new registry schema; the P0 alert severity is already defined by Ch.21. Existing -1021/-2026 mappings stay untouched.

- [ISSUE-CP14-004] severity: MINOR | status: CLOSED
  A: G3 requests the exact Ch.7 severity and text.
  B: Ch.7 and apex/errors.py contain no severity field. The existing test pins 23 rows before the Session-A P6 addition.
  Rule applied: exact normative registry schema wins; D9 adds the row, not a new schema.
  Interim behavior: code/meaning/handling copied verbatim; owner-authorized registry test pin increases to 24; no other new registry code.
  Needs from owner: none.

- [ISSUE-CP14-005] severity: MAJOR | status: CLOSED-with-regression-pending
  A: D4/P7 requires PAPER and LIVE balance separation everywhere.
  B: ControlPlane.screen_main_menu interpolates paper_balance for every environment, including LIVE (apex/telegram/control_plane.py L526).
  Rule applied: D4; absent LIVE account measurements cannot become a PAPER amount.
  Interim behavior: PAPER displays its injected YAML/ledger balance; every other environment displays UNAVAILABLE until its own authoritative balance source is wired. ENVS and EXPORT_ENVIRONMENTS remain separated and unchanged.
  Needs from owner: none.

- [ISSUE-CP14-006] severity: MAJOR | status: CLOSED-with-regression
  A: E03 compute's calibration helper promises a continuation close beyond the climax candle within five bars (apex/engines/e03_volume/engine.py L1021-1024).
  B: ParticipationEvidence has no c attribute (L447-480), but _climax_calibration dereferences f.c, then tests a volume ratio instead of price continuation. The real existing helper failed with AttributeError: 'ParticipationEvidence' object has no attribute 'c' when given a climax and a subsequent participation event. Direct VolumeEngineV4 state driving does not call this helper.
  Rule applied: fix the obvious engine-internal adapter error, not a silent switch to another execution path; no frozen engine base or data-catalog change.
  Interim behavior: compute passes its actual input bars to calibration; the helper resolves OHLC by event timestamp and compares future closes in the climax candle's body direction. Test test_cp14_climax_calibration_uses_actual_bar_closes covers continuation and failure with unchanged participation ratios. The existing event/state paths remain in place.
  Needs from owner: none.

- [ISSUE-CP14-007] severity: MAJOR | status: ESCALATED
  A: E11 Section 2 participation raw is 0.6*VolumeZ + 0.4*OI_z (APEX_GEN5.md L11741), and D21 trains from the harvested store only.
  B: Toobit public klines have no OI; parse_kline_to_observation returns oi=None and bootstrap ingests oi_state=MISSING (apex/ops/bootstrap_service.py ingest_observations). E03 ParticipationEvidence.oi_z is optional, while E11 compute_state_vector requires finite participation_raw. No missing-OI transformation for this E11 feature is specified; its zero-volume handling is a different condition and cannot authorize substituting OI_z=0. The phone's described kline-only harvest therefore does not supply this term.
  Rule applied: T-DC-004 (missing OI is never numerical zero), D16 store/source restriction, No-Guess.
  Interim behavior: no missing-OI numerical substitute or feature reweighting is implemented. Complete store-derived E11 feature construction remains pending this adjudication; no successful trainer CLI is claimed.
  Needs from owner: choose strict refusal of missing-OI training samples, or authorize an exact missing-component transformation and its quality/freshness consequences. If strict refusal is chosen, the kline-only phone store cannot by itself be promised a train-e11 PASS.

- [ISSUE-CP14-008] severity: MAJOR | status: ESCALATED
  A: P1 requires authoritative capital_hard_cap and per-TF p_min_tf for all 140 cells; Ch.17 identifies capital_hard_cap as owner-set and P_min(tf) as governed (APEX_GEN5.md L17065-17068).
  B: No numeric owner capital hard cap exists in the six frozen YAMLs; the P7 account block contains only capital and simulated-margin fractions. Ch.17 lists P_min for 1m/1h/1d only (a 15m value also appears in a case study, which cannot govern); no 14-TF table is supplied. These required bridge values cannot be inferred from capital, interpolated, or defaulted to permissive kernel infinities.
  Rule applied: P1 authoritative-producer table; Section 9.5-2 (case studies illustrative); No-Guess and no unauthorized YAML fork.
  Interim behavior: no invented owner cap or per-TF probability thresholds. Complete risk/context assembly remains pending; existing kernel and six YAMLs are untouched.
  Needs from owner: provide the capital hard cap and the missing per-TF probability policy, and authorize their exact governed storage location (the six YAMLs are frozen and P7's new account block currently has an exact two-key schema), or explicitly require fail-closed unconfigured cells.

- [ISSUE-CP14-009] severity: MINOR | status: ESCALATED
  A: CP-14 requires absent-until-trained classifier YAML in the allowlist and a LIVE query that never sees paper_balance.
  B: tests/unit/test_config.py::test_params_loader_reads_only requires every allowlisted YAML to exist; tests/unit/test_telegram_control_plane.py::TestControlPlaneGuardrails::test_the_environment_and_balance_are_displayed_from_the_settings explicitly requires the injected PAPER balance in a LIVE title. Targeted run: these two failures, 213 passed. Both old assertions contradict the newly mandated behavior. The brief pre-authorizes only two other pin updates, not these.
  Rule applied: do not weaken production separation or commit a runtime fixture merely to satisfy stale tests; escalate additional assertion changes.
  Interim behavior: the conflicting tests are left unchanged pending owner approval; new tests assert missing-artifact fail-closed/lazy boot behavior and no PAPER balance in LIVE. No final green-suite or completion claim.
  Needs from owner: authorize updating these two obsolete assertions to assert the new requirements, preserving coverage and counts.

### OWNER ANSWERS to CP-14 issues (2026-09-17) — continued

D23 for ISSUE-CP14-007 (owner decision 2026-09-17, binding): apply the document's own missing-OI policy to E11 participation. When E03 reports oi_state != AVAILABLE (MISSING, STALE, INVALID, DEGRADED — OI_z is None), the OI term's weight is set to 0 and the remaining weight is renormalised, exactly as E03 §3.6 already does for its A/D weights: raw_part = VolumeZ, x6 = sigmoid(raw_part). Never substitute OI=0 (AI.6). The same rule applies identically in training (train-e11) and at runtime (the G1 producer), so there is no train/serve skew. When oi_state == AVAILABLE the full formula 0.6*VolumeZ + 0.4*OI_z applies unchanged. Quality: the sample/candle carries oi_state and a participation=PARTIAL marker in contributing_features; E11 quality Q5 is unreachable while OI is not AVAILABLE (Q5 requires all dependencies healthy per E11 §1); no other quality change. No sample is excluded for missing OI; the degenerate-class refusal is unaffected. Doc patch under the "Session-CP-14 (2026-09-17)" lead-in: one sentence after the E11 §2 participation row (x6) + one Appendix AJ row. Tests: OI None → raw_part == VolumeZ, oi_state recorded, Q5 never; OI available → 0.6/0.4 formula. Record D23 verbatim in PHASE2_DECISION_LOG.md under "OWNER ANSWERS to CP-14 issues (2026-09-17)". Then ask ISSUE-CP14-008 and 009 one at a time, each with A/B and your recommendation.

D24 for ISSUE-CP14-009 (owner decision 2026-09-17, binding): both updates authorised. (1) The artifact test now asserts the lazy fail-closed behaviour: with params/e11_classifier_v1.yaml absent, boot/status/bootstrap/repair-partial/train-e11 work and only the E11/plan path raises CONFIGURATION_INVALID; a valid tests/fixtures artifact loads and its artifact_sha256 is verified. (2) The title test now asserts PAPER/LIVE separation per D4: the PAPER balance never appears in any LIVE title/report/export, and appears in PAPER ones. Do not commit any runtime artifact. These are the only two additional pin changes; file both under ISSUE-CP14-009 with before/after assertions.  CORRECTION about earlier answers: any answer you received as a BARE option click WITHOUT accompanying owner text was an accidental click on the phone and is VOID — it is not an owner decision. Only answers that came with explicit text (D21, D22, D23, and this D24) are binding. ISSUE-CP14-008 in particular was answered by an accidental click: discard that selection, do not implement or record it, and re-ask ISSUE-CP14-008 in full (A/B, effect of each, your recommendation). Additionally, list every other prompt of this session that was resolved by a bare click with no owner text (include the question text and which option got selected), mark each as VOID in PHASE2_DECISION_LOG.md, and re-ask them one at a time. If you already implemented anything under a bare-click answer, say exactly what and hold it until the re-asked decision arrives. Then continue.

### Bare-click audit (D24)
- VOID: question "ISSUE-CP14-001: what should supply entropy for the first E11 training labels, before trained W/b exists?"; bare selection "Authorize a precise bootstrap rule" (specify_bootstrap). No implementation was authorized by this click. The subsequently supplied explicit D21(a)-(f), not this selection, is the basis of the D21 documentation and training-label helpers. The initial text was truncated; the explicit continuation completed D21(f).
- ISSUE-CP14-008 remains OPEN/UNDECIDED. Its bare selection is discarded, not treated as a decision; no cap, timeframe probability values, or parameter storage location has been implemented from it.
- There were no other bare-click-only answers in this session. D22, D23 and D24 arrived as explicit text; the continuation question also received explicit text. No implemented behavior depends on a bare click. Re-ask ISSUE-CP14-008 first, then the earlier E11 question individually as requested; retain the explicit D21 as binding unless the owner changes it in text.
- ISSUE-CP14-007 and ISSUE-CP14-009: CLOSED(design-side by explicit D23/D24); implementation/expanded tests remain pending. Before/after assertions for ISSUE-CP14-009 will be recorded when updated.

### OWNER ANSWERS to CP-14 issues (2026-09-17) — D25

D25 for ISSUE-CP14-008 (owner decision 2026-09-17, binding, record verbatim): (1) capital_hard_cap = 0.60 of capital (fraction of capital_usdt), equal to the Ch.17 SL-12 governed default portfolio_exposure_cap 60 %, so veto 3 "portfolio_exposure + proposed > capital_hard_cap" fires exactly at the documented portfolio ceiling; in PAPER with capital_usdt 10000.0 this is 6000 USDT notional exposure. No other number is invented. (2) P_min(tf): governed anchors stay 1m 0.52, 1h 0.55, 1d 0.50; every timeframe without a governed value (3m, 5m, 15m, 30m, 2h, 4h, 6h, 8h, 12h, 1w, 1mo) takes the STRICTEST governed anchor, 0.55 — no interpolation; lowering later is a governed change. c_min = 0.50 as in SL-12. (3) Apply the same "strictest governed anchor" rule to any other per-timeframe SL-12 input that the 23 REQUIRED_RISK_KEYS need and the document leaves incomplete (e.g. H_max(tf): 12 for all timeframes without a governed value, since stricter = fewer candles); list every such key under this same ISSUE with the anchor used. (4) Storage: one additive file params/decision_runtime_v1.yaml (ADR-CP14-00N under ADR-P2-003, §9.5 repo tree line + one Appendix AJ row + apex/config.py allowlist), containing only the keys that have no home in the six frozen YAMLs or in paper_account_v1.yaml: capital_hard_cap_fraction, p_min_tf (all 14), c_min, and any key from (3); each with a comment naming its Ch.17 SL-12 source and D25. Twin rule as in P7: the SL-12 table wins on any mismatch. Never duplicate values already in risk_defaults_v1.yaml (ADR-SA-004). (5) Both PAPER and LIVE read this file; nothing here changes PAPER/LIVE separation. Unconfigured-cell fail-closed behaviour stays as the code path for a missing/invalid file. Add tests: file loads; all 14 timeframes present; strictest-anchor rule; missing file → fail-closed. Continue with the producer and trainer.

- ISSUE-CP14-008: CLOSED(design-side by explicit D25); implementation and tests pending. The void click is not the authority.

- Owner confirmation 2026-09-17 for ISSUE-CP14-001: D21 (a)–(f) is retained UNCHANGED and is the binding resolution. The void bare click is superseded by this text. No new labelling policy. Record this confirmation under ISSUE-CP14-001 in PHASE2_DECISION_LOG.md and continue with the producer and trainer; pause again only for a genuinely new decision.
- **ADR-CP14-003 (under ADR-P2-003):** owner D25 authorizes the additive `params/decision_runtime_v1.yaml`, its Section 9.5 tree/Appendix AJ row and config allowlist entry. No duplication of risk_defaults. The 23 risk keys need no additional incomplete per-TF table: freshness/OI thresholds already cover all 14 TFs; H_max is a forecast-horizon input, not one of those 23, and is not changed by this checkpoint.
- ISSUE-CP14-009 implementation: before, test_params_loader_reads_only required every allowlisted file to exist; after, the generated classifier is explicitly tested as lazy FileNotFoundError while all installed YAMLs still load. Before, the LIVE title assertion demanded the injected paper amount; after, it rejects that amount in LIVE and still requires it in PAPER. Expanded classifier and environment tests remain separate; neither original test is deleted.

- [ISSUE-CP14-010] severity: MAJOR | status: CLOSED-with-regression-pending
  A: P1 runtime order names E04's ATR_20 as E03's input; E11 consumes an ATR normalization input (atr_z).
  B: E04's frozen VolatilityState exports ATR14/ATR50, not ATR20. The carried-over description attributed ATR20 to E11 Section 2, but the actual named ATR20 edge is P1/E03; there is no ATR20 export in code.
  Rule applied: P1 producer ownership; Section 9.5 consumer projection mapping, not a second indicator implementation.
  Interim behavior: add atr20_series inside E04, reusing its canonical true_range/WilderATR(20); warmup returns None, ATR14/50 schema unchanged. The wiring must consume this E04 projection, not invent ATR math.
  Needs from owner: none.

### ISSUE-CP14-001 — reaffirmation acknowledged (2026-09-17)
The owner's repeated textual confirmation retains D21(a)–(f) UNCHANGED. The verbatim confirmation above remains the binding record. No new bootstrap, entropy, confirmation, label-membership or class-balancing policy is introduced. No decision on ISSUE-CP14-001 is outstanding.

- **ADR-CP14-004 (under ADR-P2-003):** `engine_context.py` owns the deterministic multinomial log-loss fitter and supported-YAML atomic writer. Optimizer implementation: float64, fixed full-batch order, 2000 gradient steps at learning rate 0.2, seeded normal initial W with standard deviation 0.01, zero initial b, no reweighting or synthetic members. Initialization is training-only, never a runtime classifier fallback. Negative integer seeds map modulo 2^128 for the RNG while retaining the original recorded seed. This chooses numerical optimizer mechanics, not a new label policy. Whole-store/two-process successful training remains unverified while ISSUE-CP14-011 is open.

- [ISSUE-CP14-011] severity: MAJOR | status: ESCALATED
  A: E11 Sections 1.2/2/3.1 require finite atr_z and raw_liq=density*age/(1+sweepRate), with PIT consumer projections from the frozen E04/E02 schemas. Their values alter compression/liquidity features and therefore the unchanged D21 labels.
  B: E04 exports ATR14/ATR50 but no atr_z; the plan specifies the atr_z < -1 compression adjustment without defining its source ATR period or z-score reference window. E02 exports a dictionary of levels, pools and sweep log, not scalar age_score/sweep_rate/level_density; the pool density schema at Section 5 marks density informational, never a gate input. No aggregate pool/level population, age statistic or sweep-rate denominator is defined for this E11 projection. The provisional adapter used active-level count/price span, mean age, sweep count/window length and ATR20 z-scored against 20 prior ATR values; those choices were not authorized and are now removed.
  Rule applied: No-Guess, frozen producer schemas and D21 unchanged. Choosing feature formulas is distinct from choosing label policy; no zero fill or sample fabrication.
  Interim behavior: the first seven engine outputs remain available, but the E11 input projection explicitly reports E11_ATR_Z_UNCONFIGURED and E11_LIQUIDITY_PROJECTION_UNCONFIGURED. train-e11 refuses with its nine-class histogram and exclusion diagnostics; it cannot write a classifier from these unconfigured features. No successful store-only training or complete producer claim.
  Needs from owner: specify/authorize the exact ATR_z and aggregate liquidity consumer projections (source population, lookback, lag and degenerate behavior), or point to their governing definitions. Recommendation to adjudicate, not implemented: ATR20 against the prior E11 180-day reference window using Method-B epsilon regularization (before sigmoid); active E02 levels at t, density=count/price span, age=arithmetic mean age in bars, sweepRate=number of confirmed sweeps over the same engine window divided by its closed-bar count; undefined spans/histories fail closed. The 180-day/engine-window interpretation must be explicit for non-H1 timeframes. D21 remains unchanged whichever projection is authorized.

- [ISSUE-CP14-012] severity: MAJOR | status: CLOSED-with-regression-pending
  A: G1 requires complete EvidenceEvent persistence/readback through the public frozen store API.
  B: EvidenceEvent.to_ddl_row retains explanation in raw but drops availability_time, observation_window, lifecycle and other semantic fields. A reduced SQL row cannot reconstruct the contract.
  Rule applied: no frozen catalogue/store change; use the existing raw TEXT carrier through public insert_evidence.
  Interim behavior: a storage-only event copy places a versioned envelope with all 24 original fields and canonical payload hash in explanation/raw. The original explanation is inside the envelope and restored. Readback validates schema, hash, identity and the original contract; legacy reduced rows refuse rather than acquire invented fields. Repeated identical IDs are idempotent; collisions refuse.
  Needs from owner: none; producer integration remains pending, independent round-trip regressions are required.

- [ISSUE-CP14-013] severity: MAJOR | status: CLOSED-with-regression-pending
  A: E02 level age uses local bar indices; engines consume close-time candles and E03 takes max dependency availability.
  B: get_window provides open-time observations with sequences relative to the returned window. Slicing a training window without rebasing sequence gives negative E02 ages. The original raw receipt may be 1970 while an E04-derived lagged ATR projection is produced at the current closed evaluation boundary.
  Rule applied: in-memory producer projection only, never raw timestamp/availability rewriting or SQL mutation.
  Interim behavior: closed_engine_window derives close timestamps, maps final CORRECTED status to CLOSED for engine input, and enumerates local sequence indices. Raw availability_time is retained. The E04 lagged ATR projection is evaluated/emitted at the current close and carries that derived availability independently of raw receipt; E03 accepts aligned per-bar OI/ATR availability lists as well as its unchanged scalar contract. Misaligned lists refuse.
  Needs from owner: none for index/contract mechanics. These do not authorize the missing E11 projections in ISSUE-CP14-011.

- ISSUE-CP14-010/012/013 regression evidence (2026-09-17): the added context tests verify E04 ATR20 against true_range/WilderATR, real seven-engine execution without OI, preserved raw availability and rebased local indices, aligned E03 availability/misalignment refusal, and all-24-field public-store round-trip/hash/schema/identity/idempotence/legacy-row refusal. Targeted engine/context/config/control/store suite: 315 passed in 3.11 s. These helper tests do not close G1 integration or ISSUE-CP14-011.
- ISSUE-CP14-007 implementation evidence: all five OI states tested; AVAILABLE uses 0.6/0.4 and can reach Q5, the other four use VolumeZ directly and cap only Q5 to Q4; Q2 remains Q2. The marker is carried in the E11 wrapper/evidence observation_window and canonical snapshot payload, without adding fields to the frozen RegimeState additionalProperties:false schema.
- ISSUE-CP14-008 implementation evidence: D25 YAML all 14 TFs/strictest anchor/6000 USDT cap/c_min tested, with missing, incomplete and invalid file failures. Reading it from the final bound producer remains pending.

- [ISSUE-CP14-014] severity: MINOR | status: OPEN-until-commit
  A: The full suite's CP-8 read-only test executes git status --porcelain params and requires empty output.
  B: The two owner-authorized new CP-14 YAML files are still untracked in this in-progress branch. Full suite therefore reports 2777 passed, 1 failed, 14 warnings in 84.95 s; failure is test_frozen_params_files_are_untouched_by_this_suite, listing only those two new paths, not any changed frozen YAML. This run preceded two additional helper tests.
  Rule applied: preserve the original test; no extra assertion changes or ignore rules. No false twice-green claim.
  Interim behavior: leave the test intact. The normal implementation commit will make authorized additions tracked/clean; then rerun the complete suite twice on the completed checkpoint. No premature push/PR solely to clear a housekeeping test.
  Needs from owner: none.

### OWNER ANSWERS to CP-14 issues (2026-09-17) — D26-A

D26-A for ISSUE-CP14-011, part 1 of 2 (owner decision 2026-09-17, binding, record verbatim; part 2 follows). atr_z: the document's own freeze note (Ch. E12 interface provenance, "E04 has no atr_z field; consumers must map to the frozen v4.0.0 schemas") governs. There is NO ATR20 in E04 — do not use one. atr_z = z-score of E04 VolatilityState.atr14_wilder (the canonical published ATR that E03 and E02 already consume) against its own prior same-cell history: atr_z_t = (atr14_t − μ_{t−1,W}) / (σ_{t−1,W} + ε), using the SAME rolling reference-window helper and constants the E11 engine already uses for Method B (W = the 180-day window capped by available history, lag 1, the helper's existing minimum-prior-samples rule, E11 ε) — introduce no new window constant. Below the helper's minimum history the value is unavailable and the sample/candle fails closed exactly as the existing INVALID_E11_HISTORY path does. Same definition in train-e11 and in the G1 producer. Doc patch: one sentence under the "Session-CP-14 (2026-09-17)" lead-in after the E04 input line in E11 §1.2, plus one Appendix AJ row. Test: known series → expected z; short history → fail closed.

- ISSUE-CP14-011 part 1: CLOSED(design-side by D26-A); part 2, liquidity projection, awaits the owner's announced continuation. D21 stays unchanged. The former ATR20 recommendation and ISSUE-CP14-010's added ATR20 projection are superseded, not retained as authority.
- D26-A implementation: remove atr20_series and all production ATR20 uses; E03's lagged ATR adapter now aligns the published E04 atr14_wilder by state.as_of. Extract E11's existing Method-B mean/sigma and minimum/degenerate guards into rolling_method_b_reference; rolling_sigmoid_norm calls it without changing its pre-existing numeric result. atr_z_input uses that same reference, existing W_180D_H1 and EPS, with D26-A's explicit denominator sigma+EPS and no sigmoid/clipping. The shared feature_timeline carries prior same-cell ATR14 history separately and advances it only after the current projection. An explicit empty/short reference remains INVALID_E11_HISTORY, never a synthesized value or a fallback history.
- D26-A verification (2026-09-17): context/E11/E04/E03 suite = 250 passed in 25.48 s; final context/E11 verification = 134 passed in 1.85 s. Tests cover known ATR14 z-score including the explicit sigma+EPS denominator, unchanged Method-B sigmoid values, shared reference-helper invocation, the existing window cap, short/empty/degenerate history refusal, no z clipping, lagged published ATR14 consumed by E03, and per-cell history isolation/advance after projection. The removed ATR20 test was a new, uncommitted CP-14 test, replaced by the D26-A canonical-source regression; no original frozen-test assertion was changed. E04 engine bytes now match branch HEAD exactly. Part 2 is still pending; no successful whole-store training or completed G1 claim.

### OWNER ANSWERS to CP-14 issues (2026-09-17) — D27

D27 for ISSUE-CP14-019 (owner decision 2026-09-17, binding, record verbatim): authorised as recommended, because it is what the document already says. Ch.8 defines "regime_uncertainty = 1 − regime_confidence" and the P1 table defines regime_confidence = p_max of the E11 probabilities at as_of; therefore regime_uncertainty = 1 − p_max ∈ [0,1], which is what the frozen confidence combiner (apex/fabric/context.py COMBINER_INPUT_QX) requires. The P1 table row "regime_uncertainty | E11 entropy H_t (§3.3)" is a Session-A error: correct that row in place to "1 − regime_confidence = 1 − p_max (Ch.8)" under the "Session-CP-14 (2026-09-17)" lead-in, plus one Appendix AJ row. Raw H stays in the complete E11 state; h_norm = H/ln 9 stays the only normalised-entropy key. No change to the combiner or its weights. Test: p_max = 0.4 → regime_uncertainty 0.6; combiner accepts the produced context. Same in train-e11 (if used) and the G1 producer. Also: the real-store refusal (7 of 9 classes, TRANSITION and TREND empty) is the correct P2/D21 behaviour on sandbox data; do not weaken it — the phone store decides. Now ask ISSUE-CP14-017 and ISSUE-CP14-018 immediately, together in one message, each with A/B, options, effects and your recommendation, so all open decisions close in one round; then finish G1/G2 validation, run the suite twice, push and open the ONE PR.

- ISSUE-CP14-019: design resolved by D27. Implementation, in-place P1 correction, Appendix AJ entry and regression remain to be verified in the current checkout. No confidence-combiner or weight change is authorized.
- Checkout reconciliation (current-session inspection): HEAD is d77ce13, the two policy YAMLs are untracked, and engine_context.py currently has 662 lines. The earlier reported local commit 583d021 and later integration/test changes are not present in this checkout. Do not treat the previously reported 2804-test runs as verification of the current files. Reconcile the missing implementation from the conversation's recorded requirements and re-run verification before any push/completion claim; preserve all existing work and remain on arena/01a0afeb-upstage.

### CP-14 open source decisions carried forward for joint owner adjudication

- [ISSUE-CP14-017] severity: MAJOR | status: ESCALATED
  A: P1/AF.3 require an active versioned parameter package, authoritative family lifecycle status, governed quality/alignment/recency weights and measured venue facts.
  B: Kline ingestion alone supplies no approved parameter-package activation, family registry or arbitration weights. AF.3 prohibits hardcoded weights; missing position-correlation and family-outcome statistics cannot become measured zero or recency=1. An unpopulated DECISION_INPUTS reader does not constitute a production writer/source.
  Rule applied: No-Guess; D25 does not supply these separate governance decisions. Venue commission, funding, multiplier and expiry must retain actual source provenance.
  Interim behavior: missing required governance/venue facts refuse the affected plan. No bootstrap weights or lifecycle promotion is authorized by this issue record.
  Needs from owner: existing approved package/registry/weight source, or explicit PAPER-only bootstrap policy plus governed storage and missing-component behavior. Concrete options and effects are asked together with ISSUE-CP14-018 in chat, not stored as prompts.

- [ISSUE-CP14-018] severity: MAJOR | status: ESCALATED
  A: P1/P7 require PAPER margin health derived from the paper account and positions, with the existing 0.60/0.40/0.20 thresholds.
  B: Thresholds do not define margin used. Gross notional is not automatically initial margin or maintenance distance; using (capital-notional)/capital silently chooses a full-notional reservation model. Leverage-aware simulation also needs authoritative leverage and margin terms, and CP-15 simulator state is out of scope.
  Rule applied: No-Guess; no paper_sim_state, fabricated leverage, guessed maintenance rates or silent clipping.
  Interim behavior: do not infer a margin metric from the threshold YAML alone; missing authoritative measurement/model blocks plan admission. G6 balance accounting remains independent.
  Needs from owner: explicitly authorize a PAPER-only reservation proxy, including pending-order treatment and invalid-input behavior, or provide an exact leverage-aware metric and its authoritative inputs. Concrete options and effects are asked in chat.

### OWNER ANSWERS to CP-14 issues (2026-09-17) — D28

D28 for ISSUE-CP14-017 (owner decision 2026-09-17, binding, record verbatim): Option 2 authorised as a PAPER-only bootstrap, with these amendments. (1) parameter_package_id is not a fixed string: it is "cp14_paper_bootstrap-v1.0.0-" + first 12 hex of sha256 over the canonical bytes of all governed params/*.yaml present plus params/e11_classifier_v1.yaml when present, so any governed change yields a new package id (document §2 snapshot binding). (2) Family lifecycle: when the Setup Family Registry holds no record for a family, its status is ACCUMULATING (Ch.11 defines ACCUMULATING as the backtest/paper phase below the 30-trade promotion floor); an existing record is never overwritten; promotion/demotion stays owner-driven per AD.3. (3) Arbitration weights quality=1.0, alignment=0.0, recency=0.0 stored as governed values in a paper_bootstrap section of params/decision_runtime_v1.yaml (ADR + tree + AJ row); alignment and recency inputs that have no measured basis are reported UNAVAILABLE and excluded from arithmetic, never a measured 0 or 1; changing the weights later is a governed package change. (4) Venue facts keep their real provenance, no guesses: commissionRate, funding rate, contract multiplier, contract type and expiry come from Toobit PUBLIC endpoints (exchangeInfo / funding) which need no API key — the same public path G4 uses; a missing fact fails closed for that cell with a named status, never a default fee. (5) LIVE: no bootstrap permission of any kind; the section is ignored when APEX_ENV=LIVE and a test proves it. Doc: one paragraph under the "Session-CP-14 (2026-09-17)" lead-in in Ch.12 after the "All weights are governed" bullet, plus one AJ row.

- ISSUE-CP14-017: design resolved by D28; implementation and source-wiring acceptance require tests. ISSUE-CP14-018 remains open: D28 authorizes no margin formula, leverage assumption or simulator state.
- **ADR-CP14-005 (under ADR-P2-003):** D28 extends the existing additive decision_runtime_v1.yaml with a PAPER-only paper_bootstrap.arbitration_weights mapping. Existing engine_context.py owns canonical parameter binding, read-only family-status projection and public venue-fact ingestion/readback using the existing snapshot carrier; no frozen store/catalogue or execution edit. Canonical package input is a filename-keyed mapping of parsed governed YAML documents, serialized once with apex.identity.canonical_json (UTF-8); lexical filenames remove traversal-order dependence, names bind membership, comments/formatting do not change governed values, and the optional classifier is included once. Missing mandatory governed files or unparseable/nonfinite content fails closed. Family registry snapshots are read without writing a fallback record, so ACCUMULATING never overwrites owner history. D28's UNAVAILABLE components are explicitly omitted from arbitration arithmetic only at zero governed weight; a positive-weight unavailable component refuses. Public source failures never fall back to signing or guessed values. Original six YAMLs, confidence-combiner weights, and registry code set stay unchanged.

- D27 implementation evidence: `regime_uncertainty_input` validates E11's probability simplex and returns exactly 1-p_max. A p_max=0.4 regression produces 0.6 and passes the unchanged real Ch.8 confidence combiner; raw entropy and normalized-entropy calculations are untouched. The Session-A P1 source row is corrected in place under the CP-14 lead-in, with its own Appendix AJ row. This validates the projection, not the still-incomplete full G1 producer.
- D28 implementation evidence: governed paper_bootstrap weights are loaded from YAML; package ID hashes canonical filename-bound governed YAML values with optional classifier inclusion. Tests cover reproducibility in two independent processes (package identity, NOT classifier-training determinism), governed-weight/classifier changes, missing-file refusal, comment-only stability, and all four preserved family lifecycle states. Owner registry snapshots are read append-only/PIT; missing family records default without writing a lifecycle record. Invalid registry records refuse instead of becoming absence.
- D28 LIVE evidence: the runtime-policy reader defaults to the actual Config environment; LIVE omits/does not validate the PAPER section, even if its semantic content is invalid. Package/bootstrap builders return no LIVE package before reading files. The producer defaults to Config.apex_env and refuses the PAPER governance reader for LIVE. The PAPER metadata publisher requires an explicit environment and refuses non-PAPER publication. No LIVE account balance or execution behavior is changed.
- D28 public-fact evidence: actual ToobitPublicClient public methods are tested over tests/fake_toobit_responder.py, with no API-key/signature and only exchangeInfo/funding GETs. Explicit commissionRate/takerCommissionRate, contractMultiplier, contractType and deliveryDate fields are consumed; absent/invalid facts get named per-symbol refusals. PERPETUAL must be explicitly published before expiry can be None; conflicting expiry refuses. No signed commission fallback or default fee is implemented. Tests establish behavior on API-shaped fixtures, not that the live venue currently publishes every needed field.
- D28 persistence evidence: successful and failed public reads append immutable, full-64-hex, package-bound snapshots through insert_snapshot. Readback checks full payload identity, source hash, symbol, observation time and PIT cutoff. A failed later refresh blocks that symbol instead of reviving an older success, without poisoning another symbol. These helper publisher/reader interfaces are ready for the pending G1 serve composition; they are not yet bound into a complete production engine-context path.
- D28 bridge evidence: zero-weight UNAVAILABLE components are excluded before numeric conversion/multiplication; positive-weight unavailable components refuse. The real PaperPlanBridge passes a new integration test using D28-generated governance and the existing explicitly test-only engine/risk context fixture. This is not a claim of a complete harvested-store G1 context or an authorized margin formula.
- Current-checkout verification (2026-09-18): installed the unchanged requirements.lock and dev-only pytest in the refreshed sandbox; no lock/SBOM edit. Focused context/config/decision/bridge/paper-loop suite: 152 passed, 13 warnings (7.49 s). Full `python -m pytest tests -q`: 2812 passed, 0 failed, 14 warnings (95.20 s). Local commit 4999d86 adds only the two authorized policy YAMLs, including D28's amendment; original cleanliness test unchanged and now passes. No final twice-green claim, push or PR. ISSUE-CP14-018 remains unanswered; current checkout recovery of the later G1/G2 implementation also remains pending. D27 expressly accepts genuine seven-of-nine sandbox refusal; do not create class members or weaken D21.

### OWNER ANSWERS to CP-14 issues (2026-09-17) — D29

D29 for ISSUE-CP14-018 (owner decision 2026-09-17, binding, record verbatim): Option 1 authorised. C = paper_account_v1.yaml capital_usdt + realised PAPER P/L from the ledger; N = gross marked notional of open PAPER positions + full notional of unfilled risk-increasing orders (reduce-only orders reserve nothing; no double count of filled quantity); margin_health_fraction = (C − N) / C, full notional reserved with no leverage discount. The P7 thresholds apply to this fraction: warning below 0.60, action (veto 14 MARGIN_HEALTH blocks new entries) below 0.40, liquidation-approach (Emergency L3 CANCEL_ALL) below 0.20. Missing marks or order state, C ≤ 0, or a result outside [0,1] fail closed with a named status — no clipping. Exposure caps stay separate. This is documented as the PAPER reservation proxy, not exchange liquidation distance, and it is BINDING for CP-15 too: the simulator's query_account_margin_health must return exactly this fraction from its durable state (write that into the P3 passage and HANDOFF_CP14 INTERFACES). LIVE unchanged (query_account_margin_health). Doc: one paragraph under the "Session-CP-14 (2026-09-17)" lead-in after the P7 YAML block in §9.5, plus one AJ row. Tests: no positions → 1.0; N = 0.5·C → 0.5 with warning; N = 0.65·C → action; C ≤ 0 → fail closed.

- ISSUE-CP14-018 is design-resolved by D29; implementation proceeds without further owner policy questions.
- [ISSUE-CP14-025] severity: MAJOR | status: IMPLEMENTING
  A: D29's PAPER reservation proxy specifies strictly-below 0.60/0.40/0.20 thresholds and leaves LIVE unchanged.
  B: Existing margin_health_state and veto 14 use inclusive threshold comparisons for the venue metric.
  Rule applied: owner D29 supersedes the boundary only for the explicitly identified PAPER reservation proxy. Keep the original default/LIVE comparisons and the same veto registry; no execution-stage edit.
  Interim behavior: add a PAPER-aware threshold projection and require the PAPER environment plus reservation-model marker for strict veto-14 evaluation. Decimal account/notional arithmetic rejects invalid/nonfinite/out-of-range measurements, without clipping or leverage discounts. Existing exposure-cap vetoes remain separate.
  Needs from owner: none; exact boundary regressions are required.
- D29 ledger projection contract: use existing public LedgerWriter reads, nested raw.payload, explicit FSM environment, and PaperRuntime's documented i-{proposal_id[-12:]} identity. Never associate an arbitrary fill with the latest trade. Missing/ambiguous order provenance or reconciliation-required state refuses. Current FSM SUBMIT_ORDER is the entry operation; its submitted quantity/price are authoritative, not a guessed leverage or native quantity. Count filled OPEN quantity once in positions and subtract it from pending entry quantity. Cancellation/rejection removes only the remaining reservation. No new table or execution transport.

- ISSUE-CP14-018 / ISSUE-CP14-025 D29 implementation: reservation proxy and public-ledger account readback added, with no schema/transport/execution-stage changes. Strict PAPER threshold boundaries are explicitly scoped by environment/model; LIVE behavior is unchanged. Complete context/risk regression suite: 169 passed in 3.28 s. D29 adds 14 parametrized cases, including a real mixed-environment LedgerWriter chain with partial fill, cancellation and realized P/L. P7/AJ updated and P3/HANDOFF_CP14 INTERFACES now bind the exact formula for CP-15. G1 integration remains pending; no simulator implementation claimed. Green milestone is checkpointed/pushed immediately per the owner instruction.

### OWNER ANSWERS to CP-14 issues — D26-B (verbatim recovered from owner archive)

D26-B for ISSUE-CP14-011, part 2 of 2 (owner decision 2026-09-17, binding, record verbatim). E02 consumer projection for E11, anchored on the document's own Pool schema definitions: live levels = E02 levels with fate ACTIVE or STRENGTHENED (the engine's own live set). level_density = count(live levels) / price span of live levels (max price − min price), the document's "count of member levels / width" definition. age_score = MAX over live levels of Age_i = t − last_touch_i in bars (the document's Pool "age = max age of member levels, in bars"; not the mean). sweep_rate = number of confirmed §3.6 SweepEvents (five prerequisites; WickOnly excluded) divided by the number of CLOSED bars inside the E02 engine's existing retained window — reuse that window constant, add none. Edge case (owner decision): fewer than two live levels ⇒ level_density = 0, age_score = 0, sweep_rate as computed; this is a legitimate "no liquidity structure" observation, not a failure (Method A then yields its documented degenerate-range value). Then raw_liq = density·age / (1 + sweep_rate) per E11 §2 unchanged. Same projection in train-e11 and in the G1 producer. Doc patch: one sentence under the same lead-in after the E02 input line in E11 §1.2 + one Appendix AJ row. Tests: 3 live levels → density/age/sweep exact values; 1 level → zeros; sweep count excludes WickOnly. Record D26-A+B under ISSUE-CP14-011 in the DECISION_LOG "OWNER ANSWERS" section. Note: the single full-suite failure (Git-cleanliness test on the two uncommitted YAMLs) resolves itself once the files are committed — do not modify that test. Continue to completion, push, and open the ONE PR.

- ISSUE-CP14-011 part 2 is design-resolved by D26-B. Restored native projection reads levels.values/fate, uses the engine's existing volume_profile_bars constant, checks all five named prerequisites, and keeps WickOnly excluded. The explicit liquidity_raw IC key binds Method A and both warmup and post-update histories to the actual composite, not to level density alone; legacy callers without the new key retain their original input behavior. No runtime rule-tree/weights/labels changed.
- Audit repair (2026-09-18): D26-B and D29 now reproduce the exact owner messages from the read-only owner-evidence-cp14:chat_history.md archive. The earlier non-verbatim copies are replaced; no policy was re-adjudicated. Archive chat SHA256: 76155a487b66c159612fab50486eaa226a96a3182f30e80bad1ffea50c6f0f13.
- [ISSUE-CP14-026] severity: MAJOR | status: IMPLEMENTED, regression pending
  A: owner D26-B requires the documented Method-A degenerate-reference 0.5 for the new native E02 liquidity composite, including density=age=0 with fewer than two live levels.
  B: legacy E11 rolling_minmax_norm intentionally rejects constant references under ISSUE-CP5-009.
  Rule applied: scope the owner-authorized degenerate projection to the new liquidity_raw IC; preserve the legacy helper and all original tests, short-history and nonfinite refusal.
  Interim behavior: projected_liquidity_norm returns 0.5 only for a finite constant reference with the same minimum of ten prior samples; otherwise it calls the existing Method A. Training and runtime both use compute_state_vector and the same composite history.
  Needs from owner: none; D26-B is the authority. No changes to legacy volatility normalization, labels, tree or original pins.

### Raw-lineage / delayed-label restoration milestone

- D26-B checkpoint `2cd6a81` was pushed immediately after context/E11 verification: 173 passed in 3.29 s. ISSUE-CP14-026's scoped degenerate-reference regression is green; legacy normalization and original pins are unchanged.
- [ISSUE-CP14-027] severity: MAJOR | status: CLOSED-with-evidence
  A: D14/G5 preserve raw availability and D23 requires actual OI provenance; engines must use the public CLOSED-window reader.
  B: frozen get_window projects retrieved_at into availability_time and omits OI timestamps.
  Rule applied: keep get_window as the bar reader; read immutable metadata by observation_id and verify both content-hash bindings in the additive consumer. Never edit another frozen store method or raw row.
  Interim behavior: recover raw availability/OI timestamp and derived OI lag; reject missing/ambiguous lineage and exclude future-available rows. Window cache identities now include the complete projected input, not only endpoint timestamps. Regression proves the legacy reader differs, raw 1970 survives unchanged, real OI lag is recovered and later availability is gated.
  Needs from owner: none; this implements D14 rather than redefining availability.
- [ISSUE-CP14-028] severity: MAJOR | status: CLOSED-with-evidence
  A: D21 finalizes rule0 using an independently observed E01 BOS/CHoCH in the next 48 CLOSED candles.
  B: the incomplete trainer inferred confirmation only after all upstream projections succeeded and equated 48 retained rows with a contiguous horizon.
  Rule applied: independent native E01 confirmation survives unavailable E11 features; require all 49 calendar-aware candles t..t+48, and refuse unknown future confirmation.
  Interim behavior: named NONCONTIGUOUS_LABEL_HORIZON / LABEL_CONFIRMATION_UNAVAILABLE exclusions; actual training-query hash includes this lineage/horizon protocol. No synthetic labels/members/reweighting or all-nine relaxation. Focused regressions verify a missing candle and confirmation despite failed normalization.
  Needs from owner: none; D21 governs.
- [ISSUE-CP14-029] severity: MAJOR | status: CLOSED-with-evidence
  A: E03 as_of_ts is maximum dependency availability, whereas climax continuation compares actual candle closes within five input bars.
  B: CP14-006's first repair keyed actual bars by candle time but then indexed that map with availability, failing on late/equal receipts.
  Rule applied: preserve availability semantics and explicitly bind each native emission to its source index at ingest; no evidence-schema or input-price change.
  Interim behavior: calibration uses aligned source indices and its five-input-bar horizon. Regressions cover equal late receipt timestamps and a six-bar gap; the existing close-price continuation regression remains green. Producer latest-emission validation also uses the accepted source candle identity, not availability equality.
  Needs from owner: none; this corrects the wiring/calibration association, not the signal formula.
- Context/E03/E11 verification for the restoration above: **227 passed, 0 failed, 3.55 s**. This is focused evidence, not the still-pending successful full-store two-process G2 proof or final twice-full-suite evidence. Immediate checkpoint/push follows.

### Native chronological preparation and exact memoization milestone

- A first 830-observation network-free scratch-store training probe timed out after 1800 s, without a returned histogram or artifact. A 300-bar upstream profile measured 19.08 s under cProfile: 14.02 s E04 replay (11.56 s repeated GARCH fits) and 3.68 s E01 replay. This is a preparation-performance finding, not G2 proof.
- [ISSUE-CP14-030] severity: MAJOR | status: CLOSED-with-evidence
  A: D26-A requires native E04 state and prior same-cell history, shared by training/runtime; no substitute indicator or skipped GARCH calculation is authorized.
  B: the preliminary consumer reset and fully replayed E04 for every overlapping training window, repeatedly fitting the same history and discarding native chronological state at the 300-bar projection boundary.
  Rule applied: the additive feature timeline owns one actual VolatilityEngineV4 per cell, drains CLOSED inputs at E04's position in the pipeline, and retains native published states. No E04 engine/schema/parameters are edited. Only states already reached at the current candle enter the projection; preparation remains outside the plan latency budget.
  Interim behavior: native ingest_bar processes each reached candle once; the raw input/observation window stays bounded separately. E04 evidence uses the native converter and exact native VolatilityState. Full-prefix batch equality (including a rolling projection boundary), no-future-state and per-cell history regressions pass. Training-query protocol now records chronological E04 replay.
  Needs from owner: none; this is actual native-engine composition, not an ATR period/window policy change.
- [ISSUE-CP14-031] severity: MINOR | status: CLOSED-with-evidence
  A: all native E01 calculations and outputs must remain unchanged while G1/G2 reuse real engines.
  B: E01 run_pipeline repeatedly computes identical ATR(n,index) requests within the same immutable candle window (11,069 calls in the 300-bar profile).
  Rule applied: memoize the unchanged atr_sma result only inside one native run_pipeline invocation; no global or cross-window cache, changed summation order, formula or test-pin adjustment.
  Interim behavior: private tuple window/cache in e01_structure/engine.py; every original ATR evaluation is still performed on first request. Complete native pipeline canonical JSON equals an uncached reference. Existing E01/E03/E04/E11 tests remain green. This is an additional production-file change, explicitly recorded rather than hidden as wiring.
  Needs from owner: none; exact native-output parity is the acceptance guard.
- Post-streaming scratch-store probe (before E01 memoization) completed in **597.84 s** and correctly refused: **CRISIS 78, TRANSITION 0, EXPANSION 168, TREND_EXPANSION 16, TREND_CONTRACTION 7, TREND 9, COMPRESSION 184, CHOP 3, RANGE 117** (582 finalized samples). Exclusions: UPSTREAM_WARMUP 84, UNFINALIZED_TAIL 144, INVALID_E11_HISTORY 20. This is reproducible current code evidence of an **8/9 refusal**, not the earlier unverified 7/9 report, and not successful two-process G2 proof. Scratch data remains outside Git; no classifier artifact written.
- Context/E01/E03/E04/E11 verification: **339 passed, 0 failed, 39.01 s**. Immediate milestone commit/push follows the strengthened rolling-projection regression.


### OWNER ANSWERS to CP-14 issues (2026-09-18) — D30

D30 for the timed-out full-store training probe (owner decision, record verbatim under a new ISSUE-CP14 number): train-e11 trains on the E11 §1.2 base timeframes ONLY — 1h and 4h — over all ten Core-10 symbols (20 cells), never over all 14 timeframes; the trained W/b applies to every timeframe at runtime as the document's single classifier (§1.2 says other TFs are extensions by window parameter, not separate classifiers). Add flags --timeframes (default "1h,4h") and --symbols (default all ten) with the defaults recorded in the artifact's training_window block, print one progress line per cell (cell, closed bars, eligible samples, elapsed seconds), and add a hard --max-minutes (default 20) that aborts with a named status and no artifact rather than hanging. Doc: one sentence under the "Session-CP-14 (2026-09-17)" lead-in in E11 §3.3 after the D21 paragraph + one Appendix AJ row. Test: default scope is exactly the 20 base cells; the abort path writes nothing. The nine-class refusal stays unchanged.

- [ISSUE-CP14-032] severity: MAJOR | status: IMPLEMENTED, validation pending
  A: E11 §1.2 base timeframes and D30 authorize training only on 1h/4h over Core-10, with one classifier shared by every runtime timeframe.
  B: the preliminary CLI enumerated all stored timeframes and a full-store probe exceeded 30 minutes without a bounded stop.
  Rule applied: D30 exact scope/defaults and unchanged D21 nine-class refusal. --timeframes/--symbols accept only nonempty subsets of the base/Core-10 sets, in canonical order.
  Interim behavior: default exactly 20 cells including explicit empty-cell progress; effective and default scope recorded in training_window; one progress line per completed cell on stderr keeps --json stdout machine-readable. A spawn-isolated native-engine worker cannot write artifacts; the parent enforces the hard --max-minutes deadline (default 20), terminates/kills and reaps the worker, and reports TRAINING_TIME_LIMIT with artifact_written=false. Cooperative checks also protect direct trainer callers. Existing output files are never replaced on refusal. No new Ch.7 error code, synthetic class, or class-weight change.
  Needs from owner: none; D30 is binding. HTF dependency reads (including 15m) remain permitted feature inputs, not additional training cells.
- ISSUE-CP14-032 validation: context/E03/E11 battery **237 passed, 0 failed, 4.98 s**. Regressions verify exactly 20 default progress cells, selected subsets, invalid/nonbase scopes, the unchanged nine-class empty-store refusal, and process-enforced abort with absent/existing output preservation. The first run exposed a test-fixture flow-map limitation; the fixture now uses the same supported block mapping as the production writer, without changing the YAML parser. D30 schema/serialization round trips pass. The successful full-store two-process G2 proof is still pending and is not inferred from these tests. This green step is committed/pushed immediately.

### D23 documentation repair (owner instruction 2026-09-18)

Moved the unchanged D23 paragraph directly below the complete E11 §2 vocabulary table and removed the duplicate header/separator. No formula, OI policy or quality cap changed. An isolated external Markdown renderer (not a project/runtime dependency) verifies one HTML table, one header and one body, with the D23 paragraph after </table>; a repository regression verifies contiguous table rows and the single header. This is a separately committed/pushed documentation step, not G1/G2 completion.

### ISSUE-CP14-031 follow-up: prefix-view memoization

The first exact-output cache missed E01's repeated prefix slices in detect_retest_and_invalidation: a new 299-bar profile still showed 13,553 ATR calls and 4.05 s under E01. Prefix views now share only their own immutable run's cache, keyed by (period, absolute prefix boundary); non-prefix slices do not share it. Arithmetic, malformed-index/warmup failures, and full native pipeline outputs remain unchanged. Focused E01 plus exact pipeline/prefix regressions: **46 passed in 0.99 s**. This green optimization step is committed/pushed independently of unfinished G2 fixture work.

G2 work-in-progress evidence: the first 820-bar target-cell fixture hit its test-specific six-minute hard limit (440 eligible samples reported), wrote no artifact, and exited TRAINING_TIME_LIMIT. After the prefix-cache fix it completed in 320.65 s for BTCUSDT:1h (plus 0.87 s for BTCUSDT:4h), correctly refusing TRANSITION=0: CRISIS 87, EXPANSION 192, TREND_EXPANSION 24, TREND_CONTRACTION 54, TREND 9, COMPRESSION 199, CHOP 3, RANGE 134; total 702 finalized samples. Twenty progress lines were emitted; the remaining eighteen default cells were empty. This is NOT the required successful two-process determinism proof. A flat 120-bar tail was not enough to eliminate native structural confirmations from the retained engine history; fixture development continues without changing D21 or synthesizing labelled members.

### G2 actual-store two-process fixture proof

The fixture now supplies unlabelled deterministic OHLCV for BTC plus an ETH expanding-oscillation/mid-range quiet segment, with past-only M15/H4 dependencies. The native E01 confirmation and D21 projection produce every label; no feature/label monkeypatch, reweighting, prior artifact, or synthetic labelled member is used. Two separate CLI processes (each with its own isolated native-engine worker) read the same SQLite fixture and wrote byte-identical supported YAML. Both returned TRAINED with all nine classes nonempty and exactly twenty default cell-progress lines. This is fixture acceptance, not a prediction about the phone store.

Actual result: `{"artifact_sha256": "943f82550317cab603b6022f454571ad6de7751db0d03454964357a68a431d21", "per_class_counts": {"CHOP": 3, "COMPRESSION": 184, "CRISIS": 82, "EXPANSION": 169, "RANGE": 119, "TRANSITION": 54, "TREND": 10, "TREND_CONTRACTION": 7, "TREND_EXPANSION": 16}, "sample_count": 644, "training_window": {"default_symbols": ["BTCUSDT", "ETHUSDT", "SOLUSDT", "BNBUSDT", "XRPUSDT", "ADAUSDT", "DOGEUSDT", "AVAXUSDT", "LINKUSDT", "LTCUSDT"], "default_timeframes": ["1h", "4h"], "end": "2026-01-28T04:00:00.000Z", "start": "2026-01-03T23:00:00.000Z", "symbols": ["BTCUSDT", "ETHUSDT", "SOLUSDT", "BNBUSDT", "XRPUSDT", "ADAUSDT", "DOGEUSDT", "AVAXUSDT", "LINKUSDT", "LTCUSDT"], "timeframes": ["1h", "4h"]}}`.

Pytest result: 1 passed in 571.37s (0:09:31). Immediate green-step commit/push follows; G1 and final controls/twice-full-suite remain pending.

### Native engine assembly increment (G1 still incomplete)

- [ISSUE-CP14-033] severity: MAJOR | status: CLOSED-with-targeted-evidence | A: the producer's common engine view is close-stamped. B: E10's `candle_from_bar` adds an interval when `close_time` is omitted, so the generic wrapper emitted a future-dated E10 event and the real native-bundle test failed `BRIDGE_PIT_VIOLATION: E10`. Rule: PIT-safe native contract, no timestamp rewriting after computation. Interim/fix: the producer calls the unchanged E10 native driver with the actual open and explicitly derived close (including calendar closes), and uses its native catalog emitter. Needs from owner: none. Fresh full G2 and whole-suite verification remain outstanding after this wiring correction.
- [ISSUE-CP14-034] severity: MAJOR | status: CLOSED by D31 (historical pre-fix evidence follows) | A: E05's unchanged native emitter returns terminal expired zones with `resolution_class=QX`; the 24-field contract's own text says Q0..QX. B: frozen `EvidenceEvent.validate_24_fields` accepts only Q0..Q5, and public `SQLiteStore.insert_evidence` calls that validator. The real ETH fixture at 180 hours fails on an expired E05 zone. Rule: frozen catalog boundary + owner's no-clipping/no-skipping + COMPLETE payload persistence. Interim: named `EVIDENCE_CONTEXT_INVALID: E05 resolution_class=QX` before writing any events in the bundle. No expired event is discarded, no class is relabelled, and no frozen file is changed. Historical ISSUE-CP3-013 notes the same validator mismatch and says expired E05 zones stay out of emission; that omission is not silently revived under CP-14's stronger requirements. Needs from owner: explicitly authorize either a narrowly scoped frozen-validator QX correction, or an E05 terminal-state evidence mapping that preserves the underlying QX terminal state without giving it permission; otherwise this refusal remains and G1 cannot be marked complete.

`complete_engine_bundle` now assembles E01→E02→E12→E04→E03→E10→E09→E05→E06→E11→E07→E08 using native engines, joint mature E03/E04 evidence for E06, shared timeline E11 inputs/seed/history and the validated classifier hash. E07 consumes actual confirmation records rather than generic EvidenceEvents; its quality/alignment scalars are explicit required inputs, not silent 0.9/1.0 defaults. `prepare_engine_bundle` loads public SQLite history, adds raw observation lineage, and round-trips complete event envelopes through the public store.

Actual targeted command: `/home/user/cp14-tools/.venv/bin/python -m pytest tests/unit/test_engine_context.py::test_native_twelve_engine_bundle_persists_full_evidence tests/unit/test_engine_context.py::test_native_bundle_never_uses_e07_default_quality_or_alignment -q -x`

Raw result: `6 passed in 26.62s`. The first test exercises real native twelve-engine assembly/persistence at the 90-hour fixture boundary, then proves the 180-hour QX refusal leaves the existing evidence-row count unchanged. Five tests reject absent/invalid E07 scalars. E07 scalar values and classifier in this assembly test are explicit test fixtures; this is **not** the requested complete store-derived bridge acceptance test. The 38-context/23-risk projection, real plan validation, serve binding, outside-budget preparation/cache, handoff, two fresh whole-suite runs and PR remain unfinished. No phone acceptance is claimed.

### OWNER DECISION D31 — verbatim (2026-09-18)

OWNER DECISION D31 for ISSUE-CP14-034 — binding, written authorization under D24. Verified on main: contracts.py::EvidenceEvent.validate_24_fields has a self-contradiction — its docstring and error message say "Q0..QX" but the tuple is range(6); APEX_GEN5 §8 24-field mapping row 24 says "side-table, Q0..QX" and the E05 chapter defines QX_* as a legitimate terminal exception tag which E05 already collapses to "QX". This is a validator bug, not a design change. Authorized frozen-file exception, exact scope: in apex/data_catalog/contracts.py change ONLY the resolution_class membership check so the accepted set is exactly {"Q0","Q1","Q2","Q3","Q4","Q5","QX"}; no other line, no DDL, no to_ddl_row, no other frozen method or file. Required: (1) a failing-before/passing-after regression that persists a full terminal E05 EXPIRED event with resolution_class="QX" through public insert_evidence and reads it back intact; (2) a regression that "Q6", "QX_EXPIRED", "" and None are still rejected; (3) ISSUE-CP14-034 record (A/B/rule/interim/needs) + ADR-CP14 entry + DECISION_LOG D31 verbatim + one AJ row in APEX_GEN5 at the §8 mapping (one sentence: validator aligned to the documented Q0..QX set, no schema change); the existing frozen-file exception list in the checkpoint board must name this second exception explicitly next to insert_snapshot. Forbidden: dropping/relabelling expired evidence, mapping QX to Q5, widening to arbitrary strings. Commit + push immediately after green. Then continue: complete G1 (get_bridge_context on real 38+23 keys, serve wiring, persisted round-trip, engine-order test), HANDOFF_CP14, full suite TWICE with totals, then ONE PR "[CP-14] engine-context producer + E11 artifact + catch-up + paper account + staleness + CIRCUIT_OPEN". Report the PR URL only after `gh pr view` or the GitHub page shows it. D21–D31 binding.

- **ADR-CP14-006 (under ADR-P2-003):** D31 authorizes exactly one membership-check line in the frozen `EvidenceEvent.validate_24_fields`, admitting exactly Q0,Q1,Q2,Q3,Q4,Q5,QX. The docstring, error, DDL, to_ddl_row and every other frozen line remain unchanged by this increment. Public persistence retains the full native terminal E05 event including its original QX class; no omission, relabelling or numeric permission upgrade. This is the checkpoint's second frozen-file exception alongside the previously authorized insert_snapshot body, now explicitly listed on the board. §8 and one Appendix AJ row record alignment, not a schema change.
- **ISSUE-CP14-034 D31 disposition:** A: documented Q0..QX plus native terminal E05 EXPIRED/QX evidence; B: validator incorrectly admitted only Q0..Q5. Rule: binding D31 resolves the implementation contradiction within its exact one-line exception. Interim replaced: whole-bundle QX refusal is removed by fixing that validator; original full terminal evidence now goes through public insert_evidence unchanged. Needs from owner: none; D31 supplies the written authorization. Status: implementation complete, passing-after verification below closes this issue; G1 overall remains incomplete.
- **Failing-before evidence:** actual `/home/user/cp14-tools/.venv/bin/python -m pytest tests/unit/test_engine_context.py -k d31 -q` → `1 failed, 4 passed, 117 deselected in 0.79s`. Trace: `SQLiteStore.insert_evidence` line 580 → `EvidenceEvent.validate_24_fields` line 389 → `ValueError: resolution_class 'QX' not Q0..QX`. The test uses the unchanged native E05 emitter and an independently assembled full transport envelope, so this failure is specifically at the public store's validator. Raw log: `/home/user/cp14-d31-before.log` (tooling artifact, not committed).
- **Passing-after evidence / ISSUE-CP14-034 CLOSED-with-evidence:** actual `/home/user/cp14-tools/.venv/bin/python -m pytest tests/unit/test_engine_context.py tests/integration/test_store_integration.py -k 'not g2_actual_store_training_in_two_processes' -q` → `143 passed, 1 deselected in 30.98s`. This includes the exact failing-before test, four invalid-tag public-insert rejections, the 180-hour native bundle preserving terminal E05 QX, all complete-event round trips and store regressions. The explicitly deselected nine-minute G2 proof is not claimed as rerun here; both final full runs must include it. Raw log: `/home/user/cp14-d31-after.log`. Immediate commit/push follows this green step.

### G1 outside-budget preparation boundary (intermediate, not completion)

`PaperPlanBridge` accepts an explicit `context_preparer` alongside `context_source`; preparation never invokes the plan builder. `PaperRuntime.run_cycle` prepares due cells before starting scheduler cell clocks, excludes D22 catch-up failures, isolates named per-cell preparation failures, blocks stale cached source access, and retries a failed preparation at the same close next cycle. JSON reports checked/prepared/failure counts separately from catch-up. No fallback source, trade-budget admission or execution change is introduced. Serve producer composition and the full authoritative 38+23-key projection remain unfinished; this boundary does not claim to complete them.

Actual command: `/home/user/cp14-tools/.venv/bin/python -m pytest tests/integration/test_ops_paper_loop.py tests/unit/test_plan_bridge.py -q` → **26 passed, 13 warnings in 3.74s**. A real-bridge/runtime fixture advances its clock 60 seconds during preparation and still completes its scheduled stages; another two-cell test proves a failed BTC preparation never calls its source, ETH remains independent, and BTC retries successfully at the same close. The first isolation-test run correctly stopped at NO_MARKET_DATA because its raw bars were not seeded; the fixture now ingests actual bars for both cells instead of bypassing the ingest stage. Immediate green-step commit/push follows.

### Interim whole-suite verification after D31 and preparation hook

Actual `/home/user/cp14-tools/.venv/bin/python -m pytest -q`: **2858 passed, 14 warnings in 601.85s (0:10:01)**. No deselection: includes the successful actual-store G2 two-process determinism test after the E10 open/close correction, D31 full terminal-QX persistence, invalid-tag rejection, and the preparation boundary tests. This is an interim full run, not either required final closeout run: G1 source projection/serve composition and final handoff remain unfinished. Raw log: `/home/user/cp14-interim-full-d31.log`. Immediate green-step commit/push follows.

### G1 full E11-state bridge boundary

- [ISSUE-CP14-035] severity: MAJOR | status: CLOSED-with-targeted-evidence | A: P1 requires the complete native E11 regime_state object. B: the inherited bridge applied str(regime_state) when passing labels to conflict/setup/playbook/arbitration, so a native object became a dictionary representation instead of its confirmed state label. Rule: preserve the complete native payload and select its documented state field for label consumers; no aliases or entropy/probability rewrites. Interim/fix: one checked projection accepts the native mapping or the legacy label adapter and rejects missing/invalid confirmed labels; the source object remains unchanged. Needs from owner: none (contract wiring, not a model change).

Actual command: `/home/user/cp14-tools/.venv/bin/python -m pytest tests/unit/test_engine_context.py tests/unit/test_plan_bridge.py tests/integration/test_ops_paper_loop.py -k 'not g2_actual_store_training_in_two_processes' -q` → **149 passed, 1 deselected, 13 warnings in 31.44s**. Includes projection of the actual native fixture bundle, source preservation/malformed-state checks, and existing real-bridge/runtime regressions. The preceding interim full run was 2858 passed / 14 warnings / 601.85s before these two additional tests; no new full-run total is inferred. G1's complete store-derived 38+23-key assembly/serve binding is still unfinished. Immediate green-step commit/push follows.

### Owner ACK 2026-09-18 and G1 forecast projection audit

Owner verified D31 at 1a18dd2 and the branch through 1ee744a, directed continuation in G1 → serve → handoff → two clean final full suites → one verified PR order, and required stopping with an A/B/options ISSUE if a new owner decision is necessary. The accidental Markdown autolinks in the D31 entry have been removed as explicitly requested; their displayed text and all decision wording are unchanged. No new frozen-file change is made.

- [ISSUE-CP14-036] severity: MAJOR | status: RESOLVED-DESIGN by D32; implementation deferred pending batch audit below (historical pre-decision proposal follows)
  - **A:** Ch.13 §13.1 defines the mandatory twelve-member forecast vector including `vol_quantile`; P1 assigns assembly of `x` to the producer from authoritative engine/fabric outputs. `apex.forecast.logistic.logistic_bootstrap_p` rejects any omitted feature even when the PAPER bootstrap coefficients are zero.
  - **B:** E04's native `VolatilityState` exports `hv30` and `regime`, but no `vol_quantile` or percentile-rank field. Its `rolling_quantiles` produces **HV-valued thresholds at given percentiles**, not the current HV observation's percentile rank. Neither the sole `vol_quantile` occurrence in the blueprint nor D21–D31 specifies the inverse projection's estimator/tie law. E11's sigmoid-normalized volatility and E04's categorical regime are not the same quantity and cannot silently be relabelled. E04 also has a specific full-window warmup law; D26-A's capped-history ATR standardization does not authorize a different warmup for this separate forecast feature.
  - **Rule:** complete forecast vector + authoritative-source/PIT/no-guess requirements; no omitted feature, zero fill, silent bucket mapping, or alteration of the frozen forecast contract to exploit β=0.
  - **Interim:** no `vol_quantile` formula implemented; G1 completion, serve binding and final-closeout/PR claims remain paused. Existing missing-feature refusal remains unchanged. D31/ISSUE-034 and the existing D21–D31 decisions are not reopened.
  - **Needs from owner / options:**
    1. **Recommended explicit authorization:** define `vol_quantile(t) = count(h <= HV30_t for h in prior_window) / len(prior_window)` (right-continuous empirical CDF; ties count as `<=`). Use the actual native E04 HV30 history, excluding t and future data; use the unchanged native E04 regime window `W = max(30, round(regime_window_days*86400/tf_seconds))`, with its exact warmup condition (`len(full_prior_history) > W`, then use the last W values). Require finite current/history values and available native quantile context; otherwise return a named `VOL_QUANTILE_UNAVAILABLE` refusal. No capped-short-history fallback, no E04/E11 formula change, no new numeric parameter, and no implication that the currently short fixture or the phone history satisfies this window. This is a proposed **new forecast projection**, not an already-governed formula.
    2. **Alternative owner-specified projection:** provide the exact desired meaning (for example a normalized regime bucket instead of empirical rank), mapping/estimator, tie rule, lookback and warmup/degenerate-history handling. Until those are explicit, retain named unavailability rather than invent a value. No alternative has been implemented.

**Actual diagnostic** (native E04 driven from public SQLite ingest/window, using the existing unlabelled ETH fixture; no HTTP, engine replacement, injected indicator or runtime fallback):

```text
public_store_closed_bars: 180
native_E04_state_fields: ["as_of", "atr14_wilder", "atr50_wilder", "boll_width", "cluster_p", "contract_version", "ewma_vol", "garch_vol", "gk", "hv30", "ljung_q", "nondir_corr", "parkinson_adj", "q_tag", "range_z", "regime", "rs", "squeeze", "vol_ratio", "yz"]
native_E04_has_vol_quantile: False
forecast_requires_vol_quantile: True
native_E04_regime_window_bars: 4320
native_E04_prior_HV_count: 178
native_E04_thresholds: {'15': 'nan', '35': 'nan', '75': 'nan', '95': 'nan'}
forecast_missing_quantile_refusal: FORECAST_FEATURE_VECTOR_QX::missing vol_quantile
```

Raw diagnostic log: `/home/user/cp14-forecast-quantile-audit.log` (outside the repository). The final line is a contract-negative probe of the unchanged forecast validator, not a synthetic vector supplied to a runtime bridge. No G1 positive acceptance is claimed from this diagnostic.

Unchanged forecast battery verification: actual `/home/user/cp14-tools/.venv/bin/python -m pytest tests/unit/test_forecast_logistic.py -q` → `41 passed in 0.19s`. This verifies the existing contract, not a resolution of ISSUE-CP14-036. The only repository changes in this increment are control/documentation records and the owner-requested D31 autolink removal; no production/test code or parameter change. Immediate commit/push follows this green check.

### D32 receipt and exhaustive pre-implementation projection audit (2026-09-18)

**Status:** ISSUE-CP14-036 is RESOLVED-DESIGN by D32, NOT implemented. The earlier right-CDF/full-window proposal is superseded, not an alternative still awaiting approval. ADR-CP14-007, Ch.13/AJ changes, implementation and D32 tests remain pending the owner's explicitly required batch audit. Under bootstrap beta=0, p does not depend on vol_quantile's value; presence and validity still do.

**Owner D32, verbatim:**

OWNER DECISION D32 for ISSUE-CP14-036 — binding. Option 1 is accepted with three corrections. Definition: vol_quantile = (count(prior_HV30 < HV30_t) + 0.5·count(prior_HV30 == HV30_t)) / N (mid-rank; a constant history yields 0.5, consistent with the E11 Method-A degenerate rule). prior_HV30 = the finite HV30 values of the same cell strictly before t, taken from the native per-cell E04 state already owned by the producer (ISSUE-030), limited to the most recent E04 regime_window_bars (regime_window_days=180 converted with the cell's tf_seconds, exactly as E04 does), never including t or any later bar. Warmup: N ≥ 50 (E04's existing min_bars constant), NOT the full window — rationale: Toobit retention is about 3500 bars per interval, so a full 4320-bar requirement would make vol_quantile permanently UNAVAILABLE on 1h and slower timeframes and block every PAPER plan there; E04 itself does not refuse in this case, it falls back to its baseline regime. N < 50 or any nonfinite HV30_t → VOL_QUANTILE_UNAVAILABLE, fail closed for that cell, no substitute, no zero. Range [0,1], finite. Implement in engine_context only (no E04 engine or frozen-file change). Controls: ISSUE-CP14-036 record, ADR-CP14-007, DECISION_LOG D32 verbatim, one sentence in Ch.13 right after the bootstrap x vector defining vol_quantile as above, one AJ row. Tests: mid-rank tie handling; constant history → 0.5; 49 prior → UNAVAILABLE, 50 → value; window capped at regime_window_bars; PIT (current and future bars excluded); note in the ISSUE that under β=0 the bootstrap p is independent of this value, so this is a presence/validity requirement. BEFORE implementing D32: walk every remaining key of the 38 context + 23 risk tables against the native engine outputs and the plan, and report ALL still-undefined producer projections in ONE message as ISSUE-CP14-037… with A/B and options (or state explicitly that none remain). I will decide them together; no more one-at-a-time stops. Then implement D32 plus any further decisions, commit + push each green step, and continue the closeout order previously given (producer, serve wiring, HANDOFF, suite twice, ONE PR). D21–D32 binding.

#### Audit scope and classification

Read-only source/consumer audit against APEX_GEN5.md P1's two tables (lines 20474–20552 at c57b7d9), Ch.2/8–17, the native engine contracts, and every REQUIRED_CONTEXT_KEYS/REQUIRED_RISK_KEYS entry in apex/ops/plan_bridge.py. Also traced the twelve x members and downstream dependencies: E07 inputs, pattern admission, geometry, sizing, costs, normalized forecast quality, structured uncertainty, ledger marks/losses and circuit reset. This is **not** a passing real-source-to-plan test or an assertion that the real phone store contains the required measurements.

- **W** = authority exists; implementation/transport/validation remains. Missing authoritative data invokes the existing fail-closed rule, not a new policy question.
- **Dnnn** = unresolved projection described in ISSUE-CP14-nnn below. All such questions are reported together, not reserved for later serial stops.
- A W row depending on a D row is not ready for runtime simply because its own transformation is defined.

#### Context inventory — exactly 38 keys

| Context key | Native source → consumer; disposition |
|---|---|
| `events` | complete_engine_bundle → persist_complete_evidence/public SQLite insert/readback → _fabric_ref/EvidenceFabric; W, D31 QX preserved, no reduced rows or terminal-event deletion |
| `data_trust` | quality vectors → Ch.8 normalized Q_raw → build_context/resolve; D037 window-versus-current reduction |
| `q_raw` | calc_quality_vector/calc_window_quality → solvency, conflict and risk veto 1; D037 |
| `market_regime` | complete E11 regime_state.state → ContextFabric; W, same confirmed label as regime_state, not dict stringification |
| `mtf_state` | last-CLOSED required coarser native states → ContextFabric and setup gates 5/6; D038 |
| `utc_window_state` | E12 temporal_state.temporal_window → ContextFabric; W, canonical registry names only |
| `is_overlap` | E12 temporal_state.is_overlap → ContextFabric; W, no locally invented UTC windows |
| `volatility_state` | E04 VolatilityState.regime → ContextFabric; W |
| `structure_state` | E01 structure result → ContextFabric label; W, preserve native state rather than invent direction from candles |
| `regime_confidence` | max(E11 regime_state.probs) → Ch.8 combiner; W, D27 |
| `regime_uncertainty` | 1 - regime_confidence → Ch.8 combiner; W, D27, not entropy |
| `divergence_magnitude` | E10 normalized divergence EvidenceEvent.strength (native _to_evidence owns the normalization), retaining raw D_mag/pivots → Ch.8 bounded input; W; raw PIVOT D_mag can exceed 1 and must not be passed as already normalized |
| `temporal_window_validity` | E12 schema/quality/EvidenceEvent.validity → bounded Ch.8 input; D041 |
| `atr` | native E04 atr14_wilder → family ATR gate/playbook stops; W, D26-A, never ATR20 |
| `fvg_zones` | native E05 FVG object bounds/index/mitigation/fate → family fvg_gate and playbook; W, transport/index alignment only, terminal evidence still persisted |
| `bos` | native E01 BOS strength.S/direction/index → structure_gate; W; CHoCH lacking native S_struct cannot acquire a fabricated score |
| `regime_state` | entire native E11 mapping → validation plus checked .state projection at label consumers; W, ISSUE-035 |
| `e11_context` | shared PIT IC/history/classifier inputs → _validate_e11_context; W, D21/D23/D26/D30 |
| `direction` | admitted native PatternHit.direction → family/playbook; D040 when multiple hits compete |
| `pattern_id` | matching admitted PatternEntity/PatternHit → _pattern_entity/assert_scoring_admissible; D040 |
| `x` | twelve forecast features → logistic_bootstrap_p; D039 plus D32; log_cost_R also depends on D044 |
| `forecast_quality` | normalized native Q_forecast/prior forecast → gate10's categorical interpretation; D042 |
| `forecast_rr` | family sweep/entry + native E05 bounds/E04 ATR → build_stops/units_of_r → build_forecast; W, must be identical to downstream geometry, not fixture 3.0 |
| `forecast_cost_r` | actual commission/funding + slippage_model + geometry → forecast/EU/log_cost_R; D044 |
| `window_qualities` | per-observation measured quality and bar age → calc_window_quality/gate2; W for formula; missing historical provenance is a named refusal, not permission to use dataclass defaults |
| `temporal_quality` | E12 temporal_state.quality → gate8; W, no promotion of Q1 to Q2 |
| `volatility_quality` | E04 q_tag → gate9; W |
| `s_i` | admitted component evidence → family score/freshness/redundancy; D039 |
| `q_i` | native evidence quality, component reduction → family score; D039 |
| `package` | D28 versioned governed package → gate13/arbitration/plan; W; distinct from a calibrated forecast package |
| `p_min_tf` | D25 decision_runtime/SL-12 → eligibility/arbitration; W, all 14 TFs |
| `c_min` | D25 governed 0.50 → eligibility/arbitration; W |
| `freshness_ok` | D14 staleness <= governed SLA → eligibility; W, D22 catch-up failure remains separate |
| `risk` | the entire 23-key assembly below → adjudicate; W container, dependent on unresolved subprojections |
| `risk_state` | latest applicable durable apex_risk_ladder_state revision → forecast penalty/arbitration; W, same revision as risk.risk_state; absent complete revision is not a measured NoRisk |
| `h_norm` | E11 entropy / ln(9) → gate7; W, D27 |
| `family_status` | actual registry or D28 PAPER-only missing-registry ACCUMULATING → family/arbitration; W, no promotion |
| `arbitration` | D28 quality/alignment/recency weights 1/0/0, measured or explicitly excluded UNAVAILABLE inputs → arbitrate; W |

#### Risk inventory — exactly 23 keys

| Risk key | Native source → consumer; disposition |
|---|---|
| `capital` | D29 initial PAPER YAML capital + realized PAPER ledger P/L at PIT → sizing/caps; W, never PAPER capital in LIVE |
| `portfolio_exposure` | ledger net open quantities × actual marks × contract multipliers, gross across symbols → vetoes 3/5/ladder; D046 mark selection; positions-only exposure stays separate from D29 pending reservations |
| `proposed_notional` | read-only sizing projection + entry reference/multiplier → veto3; D045, including missing ATR_cap authority |
| `capital_hard_cap` | D25 0.60 × authoritative capital → veto3; W |
| `circuit_breaker_engaged` | durable emergency/circuit state, including aggregate-loss latches → veto4; W, never cleared by a new data bar |
| `emergency_state` | durable L1–L5/NORMAL revision → ratchet/veto4; W; boot's empty-table NORMAL check is not a complete persisted risk/budget record |
| `per_symbol_exposure` | same account mark projection scoped to symbol → veto5; D046 |
| `symbol_cap` | SL-12/research.governance symbol_exposure_cap 20% × capital → veto5; W, no duplicate risk YAML default |
| `portfolio_cap` | SL-12/research.governance portfolio_exposure_cap 60% × capital → veto5; W |
| `staleness_seconds` | max(0, raw receipt - calendar-derived candle close) → veto7; W, D14, raw availability untouched |
| `freshness_sla_seconds` | governed quality_weights freshness_threshold_seconds[tf] → veto7; W |
| `oi_lag_seconds` | measured OI observation availability versus as_of → veto8; W for available measurement; missing lag must refuse by name, never float(None), zero or invented infinity |
| `oi_lag_threshold_seconds` | quality_weights oi_lag_threshold_seconds[tf] → veto8; W, do not substitute quality-plane five-threshold band for the risk veto |
| `is_risk_increase` | new entry intent versus account/reservation projection → decision.monotone_ok and veto9; D045 specifies the pre-decision request boundary; existing SUBMIT_ORDER entry reservations are already risk-increasing under D29 |
| `uncertainty_is_rising` | prior/current E11 and fabric observations → decision anti-monotonicity/veto9; D047 |
| `realized_daily_loss_fraction` | PAPER ledger outcomes → veto10; D048 sign/netting/denominator, not the already fixed 3% threshold |
| `realized_weekly_loss_fraction` | PAPER ledger outcomes → veto11; D048; UTC-week reset/review obligation already exists |
| `consecutive_losses` | completed trade outcomes, not fills → veto12; D048 zero-P/L treatment and aggregation |
| `time_to_expiry_days` | dated exchangeInfo expiry minus as_of; explicitly PERPETUAL is not applicable → veto13; W, typed applicability adapter required, no numeric sentinel or guess for missing dated expiry |
| `margin_health_fraction` | D29 durable PAPER reservation proxy → veto14/L3; W formula, D046 mark choice; preserve strict PAPER thresholds and LIVE isolation |
| `min_quantity` | universe quantity_step and actual exchangeInfo quantization precedence → size; W, do not confuse quantity step with min-notional |
| `contract_multiplier` | actual venue contract spec → size/notional/account; W, missing spec refuses |
| `risk_state` | same durable revision as context.risk_state → adjudicate floor/ladder; W, no new independent ladder |

#### Nested forecast feature inventory — exactly 12 members

| Feature | Projection status |
|---|---|
| `s_struct` | D039: precise component/quality scaling |
| `s_liq` | D039 |
| `s_vol` | D039: explicitly choose volume, not silently volatility |
| `s_fvg` | D039 |
| `s_ob` | D039 |
| `trend_stack` | D039: E09 native stack is a mapping and native bias is a scalar; choose the documented scalar binding explicitly |
| `momentum_z` | W: native E10 momentum.momentum_z; nonfinite/None refuses, not zero |
| `regime_entropy` | W: native raw E11 entropy; h_norm is the separately named normalized input (D27) |
| `vol_quantile` | D32 mid-rank, prior same-cell native E04 history, capped native window, N>=50; implementation deferred |
| `temporal_core_flag` | W: numeric boolean from E12 is_utc_activity_window/is_core_window, not overlap or merely any UTC-window membership |
| `log_rr` | W: natural log of actual geometry RR, positive-domain validation |
| `log_cost_R` | W logarithm/domain validation, D044 cost source; preserve governed unavailable-spread floor, never log an invented zero |

#### Consolidated unresolved projection batch — ISSUE-CP14-037 through ISSUE-CP14-048

All twelve are **OPEN—owner projection decision required**, severity MAJOR. In each entry A/B are the conflicting authority/implementation facts; numbered choices are proposals, **not decisions**. Shared rule/interim: no fabricated defaults, clipping, weakened gates, extra pattern family, frozen changes or runtime fixtures; retain named unavailability until authorized. No proposal below reopens D21–D32. Where a choice explicitly leaves an input unavailable, it does not promise a tradable startup or count an always-refusing stub as completed G1.

**[ISSUE-CP14-037] Which window reduction supplies q_raw/data_trust?**
- A: Ch.8 says data_trust is normalized Q_raw; P1 says derive trust over window quality vectors. Ch.2 defines both per-candle Q_raw and minimum-veto/exponentially weighted Q_window.
- B: calc_quality_vector and calc_window_quality expose those distinct quantities, but no producer selects which scalar becomes the context/risk q_raw. Choosing latest, minimum or weighted mean changes solvency/conflict/veto1. This is separate from the implementation gap that raw ingest stores NULL quality metadata and get_window reconstructs healthy-looking defaults.
- Options: **1 (recommended):** use the native required-window minimum-veto plus exponential mean as context q_raw, with data_trust equal to that already-[0,1] value; keep every measured (Q_i, age_i) for gate2. A failed window never gets an average substituted. **2:** use latest closed candle's native Q_raw for both context q_raw/data_trust, retaining the independent full-window gate2. Owner may specify another exact reduction instead.
- Interim/needs: approve 1 or 2; absent measured quality remains unavailable in either. No approval to reconstruct source health/completeness/replay results from dataclass defaults or content_hash. Tests: bad historical candle versus healthy current, weighted mean versus current, unknown provenance and D14 freshness.

**[ISSUE-CP14-038] Directional MTF state construction.**
- A: Ch.10 fixes relative timing/intermediate/HTF selection, last-CLOSED/PIT requirements and top-TF vacuous pass; Ch.8 fixes the scalar mapping ALIGNED=1, PARTIALLY_ALIGNED=.6, STALE=.3, conflicting/unavailable/insufficient=0.
- B: quality.pit.calc_snapshot_pit_window classifies sufficient bar counts as ALIGNED; it does not compare directions. E09 supplies per-cell trend bias but there is no cross-cell directional classifier defining neutral versus opposing versus partial agreement. Copying the count label would call opposing markets aligned. E07's required mtf_align also needs the resulting authoritative state, not a fixture .5/1.
- Options: **1 (recommended):** for the base cell and Ch.10's required coarser cells, use native E09 bias with existing |bias|<.05 neutral law. Both positive and negative directions present => CONFLICTING; all are nonzero and the same => ALIGNED; otherwise (neutral, none opposed) PARTIALLY_ALIGNED. This comparison is pattern-independent, so E07 never depends on a later E08/pattern decision. Missing required closed state => INSUFFICIENT/refuse, stale => STALE/refuse; no required coarser TF => documented vacuous ALIGNED. E07 consumes the existing Ch.8 scalar mapping. **2:** require an authoritative persisted cross-TF MTF record; do not synthesize a classifier, and refuse where it is absent. This postpones actual emission on such cells, not an assumed pass.
- Tests: both sides, neutral, opposition, missing/stale, 1mo vacuity, and future/open HTF exclusion.

**[ISSUE-CP14-039] Component s_i/q_i aggregation, six x projections and E07 average quality.**
- A: AC.4 defines binary pattern presence s_i and governed q_i; Ch.10 weights twelve engine components; P1 requires complete score maps and x assembled from them. Native events provide individual quality/strength, often multiple events per engine. E09 exports both stack mapping and bias scalar.
- B: no function defines component-level evidence selection/quality reduction, whether the five s_* forecast features include q_i, or the scalar meaning of trend_stack. The family has 1.0/.9 convenience defaults; E07 requires avg_quality. These defaults are not measured runtime authority.
- Options: **1 (proposed explicit conservative adapter):** from SL-14-admitted same-cell evidence, each component is present iff it has setup-direction or neutral context support; s_i is that binary presence. Opposite evidence is retained for conflict, never dropped from events/fabric. q_i is the minimum native quality of all admitted evidence for that component, including opposing evidence. Known absent optional components are omitted from both score maps (a native zero contribution); their binary presence feature is 0. Missing required evidence refuses, and a missing quality measurement never becomes 0 or .9. Feed x.s_struct/s_liq/s_vol/s_fvg/s_ob from these unweighted binary structure/liquidity/**volume**/fvg/orderblock scores; quality remains in the setup score, not silently multiplied into x. x.trend_stack is native E09 bias. E07 avg_quality is the arithmetic mean of native qualities of its actual upstream confirmation contributors (E01/E02/E03/E05), not E07/E08 itself; missing contributor quality refuses. **2:** owner-specified versioned component selection/quality model and explicit six-feature bindings, with no use of the convenience defaults while undefined. This is not a request to retune the twelve weights or E07 coefficients.
- Tests: multiple event qualities, opposition retained, absent versus unavailable, exact volume/volatility distinction, no double quality weighting, E07 contributor provenance/no dependency cycle, native E09 scalar equality.

**[ISSUE-CP14-040] One pattern/direction from multiple admitted hits.**
- A: P1 requires a detected catalogue pattern and its direction; the bridge accepts one pattern per cell, and only SF_FVG_SWEEP_REV is Wave-In. Pattern admission/lifecycle is separately governed; D28 ACCUMULATING is a family status, not pattern promotion.
- B: pattern.detect.detect_all can return multiple opposing hits, is_invalidated is separate, and E08-backed Spring/Upthrust use separate APIs. Neither native detector enumeration order nor the bridge defines a winner. Registering PAT-LIQ-003 automatically, choosing E09 direction instead, or blindly promoting catalogue examples would invent authority.
- Options: **1 (recommended conservative):** consider only independently admitted, native-confirmed, not-subsequently-invalidated hits within their valid PIT context; include E08-backed hits only with their native confirmation and lawful registry row. If none, refuse; if more than one distinct hit remains, report PATTERN_SELECTION_AMBIGUOUS/refuse instead of choosing. **2:** select latest confirmation among those same admitted valid hits; break equal-time ties by highest native hit strength and then lexical pattern_id. This is an explicit new ranking policy, not an existing native rule. Neither option admits a new pattern/family or relaxes AC.1/AC.2.
- Tests: no hit, unique hit, opposing/multiple hits, deterministic ties (option2), invalidation after original hit, research-only exclusion, E08 source preservation.

**[ISSUE-CP14-041] E12 categorical validity to numeric temporal_window_validity.**
- A: Ch.8 requires a bounded numeric validity input. E12 publishes schema validation, quality tags and EvidenceEvent.validity (VALID/DEGRADED); UTC core/overlap are separate fields.
- B: no numeric DEGRADED mapping is specified. Schema-valid low-quality state, native VALID event and being in a core window are different conditions. The current combiner weight is zero, but as with D32 presence/validity remains a contract obligation.
- Options: **1 (recommended):** project the native event validity as an indicator: VALID=>1, explicitly DEGRADED/INVALID=>0, after native schema/PIT checks; missing/unknown state refuses. This is a categorical indicator, not replacing missing data by zero. **2:** schema/PIT-valid canonical temporal state=>1 regardless of quality; quality degradation remains exclusively in temporal_quality/gate8, malformed/missing state refuses. Do not confuse either with temporal_core_flag.
- Tests: schema-valid degraded state distinguishes choices; valid state, missing state, invalid schema and future state.

**[ISSUE-CP14-042] Normalized forecast quality versus gate10 Q-class.**
- A: Ch.13/native forecast defines bootstrap Q_forecast=.5; gate12 accepts the governed normalized threshold .5. P1 separately requires forecast_quality for gates10/12.
- B: gate10_forecast_quality interprets numeric quality using int(value) against gate_quality_min_class=2. Passing native .5 becomes class0, while passing fixture Q3 manufactures an unproduced categorical quality. ForecastRecord has q_forecast but no native categorical tier. Prior-cycle reuse also needs the matching forecast identity/package/PIT, not any old prediction.
- Options: **1 (recommended):** explicitly authorize gate10's normalized-quality transport for native ForecastRecord/bootstrap records, using the existing governed Q_forecast minimum, retaining categorical handling for genuinely categorical inputs and the bootstrap LIVE ban. Pass actual bootstrap_prior metadata; gate12 remains separate. No fabricated Q2/Q3 conversion. **2:** retain categorical gate10, require an independently governed forecast-quality class record (matching event/package/PIT); missing tier is FORECAST_QUALITY_UNAVAILABLE even when normalized Q_forecast=.5. No class record is currently produced by the bootstrap.
- Tests: exact .5/.49 boundaries, bootstrap PAPER/LIVE, categorical legacy inputs unchanged, prior-record scope/version/PIT mismatch. This is a non-frozen gate/bridge boundary change under option1, not an E11 or Ch.8 confidence-combiner change.

**[ISSUE-CP14-043] Forecast structured uncertainty bootstrap/projection authority.**
- A: Ch.13 separates P/U/C and names disagreement, sampling, calibration, data_quality, regime_shift and tail_risk; its weighted U formula requires actual component values. D27 only resolves E11 regime uncertainty for Ch.8, not all six forecast components.
- B: build_forecast accepts six supplied scalars but defaults all absent values to zero; an omitted producer forecast_uncertainty therefore claims U=0/C=1. Native E11 entropy/turbulence/probabilities and calibration diagnostics do not define the six scalar projection laws (especially sampling/calibration/tail risk). A named component is not an estimator.
- Options: **1 (recommended safety boundary, with explicit startup consequence):** consume only an independently versioned/PIT-valid complete forecast uncertainty record with its event/package/measurement provenance; absent component => FORECAST_UNCERTAINTY_UNAVAILABLE, never the zero defaults. This does not authorize a cold-start uncertainty model and may prevent bootstrap plans until that source exists. **2:** owner defines a PAPER-only bootstrap uncertainty model now: exact six values/formulas, source windows/minimum observations, missing-data treatment and provenance labeling. Any conservative bound must be labeled as a bound, not a measurement; no automatic use in LIVE. An unspecified 'use E11 uncertainty' does not resolve six different quantities.
- Independent implementation defect (no owner policy choice): build_forecast currently computes C=1-max(component U), while Ch.13 specifies C=clip(1-U,0,1); the pure composite path already uses 1-U. Correcting that downstream forecast C to the explicit Ch.13 formula is wiring/contract repair; **do not change the Ch.8 combiner**. Tests must distinguish unequal components and forbid invented zero components.

**[ISSUE-CP14-044] Cost-model units, ADV window and funding horizon.**
- A: Ch.13/16/20 define round-trip fees + alpha_spread*abs(order_size/ADV) + expected holding-period funding, converted to R; D28 supplies actual public rate/spec facts, never guessed fees. A missing spread invokes cost_R>=.05, not invented fee/funding zero.
- B: slippage_model consumes precomputed ADV. No native producer defines ADV lookback/units or how a present funding rate becomes expected holding cost; the public rate helper exposes neither a full funding schedule nor historical average holding duration. Using the rate once, assuming an 8h interval, treating a percentage as R, or setting total cost to .05 are not defined by the cited formulas.
- Options: **1 (proposed fully disclosed PAPER estimator):** ADV is mean daily base-asset volume over the previous 30 complete UTC days, assembled from contiguous CLOSED 1h bars (exclude current incomplete day); require verified volume units and convert order quantity using actual contract multiplier to the same base units. Use the existing alpha_spread formula. Fee fraction is twice the actual taker fee. Funding charge is the nonnegative side-specific current rate times the number of verified venue funding settlements in the calendar-derived maximum holding interval allowed by the instantiated playbook and D25 H_max; no assumed schedule. Convert the summed price fractions by entry/actual stop distance to R; preserve the governed unavailable-spread floor. Rate persistence and using maximum holding duration rather than historical average are explicit estimator assumptions requiring authorization. Missing fee/rate/schedule/units refuses. **2:** require authoritative precomputed ADV and holding-period funding-cost records from a governed cost model, scoped/PIT/unit-checked; until present report COST_MODEL_UNAVAILABLE. Owner may supply a different exact estimator instead of 1. Neither option silently upgrades the advisory Economic Gate to a hard veto; the existing mandatory forecast cost remains required.
- Tests: quantity/multiplier units, 30 full days and missing-day refusal, no future/incomplete-day volume, positive/negative side funding, zero versus unknown settlements, R conversion, floor and no default fee.

**[ISSUE-CP14-045] Pre-veto candidate sizing, undefined ATR_cap and notional units.**
- A: P1 requires sized_quantity*reference_price before the risk vetoes; Ch.15 sizing also requires k_attn*ATR_cap*Capital/ContractMultiplier. D29 consistently includes multiplier in actual notional and labels entry orders risk-increasing.
- B: neither E04 nor governance supplies ATR_cap (it appears as an unbound input to risk.size); kernel silently omits the attention bound when None. ATR14 itself and its reciprocal have different units. Computing notional from final adjudication is circular; ignoring multiplier differs from the sizing/account units when multiplier!=1.
- Options: **1 (proposed explicit adapter):** ATR_cap=1/native E04 atr14_wilder, with positive finite ATR required, retaining existing k_attn and all sizing bounds. Compute a read-only candidate quantity with the same native sizing routine and conservative declared/derived ladder floor, then proposed_notional=quantity*entry*contract_multiplier. This quantity is a request bound, **not** permission; all vetoes still precede final sizing/adjudication. NEW_ENTRY proposals are is_risk_increase=true as in D29; reduce-only/manage actions remain outside this producer. Add a final quantity/notional consistency assertion. **2:** require an owner-governed explicit ATR_cap (units base-asset/USDT) or exact alternative native projection before sizing; retain named SIZE_INPUT_UNAVAILABLE until supplied. Owner must also confirm multiplier-aware notional and the read-only proposal boundary or specify another unit-consistent request law. No silent omission of the attention cap.
- Tests: ATR zero/unknown, multiplier!=1, higher derived ladder than declared, min-step rounding, attention-bound activation, veto before final authorization, no preview/final notional mismatch.

**[ISSUE-CP14-046] A single authoritative PAPER mark selection law.**
- A: D29 requires gross marked open notional and fails on missing marks; P1 uses those marks for portfolio/per-symbol exposure. paper_account_state intentionally takes marks as inputs.
- B: the store has fourteen last-CLOSED prices per symbol; account_state does not select a timeframe and does not define selection among them. Using the candidate cell's close makes one account have different exposure/margin health for simultaneous different-TF plans. Actual exchange mark price and last-trade/candle close are also not the same measure.
- Options: **1 (recommended PAPER closed-data convention):** latest CLOSED 1m close for each held symbol, available at the common PIT boundary and passing D14/SLA; share one account mark set across cells at that boundary. No fallback to entry price, another TF or a future close; absent/stale required mark refuses. State explicitly that this is a PAPER close-price mark, not exchange liquidation mark. **2:** actual public venue mark-price observations with explicit receipt/availability, governed freshness and retained source payload; no candle-close fallback. This requires an additive public observation source, not signing or frozen client edits.
- Tests: identical portfolio health across candidate TFs, future mark exclusion, stale/missing held-symbol mark, flat account requires no fictional mark, multiplier and D29 thresholds unchanged.

**[ISSUE-CP14-047] Rising uncertainty/conflict comparator.**
- A: P1 requires fabric conflict state plus E11 entropy trend for uncertainty_is_rising; decision anti-monotonicity and veto9 consume a boolean. D27's 1-p_max is not the same statistic as raw entropy.
- B: native E11 publishes entropy but no 'rising' predicate; neither lookback, equality nor combination with conflict severity is defined. The bridge's optional conflict uncertainty also defaults to zero unless explicitly bound. Choosing one-step entropy, rolling slope, 1-p_max, or just HARD_CONFLICT changes which requests are blocked.
- Options: **1 (recommended minimal trend law):** compare current and immediately preceding CLOSED same-cell observations with matching parameter/classifier versions. rising iff H_t>H_previous OR the native conflict restriction rank increases (CONSENSUS=0, MATERIAL_CONFLICT=1, INSUFFICIENT_EVIDENCE=2, HARD_CONFLICT=3, from conflict._OUTPUT_RESTRICTION), strict comparisons; equality is not rising. Supply the conflict resolver's uncertainty scalar from the already governed E11 1-p_max (D27), never missing=>0. Missing/incompatible prior comparison => UNCERTAINTY_TREND_UNAVAILABLE/refuse risk-increasing plans. **2:** owner supplies a different exact trend window/statistic/conflict ordering and cold-start rule; no implicit smoothing/tolerance or reuse of another cell's history.
- Tests: entropy versus p_max moving in opposite ways, rising conflict with falling entropy, ties, first comparison, changed artifact/package, future/cross-cell exclusion; hard conflict still independently vetoes.

**[ISSUE-CP14-048] Realized-loss sign/netting, capital denominator and outcome streak.**
- A: P1 says realized daily/weekly P/L divided by capital; risk vetoes compare a positive loss fraction with .03/.06, and streak>4. D29 defines current capital; Ch.15 fixes next UTC day/week and OWNER-review reset requirements.
- B: paper_account_state returns signed OUTCOME P/L/timestamps, not loss aggregates. Direct signed negative P/L would not trip a positive threshold. Net loss versus sum of losing trades, period-opening versus current capital, and whether zero-P/L trades break a streak are not selected by a native producer. Counting fills instead of completed trade outcomes is incorrect. This is not permission to change the fixed thresholds or resets.
- Options: **1 (recommended):** daily/weekly loss=max(0,-sum(net realized PAPER completed-trade P/L in period))/current D29 capital; UTC day and ISO Monday-start UTC week; a trade with P/L>=0 breaks the consecutive-negative-outcome streak. Evaluate only canonical completed outcomes at/before as_of, not partial fills, with ledger-order tie breaking and correction-chain integrity. The max(0,...) defines positive loss, not clipping an invalid measurement. **2:** gross realized losing-trade sums divided by period-opening capital, zero-P/L outcomes ignored in streak; same PIT/environment/integrity requirements. This is a different financial meaning and needs explicit owner authorization; specify another complete rule if neither is intended.
- Shared mandatory implementation in either choice: persist/reconstruct a threshold-crossing latch so later profits/new bars cannot auto-clear CIRCUIT_OPEN. Daily reset only at UTC rollover; weekly also needs OWNER review; consecutive-loss reset needs OWNER review. An empty verified PAPER outcome history genuinely has zero losses/streak; missing/unreadable/ambiguous ledger does not. Tests: -300/+300 offsets, losses lowering capital, day/week boundaries, zero-P/L streaks, partial fills, duplicates/corrections, recovered P/L after a breach and authorized resets.

#### Already-governed gaps — not additional owner questions

1. Persist/recover measured quality and validation provenance through an additive, identity-bound seam before trusting it; raw quality_vector=NULL and default health=1/completeness=100/delay=0 are not measurements. Existing missing-provenance failure is lawful, even if initial historical windows consequently cannot pass. D14 still applies to receipt-derived freshness; no historical backfill waiver is proposed.
2. Build the real get_bridge_context/source cache, validate every required key, persist/read the complete events and exact engine order, preserve lifecycle/lineage/PIT, then bind source/preparer in PAPER serve outside scheduler budgets. Existing helper tests are not this acceptance.
3. Consume E01 native BOS strength.S (not CHoCH break magnitude as S_struct); map E02 five-prerequisite sweep facts, E05 bounds/creation indices/mitigation/fates, E06 stateful EWMA and E04 ATR14 directly. No re-detector, fake optional evidence or dropped terminal QX.
4. Reuse native family sweep/reclaim and playbook stop/target geometry for pre-forecast and final plan; align FVG indices to the same raw window and apply native lifecycle admission to the decision view, not to persistence. An absent/invalid setup produces a named no-setup result, not dummy RR/stop/quantity.
5. Native E10's normalized divergence event strength is already an exported authority; do not add producer clipping of raw pivot D_mag. Explicit native no-divergence differs from missing E10 state.
6. Query durable ladder revisions at the applicable PIT boundary, preserve the ratchet and use the same revision in both risk_state fields. The startup check's empty-table NORMAL message does not supply a complete measured risk-state/budget revision. Missing complete source => named refusal; no bootstrap ladder authorization requested or inferred.
7. OI lag unavailable => named risk-input refusal, not float(None), zero or infinity. This does not exclude an E11 training sample or change D23's participation/Q5 rule. Available/stale measured lag still follows the existing veto8 threshold.
8. Known PERPETUAL with no expiry is typed 'not applicable' for the existing quarterly-contract veto13; propagate contract type and evaluate that applicability explicitly. Unknown type/missing dated expiry refuses. No infinity/large-day sentinel and no guessed exemption; this corrects float(None) transport, not the veto definition.
9. D29 account reads require as_of filtering and PAPER identity separation. Pending notional is a margin reservation, not a replacement for position-only exposure caps. Governance already supplies 20%/60% caps and D25 supplies the separate hard-cap fraction.
10. Optional consumer defaults must not fabricate authority: propagate the forecast bootstrap flag, separate D28 parameter package from statistical calibration, bind measured conflict uncertainty/redundancy/component history, and honor existing insufficient-history redundancy-skip law (fewer than 20 OK points). Ch.13's explicit C=1-U repair is described in ISSUE-043; the Ch.8 confidence combiner is untouched.
11. CIRCUIT_OPEN latching/reset semantics, calendar-aware close law, public venue facts, family registry default, arbitration exclusions, quantity-filter precedence and LIVE isolation already have authority. Missing inputs stay named refusals; none is a request to relax a gate or to implement CP-15 simulator execution.

**Audit outcome:** the complete decision batch is ISSUE-CP14-037–048 above. No additional undefined projection was identified in the 38+23 keys and their traced consumers/dependencies beyond this batch and the already resolved D32 projection. This is a source-audit conclusion, not a guarantee that later tests cannot reveal implementation defects. All runtime implementation, including D32, remains deferred pending the batch response. Existing frozen exceptions remain exactly insert_snapshot's body and D31's single membership-check line.

**Verification of this documentation increment:** static AST/schema coverage checks and git diff --check only; no production/test/parameter/blueprint change, no new runtime or full-suite result. The external test virtualenv is absent in this restored environment, so the earlier 41-test and 2858-test results are historical, not rerun. Immediate documentation commit/plain push follows the green static audit. OWNER-CHECKED:[ ].

### OWNER ANSWERS D33/D34 (2026-09-18, verbatim)

OWNER DECISION D33 (part 1 of 2) — binding, resolves ISSUE-CP14-037..042. 037: Option 1 — native minimum-veto plus exponentially weighted window quality feeds both q_raw and data_trust; full-window gate 2 stays separate. 038: Option 1 — compare native E09 directions across base + required coarser cells: opposing signs CONFLICTING; all nonzero and equal ALIGNED; neutral without opposition PARTIALLY_ALIGNED; missing/stale refuse; top-TF vacuity preserved; E07 uses the existing state→score map. 039: Option 1 as proposed — binary presence from admitted supporting evidence; component quality = min native quality of admitted evidence; known-absent optional components contribute nothing, unknown measurements refuse; s_struct/s_liq/s_vol/s_fvg/s_ob = unweighted presence of E01/E02/E03(volume)/E05/E06; trend_stack = native E09 bias; E07 avg quality = mean of its actual confirmation contributors; no 1.0/0.9 defaults. 040: hybrid — admitted, confirmed, still-valid hits only; if any two have opposing directions → PATTERN_SELECTION_AMBIGUOUS (no plan); if all share one direction → deterministic rank: latest confirmation, then native strength, then lexical pattern ID; research-only patterns never promoted. 041: Option 1 — VALID→1, DEGRADED/INVALID→0, missing/unknown refuses; not temporal_core_flag. 042: Option 1 — gate 10 consumes the normalized native Q_forecast with the existing Ch.7 rule "Q_forecast < 0.5 → BLOCK" (strict less-than, so bootstrap 0.5 passes); genuine categorical inputs keep categorical handling; bootstrap LIVE ban (Setup Gates 10/12 fail for LIVE) unchanged; non-frozen gate/bridge correction with a failing-before test. Part 2 follows immediately.

OWNER DECISION D34 (part 2 of 2) — binding, resolves ISSUE-CP14-043..048 and releases D32. 043: Option 2 — PAPER bootstrap uncertainty model, versioned "cp14_paper_bootstrap_uncertainty-v1": U_cal = 1 − Q_forecast = 0.5 (constant-0.5 model, uncalibrated); U_ood = 0.5 (no reference distribution → maximal ignorance, never 0); U_dis = D27 regime_uncertainty = 1 − p_max from the same E11 snapshot; U = clip(0.5·U_cal + 0.3·U_ood + 0.2·U_dis, 0, 1) and C = 1 − U exactly per Ch.13 (fix the max-component mismatch with a failing-before test); any other named uncertainty field is typed UNAVAILABLE and not consumed; no component may default to 0; the record carries model version + E11 snapshot_id; LIVE ignores this model. 044: Option 1 — ADV = base-asset volume of CLOSED 1h bars over the previous 30 complete UTC days (same units as order_size); round-trip actual fee; funding = side-specific nonnegative charges × number of settlements inside the governed max holding interval, rate and interval read from public Toobit (D16/D28), never an assumed 8h; public endpoint failure → FUNDING_UNAVAILABLE named refusal for that cell, retry next cycle; spread UNAVAILABLE keeps cost_R ≥ 0.05. 045: Option 1 — ATR_cap = 1 / E04 atr14_wilder (price units, same source as D26-A), ATR ≤ EPS → refuse; native sizing as read-only request bound; notional = quantity × entry × contract_multiplier; new entries are risk-increasing; all vetoes precede authorization; never None. 046: Option 1 — one shared PIT-valid last-CLOSED 1m close per held symbol within the freshness SLA, labelled PAPER_CLOSE_MARK (not an exchange mark); stale/missing → mark UNAVAILABLE → D29 fail closed. 047: Option 1 — consecutive CLOSED same-cell states under matching versions; rising iff raw entropy increases OR conflict restriction rank increases; equality not rising; missing prior state refuses risk increases only; conflict uncertainty bound to D27. 048: Option 1 — max(0, −net period realised P/L)/current D29 capital, UTC day / ISO UTC week, nonnegative completed trades break the streak; thresholds, PAPER/PIT filtering and latches unchanged. Governance for all: ISSUE record closed with evidence, ADR-CP14 entry each, DECISION_LOG D33/D34 verbatim, one-sentence doc insertion in the owning section + one AJ row each, tests per rule (including the failing-before tests named above). Now implement D32 + D33 + D34, commit + push after each green step. From here on do NOT stop for new gaps unless a frozen file must change or a rule contradicts D1–D34: apply the most conservative named refusal, record the ISSUE, continue, and list all such items in the PR body. Then producer, serve wiring, HANDOFF_CP14, suite twice, ONE PR. D21–D34 binding.

All ISSUE-037–048 audit options above are now superseded by these written decisions. Status: RESOLVED-DESIGN; individual implementation closures require their evidence below. No bare selection is authority. Implementation may continue with recorded conservative refusals for further gaps; only frozen-file changes or contradictions with D1–D34 require an owner stop.

### ADR-CP14-007 — D32 native forecast HV mid-rank

Decision: forecast_vol_quantile consumes the producer-owned same-cell native E04 states; excludes current/future states, caps the native prior-bar window, filters finite HV, requires native min_bars=50 and uses mid-rank ties. No E04 or frozen-file change. Current nonfinite/short history => VOL_QUANTILE_UNAVAILABLE. The bootstrap beta=0 feature is a presence/validity obligation, not a probability adjustment.

ISSUE-CP14-036 CLOSED-with-projection-evidence: actual `python -m pytest tests/unit/test_engine_context.py -k d32 -q` => **6 passed, 122 deselected in 0.52s**. Tests cover ties/constant history, rank endpoints, 49/50, nonfinite history/current, native 180/4320 window differences and strict PIT. Runtime source integration remains pending, not G1 completion. D33/D34 are received verbatim; this green increment implements D32 only.

Broader D32 verification: context + forecast batteries excluding only long G2 => **168 passed, 1 deselected in 30.57s**; no final full-suite claim.

### ADR-CP14-013 — D33 normalized gate-10 quality

ISSUE-CP14-042 CLOSED-with-evidence: native normalized numeric quality in [0,1] uses the existing governed Q_forecast floor, with exact 0.5 passing; genuine categorical tags/legacy categorical numerics retain their class handling. NaN/Inf refuse. Bridge transports the actual forecast bootstrap flag; LIVE remains banned for bootstrap. No frozen file changed.

### ADR-CP14-014 — D34 PAPER uncertainty and forecast confidence

ISSUE-CP14-043 CLOSED-with-projection-evidence: engine_context.paper_bootstrap_uncertainty validates the actual nine same-snapshot E11 probabilities and produces model/version provenance, calibration=.5, ood=.5, disagreement=1-p_max; all other named fields are typed UNAVAILABLE. build_forecast consumes exactly those three, rejects missing/invalid/defaulted components, and uses C=1-U. LIVE cannot consume this PAPER model. Legacy explicitly measured inputs retain their declared representation, but omissions no longer imply zeros. The erroneous prior C=1-max assertion was corrected under D34; existing tests now supply explicit fixture observations, not runtime fallback measurements. Ch.8 combiner unchanged. Full producer binding remains pending.

Required failing-before command (two added regressions): `python -m pytest tests/unit/test_forecast_logistic.py::test_d34_confidence_is_one_minus_weighted_uncertainty_before_after tests/unit/test_engine_context.py::test_d33_gate10_native_normalized_half_before_after -q` => **2 failed in 0.43s**, specifically .5 gate refusal and max-component confidence. Same command after correction => **2 passed in 0.21s**. Logs outside repository: cp14-d33-d34-before.log and cp14-forecast-after.log.

Expanded after command: context, forecast, setup-gates, plan-bridge, context-to-trade and ops-paper-loop batteries, excluding only long G2 => **248 passed, 1 deselected, 13 warnings in 35.38s**. No full-suite/final-closeout claim.

### ADR-CP14-008 — D33 / ISSUE-CP14-037

Decision: Native minimum-veto plus exponentially weighted window quality supplies both q_raw and data_trust, with the separate full-window gate2 preserved and unknown provenance refusing. Implementation: engine_context.window_quality_projection. ISSUE-CP14-037 CLOSED-with-projection-evidence; source integration pending. Tests in test_engine_context cover the matching d33 cases, including failure boundaries, not only happy paths; no frozen changes.

### ADR-CP14-009 — D33 / ISSUE-CP14-038

Decision: MTF compares native E09 directions across base and required coarser cells: opposing signs CONFLICTING, all nonzero equal ALIGNED, otherwise neutral without opposition PARTIALLY_ALIGNED, with missing/stale refusal, top-TF vacuity and the existing E07 state-to-score mapping. Implementation: engine_context.mtf_projection. ISSUE-CP14-038 CLOSED-with-projection-evidence; source integration pending. Tests in test_engine_context cover the matching d33 cases, including failure boundaries, not only happy paths; no frozen changes.

### ADR-CP14-010 — D33 / ISSUE-CP14-039

Decision: Admitted evidence supplies binary supporting presence and minimum native component quality, known-absent optional components contribute nothing, the five forecast s-features use unweighted E01/E02/E03-volume/E05/E06 presence, trend_stack is native E09 bias and E07 average quality is the mean of actual confirmation contributors, never 1.0/.9 defaults. Implementation: engine_context.component_projection / confirmation_quality / forecast_features. ISSUE-CP14-039 CLOSED-with-projection-evidence; source integration pending. Tests in test_engine_context cover the matching d33 cases, including failure boundaries, not only happy paths; no frozen changes.

### ADR-CP14-011 — D33 / ISSUE-CP14-040

Decision: Only admitted confirmed still-valid native pattern hits compete: opposing directions refuse as PATTERN_SELECTION_AMBIGUOUS; same-direction hits rank by latest confirmation, native strength then lexical pattern ID, with research-only patterns never promoted. Implementation: engine_context.select_native_pattern. ISSUE-CP14-040 CLOSED-with-projection-evidence; source integration pending. Tests in test_engine_context cover the matching d33 cases, including failure boundaries, not only happy paths; no frozen changes.

### ADR-CP14-012 — D33 / ISSUE-CP14-041

Decision: Numeric temporal_window_validity projects native VALID to 1 and DEGRADED/INVALID to 0, refuses missing/unknown categories and is distinct from temporal_core_flag. Implementation: engine_context.temporal_validity_projection. ISSUE-CP14-041 CLOSED-with-projection-evidence; source integration pending. Tests in test_engine_context cover the matching d33 cases, including failure boundaries, not only happy paths; no frozen changes.

D33 projection verification: `pytest tests/unit/test_engine_context.py -k d33 -q` => **28 passed, 129 deselected in 0.35s**. Expanded context/forecast/gates/bridge/ops batteries excluding only long G2 => **255 passed, 1 deselected, 13 warnings in 37.02s**. Raw log /home/user/cp14-d33-projections.log. These helpers do not complete store-backed G1 or serve binding.

### ADR-CP14-015 — D34 / ISSUE-CP14-044

Decision: ADV is mean daily base-asset volume from CLOSED 1h bars over the previous 30 complete UTC days, costs use actual round-trip fees and side-specific nonnegative funding across publicly verified settlement intervals within the governed holding horizon, endpoint/schedule failure yields FUNDING_UNAVAILABLE for retry, and unavailable spread retains cost_R >= 0.05. Implementation: engine_context.adv_base_volume / forecast_cost_projection / collect_public_funding_schedule. ISSUE-CP14-044 CLOSED-with-projection-evidence; full producer/account/serve integration remains pending. Network-facing funding tests use the actual public client against tests.fake_toobit_responder, not the live venue. No frozen changes.

### ADR-CP14-016 — D34 / ISSUE-CP14-045

Decision: ATR_cap is 1/native E04 ATR14 with ATR <= EPS refused, native sizing produces only a read-only request bound, proposed_notional includes quantity times entry times contract_multiplier, new entries are risk-increasing and all vetoes still precede authorization. Implementation: engine_context.native_size_request. ISSUE-CP14-045 CLOSED-with-projection-evidence; full producer/account/serve integration remains pending. Network-facing funding tests use the actual public client against tests.fake_toobit_responder, not the live venue. No frozen changes.

### ADR-CP14-017 — D34 / ISSUE-CP14-046

Decision: Each PIT account view shares one fresh last-CLOSED 1m close per held symbol labeled PAPER_CLOSE_MARK, never an exchange mark or entry-price fallback, and missing/stale marks invoke D29 refusal. Implementation: engine_context.paper_close_marks. ISSUE-CP14-046 CLOSED-with-projection-evidence; full producer/account/serve integration remains pending. Network-facing funding tests use the actual public client against tests.fake_toobit_responder, not the live venue. No frozen changes.

### ADR-CP14-018 — D34 / ISSUE-CP14-047

Decision: Risk-increasing requests require consecutive CLOSED same-cell states under matching versions and uncertainty rises iff raw entropy or native conflict restriction rank strictly increases, with equal values not rising and conflict uncertainty bound to D27. Implementation: engine_context.uncertainty_trend. ISSUE-CP14-047 CLOSED-with-projection-evidence; full producer/account/serve integration remains pending. Network-facing funding tests use the actual public client against tests.fake_toobit_responder, not the live venue. No frozen changes.

### ADR-CP14-019 — D34 / ISSUE-CP14-048

Decision: Realized loss is max(0,-net period completed PAPER P/L)/current D29 capital over UTC days and ISO UTC weeks, nonnegative completed outcomes break the streak, and immutable-outcome replay preserves threshold-crossing circuit latches until their existing UTC/OWNER reset conditions are satisfied. Implementation: engine_context.realized_loss_projection. ISSUE-CP14-048 CLOSED-with-projection-evidence; full producer/account/serve integration remains pending. Network-facing funding tests use the actual public client against tests.fake_toobit_responder, not the live venue. No frozen changes.

D34 expanded verification: context, forecast, risk-kernel, plan-bridge and ops-paper-loop batteries excluding only long G2 => **310 passed, 1 deselected, 13 warnings in 35.06s**. Log: /home/user/cp14-d34-expanded.log. The initial new sizing test compared native floating arithmetic to exact 1.2; corrected the test to approximate equality, not the native quantity or sizing machine. Tests cover attention cap/step/multiplier, marked account time/scope, strict uncertainty history, 720-hour ADV/unit/PIT, fee/funding schedule/side/floor, actual client with fake responder, loss netting/denominator/zero outcome/UTC boundaries and breach persistence despite recovery.

[ISSUE-CP14-049] severity: MAJOR | status: CONSERVATIVE REFUSAL IMPLEMENTED (D34 continuation rule; no owner stop). A: funding costs require a verified public rate, interval units and settlement phase. B: the frozen get_funding_rate helper exports only (rate, alert); the actual public deployment's interval/phase availability is not yet measured. Rule: no assumed 8h or guessed schedule. Interim: additive unsigned same-endpoint raw read accepts explicit fundingIntervalHours/fundingIntervalSeconds and nextFundingTime with the original source payload, otherwise FUNDING_UNAVAILABLE and retry on next preparation; no frozen client edit. Public response-schema availability must be listed in the PR/phone acceptance, not claimed from the network-free test. Needs from owner: none; continue under D34.

### G1 native E07 contributor-quality integration (D33 / ADR-CP14-010)

complete_engine_bundle now supports the explicit derive_avg_quality path: actual upstream E01/E02/E03/E05 evidence is joined to the native confirmation list by engine, condition and confirmation timestamp, deduplicated by evidence identity, then averaged with the governed D33 helper. The returned bundle retains all contributor evidence/snapshot identities and the measured average. Missing matching evidence or quality refuses as E07_CONFIRMATION_QUALITY_UNAVAILABLE; no .9 fallback or dependency on later E07/E08 outputs. The real-store native twelve-engine roundtrip test now uses derived quality at BOTH 90-hour and 180-hour boundaries, including preserved terminal E05 QX. Its MTF input remains explicit test data; this is still not the complete 38+23 source acceptance.

Actual context/plan-bridge/ops-paper-loop command excluding only long G2 => **192 passed, 1 deselected, 13 warnings in 37.03s**. Raw log /home/user/cp14-e07-integration.log. No frozen change; immediate green-step commit/push.

### Interim clean-tree full suite after D32–D34 projections and E07 integration

Started with `test -z "$(git status --porcelain)"` at e9d01a9, then actual `/home/user/cp14-tools/.venv/bin/python -m pytest -q` => **2904 passed, 14 warnings in 627.84s (0:10:27)**, no deselections, including the long actual-store G2 two-process determinism proof. Log: /home/user/cp14-d32-d34-interim-full.log. This is an INTERIM regression run, not either final closeout run: real complete get_bridge_context/cache, serve source binding and HANDOFF_CP14 remain unfinished. Immediate result-record commit/push keeps this green step under 15 minutes. No runtime classifier is created or committed.

### CP-14 closeout A — measured SQLite quality / native coarser E09 source checkpoint

- D33 / ISSUE-CP14-037: added append-only, content-bound quality-plane measurements through the existing public snapshot API. The reader verifies raw identity, explicit measured metadata/flags/receipt provenance and native `calc_quality_vector` readback before the native minimum-veto/window-weighted projection. Raw-only rows refuse `QUALITY_PROVENANCE_UNAVAILABLE`; historical constructor defaults are never promoted to measured health. No frozen schema, ingestion or DDL changes.
- D33 / ISSUE-CP14-038: `mtf_inputs` obtains base/intermediate/HTF windows from SQLite and computes native E09 states; it supplies the existing `mtf_projection` with actual biases, close times and measured receipt freshness. Missing coarser windows refuse. The first positive test correctly refused because its fixture omitted native `relative_mtf(1h)`'s required 8h window; the fixture was corrected, not the engine/MTF policy.
- D34 / ISSUE-CP14-044: the holding-end reader consumes the SL-12 governed `H_max(tf)` entries (1h=24, 1m=12); all undocumented TFs use the strictest documented bound, 12, with calendar-aware closes. No local playbook 16-bar value substitutes for this governance input.
- Actual focused regression: `197 passed, 1 deselected in 43.68s` (source tests, engine-context tests and plan-bridge tests; only the long G2 two-process training test deselected). Earlier new-source run: `1 failed, 27 passed in 3.40s` (the fixture HTF omission above). A preliminary command named a nonexistent unit paper-loop path and collected no tests; no result claimed for it.
- This is source-reader evidence, NOT complete 38+23-key G1 acceptance, serve integration, a phone PASS, or either final suite. Normal raw-only ingestion does not yet publish the measured quality records; absence remains a named refusal rather than manufactured provenance. Full producer/account/cost/prior-state composition remains in progress.

### CP-14 closeout A — native bundle with no injected E07 inputs

`prepare_engine_bundle(symbol,timeframe,as_of)` now has a store-derived path: omitted `rtm_context` reads measured quality and all required native coarser E09 states, then derives E07 contributor quality. The explicit legacy seam remains for existing isolated tests; the new source test does not pass it. Native E04 stream history is retained for D32's prior-HV quantile rather than replacing it with the last 300 states. Coarser latest-CLOSED coverage is checked separately from D14 receipt freshness (`MTF_INSUFFICIENT` if the required frontier is absent).

Actual SQLite fixture evidence: 180-hour ETH series plus raw coarser histories and explicit quality-plane measurements; existing `load_classifier` reads the test-only artifact. Pass-through spies establish actual E01→E02→E12→E04→E03→E10→E09→E05→E06→E11→E07→E08 calls, not just a returned order list. Every emitted event is validated and read back in full; native E05 terminal QX remains present. Non-emitting native engines are not padded with invented evidence. Early test failures identified an over-strong all-engines-must-emit assertion and an unobserved direct E05 driver call; the final test checks all actual calls and all actual emissions, including zero-emission engines.

Actual regression: `221 passed, 1 deselected, 13 warnings in 91.32s (0:01:31)` (source, engine-context, plan-bridge and existing paper-loop tests; only long G2 deselected). Source-only run: `29 passed in 45.44s`. This still is not complete 38+23-key producer/serve acceptance or either final suite.

### CP-14 closeout A — durable account / mark / ADV / ladder sources

**[ISSUE-CP14-050] PAPER_CLOSE_MARK decimal preservation.** A: the earlier mark helper converted the persisted Decimal close through binary float before D29 reservation arithmetic; the real-store test exposed `401.2` versus four times the exact persisted close. B: retain the original Decimal after finite/positive validation. Rule: D29 exact durable fraction and D34 CLOSED-close mark, no rounding or clipping. Interim/final: CLOSED with the actual mark/account regression; native risk arithmetic and LIVE paths unchanged. Owner need: none.

**[ISSUE-CP14-051] Canonical correction / authenticated reset source gap.** A: native append-only correction events identify superseded records, but no authenticated durable OWNER-review binding is supplied to this producer; an `actor=OWNER` string cannot authenticate a reset. B: PAPER/unknown-scope unresolved corrections refuse `PAPER_CORRECTION_UNAVAILABLE`; purported PAPER/unknown-scope reset/review events and emergency downgrades lacking durable authentication refuse `CIRCUIT_RESET_UNAVAILABLE`. Explicit non-PAPER corrections/reviews are not used to reset PAPER losses. Rule: D34/048 immutable canonical completed outcomes and authenticated native latch resets; no silent correction/drop/profit reset. Interim: conservative source refusal, not completed reset support; disclose in final PR limitations. Owner need: none for this refusal; no new reset authority requested.

**[ISSUE-CP14-052] Public volume-unit declaration availability.** A: the public-facts response must establish whether 1h volume is BASE or CONTRACT; a schema field cannot be assumed available on the live venue. B: `adv_input` accepts an explicit public `volumeUnit` in the identity-bound response, applies the actual contract multiplier only for CONTRACT, and otherwise refuses `ADV_UNAVAILABLE`. Rule: D34/044 verified base-asset volume, exactly 720 contiguous CLOSED 1h bars over the prior 30 complete UTC days. Interim: positive actual-store/fake-public-client proof, not a claim that live Toobit supplies this field; disclose absent-unit refusal in the PR. Owner need: none.

- Added real-source readers: `paper_marks` shares one measured PIT last-CLOSED 1m mark set; `paper_account_inputs` filters the durable ledger to as_of, joins canonical completed PAPER outcome rows, runs native D29 reservations and D34 loss/latch replay, and refuses unidentified orders/fills/missing marks; `ladder_input` reads actual native append-only revisions with parent/ratchet checks rather than bootstrapping NORMAL; `adv_input` reads all actual 720 prior-period volumes and their immutable identities.
- Positive fixture account proof uses LedgerWriter and SQLite, actual partially filled entry reservations, native completed-outcome storage, measured close marks, public facts via the required fake responder, exact Decimal margin and loss fractions, future-review exclusion and unauthenticated-review refusal. Empty known ledger is measured empty state, not an absent-ledger fallback. Native migration/revision APIs supply the ladder fixture; no new schema or raw mutation.
- Actual expanded regression: `229 passed, 1 deselected, 13 warnings in 92.27s (0:01:32)`; only long G2 excluded. Initial focused source runs each had 2 failures (test API assumptions plus the mark precision defect above); final focused rerun `3 passed, 29 deselected in 1.37s`. This is not complete 38+23-key producer/serve acceptance or either final suite. Final controls must include ISSUE-CP14-050..052 as well as the requested 001..048 status table.

### ADR-CP14-020 — native decision-view composition (PAPER only)

**[ISSUE-CP14-053] Native CONFIRMED facts versus ACTIVE-only fabric.** A: E01's native emitted Q2/Q3 facts are CONFIRMED; the unchanged fabric admits only ACTIVE. B: the PAPER producer records a separate SL-14 admission transition for a VALID, non-QX, PIT-confirmed emitted fact and applies the native forward transition machine in its decision view; native 24-field records remain unchanged. Rule: AI.4 defines CONFIRMED as stable CLOSED/repeatable inputs and ACTIVE as current decision use. This does not promote CANDIDATE, alter the E01 swing-depth/pruning algorithm, or reactivate terminals. E05 terminal/filled zone fates are propagated conservatively. Interim: immutable admission snapshot plus identity-bound bridge transport, without a LIVE change. Owner need: none; implementation crosswalk, not relaxed admission.

**[ISSUE-CP14-054] Native age fields versus elapsed decision age.** A: native E09/E12 emit sample/evidence counts in `age`; copying those counts into elapsed-bar freshness expires newly emitted facts immediately. B: retain native fields exactly in public-store readback; the PAPER decision view derives elapsed CLOSED bars from each actual event_time to the decision close boundary, refuses future event time, and the bridge independently checks the derivation. Rule: Ch.8/SL-14 five-timeframe elapsed expiry; no lifetime extension, admission of future facts, or mutation of frozen evidence fields. Interim: identity-keyed derived ages in bridge transport; native terminal states and the unchanged fabric expiry still apply. Owner need: none; dimensional projection, not a new expiry threshold.

**[ISSUE-CP14-055] Native lineage tokens rejected by gate 11.** A: complete native lineage includes engine-specific tokens outside the unchanged setup gate's accepted identifier grammar. B: preserve the full native lineage and fail closed with `SETUP_NOT_EMITTED` / `GATE11_LINEAGE_UNRESOLVED`; do not strip tokens or weaken the validator to make a fixture trade. Rule: native lineage preservation and unchanged downstream validators. Interim: conservative plan refusal disclosed alongside the genuine fixture's `GATE5_MTF_CONFLICTING`; producing all 61 fields is not permission to execute. Owner need: none for refusal; no new mapping authority requested.

- New test file `tests/integration/test_cp14_producer.py` is the real SQLite/native composition proof. Input-only OHLCV, fake unsigned public venue responses, an initialized empty PAPER ledger, native ladder revision, and a test-only low-entropy classifier through `load_classifier`; no supplied evidence member, component, plan, or runtime classifier artifact. Native E06/E07/E08/E10 execute but emit no events for this particular series; the other eight emit and every field is read back through the public evidence interface.
- E12's native temporal-profile builder now consumes strictly prior observations for runtime composition. Insufficient native history remains Q1; no quality override. Feature-timeline streaming and historical OI lag share the actual source metadata. The first complete native cycle records uncertainty then refuses without a prior state; the consecutive cycle derives the comparator from storage.
- Diagnostic full-source composition produced exactly 38 context and 23 risk fields; cold persisted replay was canonical-JSON identical. Expanded pre-proof regression: `282 passed, 1 deselected in 86.62s (0:01:26)`. Initial seven-test proof: `1 failed, 6 passed in 221.17s (0:03:41)` because its assertion incorrectly required every called engine to emit, rather than verifying every emitting engine. That assertion is corrected to the actual native emitting set, with no synthetic member and no production change to force emission. Final verification remains pending.
- Corrected real-source seven-test proof plus expanded context/bridge/risk regression: `289 passed, 1 deselected in 305.38s (0:05:05)`; only long G2 excluded. Not either final twice-clean full-suite run.

### ADR-CP14-021 — PAPER serve binding and authorized phone fallback

`serve` constructs `EngineContextProducer` on its actual runtime store/ledger and binds `get_bridge_context` and `prepare` separately. Expensive preparation remains before scheduler budgets. PAPER uses the post-catch-up receipt frontier for all stage reads while scheduling retains calendar close_ms; LIVE keeps its previous stage timestamps. Per-cell bridge refusals retain their actual name rather than becoming NO_PLAN_FOR_CELL. Catch-up and D22 retry behavior remain independent of D14 freshness.

The existing PAPER-loop composition harness now runs one cycle on the real 38+23 SQLite fixture with native BootstrapService.catch_up, fixture clock, the existing fake venue adapter, and a stub notification/Telegram surface. Its classifier is a test-only temporary file passed through the existing `load_classifier` path; runtime composition has no classifier injection or fallback. JSON asserts catch_up, cell_runs, preparation, receipt as_of, and the actual SETUP_NOT_EMITTED refusal. No live venue/phone acceptance is claimed. Test runs: `188 passed, 1 deselected, 13 warnings in 41.45s`; `13 passed, 13 warnings in 223.11s (0:03:43)`.

Closeout's explicit phone fallback authorizes opt-in `15m,30m,1h,2h,4h` with `--max-minutes 90`; defaults stay `1h,4h` and 20 minutes, Core-10 and all-nine-class refusal stay unchanged. The later owner closeout instruction supersedes the earlier two-timeframe-only explicit scope, not its defaults. The scope test now uses unauthorized 8h as its negative case and separately verifies all five allowed opt-in intervals. `/params/e11_classifier_v1.yaml` is gitignored; the fixture remains tracked. This is an additive ignore/allowlist change, never permission to commit a runtime artifact.

**[ISSUE-CP14-056] Historical measured-quality publication gap.** A: ordinary raw ingestion has no identity-bound historical source-health/validation/receipt record; constructor defaults are not measurements. B: the producer's public publication/readback seam requires explicit measured provenance and refuses raw-only histories as `QUALITY_PROVENANCE_UNAVAILABLE`; native quarantined receipt quality remains quarantined. Rule: D33 minimum veto, D14 actual receipt, and the already-governed source-audit gap 1 above. Interim: successful input-only quality fixture proof, but no backfilled health=1 or fabricated historical receipt and no phone readiness claim. Owner need: none; conservative source refusal, no historical waiver requested.

**[ISSUE-CP14-057] Native ladder source absent at a new PAPER store.** A: startup's empty-table NORMAL text is not a complete durable ladder/budget revision. B: `RISK_LADDER_UNAVAILABLE` until an actual native revision is present; fixture uses the native migration/append interface, never an injected risk dict. Rule: D34 and source-audit gap 6; no bootstrap ladder authorization. Interim: conservative runtime refusal; CP-15 execution remains out of scope. Owner need: none.

**[ISSUE-CP14-058] Native floating-point quality upper bound.** A: native Q_raw summation at exactly zero delay may produce 1.0000000000000002. B: producer range validation refuses rather than clipping; the genuine positive fixture records an actual 1ms receipt delay and recomputes native quality. Rule: finite [0,1], unchanged native validators, no manufactured clamp. Interim: named range refusal at the numerical edge, not a native-quality-policy change. Owner need: none.
- Risk applicability/type, fallback/defaults, foundational allowlist regression: `263 passed, 1 deselected in 40.06s`; not a final full-suite result. No new frozen or LIVE-path change in this increment.

### CP-14 final control reconciliation (2026-09-19)

Historical milestone statuses above retain their dates; the following final disposition table supersedes their pending language. No new owner policy is inferred. Final suite results are populated in HANDOFF/board only from actual clean runs. D21–D34 OWNER ANSWERS remain verbatim (plain file names, not accidental URL markup); D30 defaults remain authoritative with the later explicit closeout fallback recorded in ADR-021. The read-only owner archive hash is unchanged.

| Issue | Final disposition | Subject / evidence |
|---|---|---|
| ISSUE-CP14-001 | CLOSED — implementation/evidence | Independent nine-class first-training labels |
| ISSUE-CP14-002 | CLOSED — implementation/evidence | Catch-up versus freshness |
| ISSUE-CP14-003 | CLOSED — implementation/evidence | Snapshot mapping bind bug |
| ISSUE-CP14-004 | CLOSED — implementation/evidence | CIRCUIT_OPEN registry schema |
| ISSUE-CP14-005 | CLOSED — implementation/evidence | PAPER/LIVE balances |
| ISSUE-CP14-006 | CLOSED — implementation/evidence | E03 climax OHLC calibration |
| ISSUE-CP14-007 | CLOSED — implementation/evidence | Missing OI participation |
| ISSUE-CP14-008 | CLOSED — implementation/evidence | Governed caps/P_min and storage |
| ISSUE-CP14-009 | CLOSED — implementation/evidence | Two explicit extra pin corrections / VOID clicks |
| ISSUE-CP14-010 | CLOSED — implementation/evidence | Provisional ATR20 discarded |
| ISSUE-CP14-011 | CLOSED — implementation/evidence | Canonical ATR14 and native liquidity projections |
| ISSUE-CP14-012 | CLOSED — implementation/evidence | Complete native event envelope persistence/readback |
| ISSUE-CP14-013 | CLOSED — implementation/evidence | Close-stamped engine views and local sequence/availability alignment |
| ISSUE-CP14-014 | CLOSED — implementation/evidence | Authorized YAML cleanliness before commit |
| ISSUE-CP14-015 | UNASSIGNED | No issue was recorded under this number; not an invented closure or open decision. |
| ISSUE-CP14-016 | UNASSIGNED | No issue was recorded under this number; not an invented closure or open decision. |
| ISSUE-CP14-017 | CLOSED — implementation/evidence | PAPER bootstrap governance/public facts |
| ISSUE-CP14-018 | CLOSED — implementation/evidence | D29 reservation margin |
| ISSUE-CP14-019 | CLOSED — implementation/evidence | Uncertainty complement |
| ISSUE-CP14-020 | UNASSIGNED | No issue was recorded under this number; not an invented closure or open decision. |
| ISSUE-CP14-021 | UNASSIGNED | No issue was recorded under this number; not an invented closure or open decision. |
| ISSUE-CP14-022 | UNASSIGNED | No issue was recorded under this number; not an invented closure or open decision. |
| ISSUE-CP14-023 | UNASSIGNED | No issue was recorded under this number; not an invented closure or open decision. |
| ISSUE-CP14-024 | UNASSIGNED | No issue was recorded under this number; not an invented closure or open decision. |
| ISSUE-CP14-025 | CLOSED — implementation/evidence | Strict PAPER margin / unchanged inclusive LIVE |
| ISSUE-CP14-026 | CLOSED — implementation/evidence | Scoped liquidity Method-A degenerate reference |
| ISSUE-CP14-027 | CLOSED — implementation/evidence | Raw identity/PIT metadata |
| ISSUE-CP14-028 | CLOSED — implementation/evidence | Delayed structural-label confirmation |
| ISSUE-CP14-029 | CLOSED — implementation/evidence | E03 calibration source-index binding with late receipts |
| ISSUE-CP14-030 | CLOSED — implementation/evidence | Native E04 chronological state/history |
| ISSUE-CP14-031 | CLOSED — implementation/evidence | Native E01 prefix memoization |
| ISSUE-CP14-032 | CLOSED — implementation/evidence | Bounded default training scope |
| ISSUE-CP14-033 | CLOSED — implementation/evidence | E10 explicit close boundary |
| ISSUE-CP14-034 | CLOSED — implementation/evidence | D31 terminal QX validator |
| ISSUE-CP14-035 | CLOSED — implementation/evidence | Complete E11 state versus consumer label |
| ISSUE-CP14-036 | CLOSED — implementation/evidence | Prior native HV mid-rank |
| ISSUE-CP14-037 | CLOSED — implementation/evidence | Native window minimum-veto/weighted quality |
| ISSUE-CP14-038 | CLOSED — implementation/evidence | Native required-coarser MTF |
| ISSUE-CP14-039 | CLOSED — implementation/evidence | Admitted components and real E07 contributor mean |
| ISSUE-CP14-040 | CLOSED — implementation/evidence | Deterministic admitted pattern selection |
| ISSUE-CP14-041 | CLOSED — implementation/evidence | Native temporal validity projection |
| ISSUE-CP14-042 | CLOSED — implementation/evidence | Normalized forecast gate-10 quality |
| ISSUE-CP14-043 | CLOSED — implementation/evidence | Versioned PAPER bootstrap uncertainty |
| ISSUE-CP14-044 | CLOSED — implementation/evidence | Actual ADV/fees/public funding horizon |
| ISSUE-CP14-045 | CLOSED — implementation/evidence | Native request-only sizing and reciprocal ATR |
| ISSUE-CP14-046 | CLOSED — implementation/evidence | PIT PAPER_CLOSE_MARK with Decimal fidelity |
| ISSUE-CP14-047 | CLOSED — implementation/evidence | Consecutive version-matched uncertainty trend |
| ISSUE-CP14-048 | CLOSED — implementation/evidence | Net realized losses/streak/latches |
| ISSUE-CP14-049 | CLOSED — conservative refusal | Absent public funding interval |
| ISSUE-CP14-050 | CLOSED — implementation/evidence | Decimal close-mark preservation |
| ISSUE-CP14-051 | CLOSED — conservative refusal | Corrections/authenticated reset source gap |
| ISSUE-CP14-052 | CLOSED — conservative refusal | Public base/contract volume-unit declaration |
| ISSUE-CP14-053 | CLOSED — implementation/evidence | Confirmed native fact decision admission |
| ISSUE-CP14-054 | CLOSED — implementation/evidence | Elapsed decision age versus sample count |
| ISSUE-CP14-055 | CLOSED — conservative refusal | Native lineage token grammar at gate 11 |
| ISSUE-CP14-056 | CLOSED — conservative refusal | Historical measured-quality publication gap |
| ISSUE-CP14-057 | CLOSED — conservative refusal | Absent complete durable risk ladder |
| ISSUE-CP14-058 | CLOSED — conservative refusal | Native floating-point quality upper edge |
| ISSUE-CP14-059 | CLOSED — implementation/evidence | Bounded consumer-only timestamp/native content-hash memo; native values and failures preserved; final suites restarted after interrupted attempt. |

### Recovery verification / ISSUE-CP14-059 — repeated parsing overhead

A: the fresh sandbox's first full-suite attempt reached a failure at the G2 test and was terminated at the 899-second command bound before a final traceback/summary; one training artifact existed in its temporary fixture directory, the second did not. This is NOT either accepted final run. B: a 120-observation profile measured 107,189 timestamp parses; native rolling windows repeatedly parse identical immutable strings and the metadata join computed each observation's identical canonical hash twice. Rule: preserve all native arithmetic, samples, time limits, validators and frozen methods; do not relax the G2 six-minute worker test or omit it. Interim: a bounded, typed consumer-local memo delegates to the identical existing parser, and each metadata-join iteration reuses its one actual content hash. Bootstrap/LIVE parsing is untouched. Parser parity/error tests and all final suites must pass before closure. Owner need: none; implementation-only redundant-work removal, not a policy change. Both final full-suite runs restart after this fix is committed.

- ISSUE-059 focused verification: `205 passed, 1 deselected in 83.71s (0:01:23)` before the additional native-hash memo; final source battery `41 passed in 48.64s`, including strict parser/error parity and Decimal-precision hash parity. A test-local missing Decimal import failed once and was fixed, without a runtime policy change. The hash memo is bounded to 4096 entries and keys the exact typed native six-field hash payload; no frozen method is edited and actual observation hashing remains the authority.

### Final twice-clean verification (2026-09-19)

Code commit 68f04e4; exact command `python -m pytest -q -p no:cacheprovider`, twice sequentially with empty `git status --short` before and after each, all tests including G2, no deselection:

```text
2960 passed, 14 warnings in 934.90s (0:15:34)
2960 passed, 14 warnings in 936.11s (0:15:36)
```

Both commands exited 0. The completed durations exceed the 15-minute step target by 34/36 seconds on this sandbox; this deviation is disclosed, not hidden by omitting tests or relaxing the existing training worker bound. Earlier 899-second tool-bounded attempts have no accepted full summary and are not counted. Exactly two frozen exceptions remain; runtime classifier absence check OK and tracked-artifact grep returns only tests/fixtures/e11_classifier_v1.yaml.

### OWNER ANSWERS to CP-14.1 — D35 (2026-09-19, binding)

Parts 1–2 are transcribed from the issued CP-14.1 instructions with exact normative content (session compaction consumed the original wrappings); part 3 is reproduced as received.

D35 part 1 of 3 (as sent, transcribed): CP-14.1 — resumable phone training for E11, repo sinamoosazadeh/Upstage base main 2448168 (PR #18 merged). Fresh executor preamble: first `git fetch origin && git log --oneline -1 origin/main` must be 2448168 and `git push --dry-run origin <arena branch>`; stop if either fails. Read first: PHASE2_HANDOFF_CP9.md HANDOFF_CP14 (RECORD, INTERFACES, PHONE ACCEPTANCE, LIMITATIONS), PHASE2_DECISION_LOG.md D21–D34 verbatim especially D21/D30, APEX_GEN5.md E11 §3.3, apex/ops/engine_context.py, scripts/run_apex.py train-e11. Laws: Persian chat / English files; repo = plan files only; frozen read-only: apex/data_catalog/**, apex/research/bootstrap.py, apex/research/backtest.py, six original params/*.yaml, PROMPT.md — no exceptions this stage; never commit params/e11_classifier_v1.yaml, data/, logs, venvs; no engine formula, label rule, class count or threshold change; owner questions only via ISSUE with A/B and options, only if frozen file must change or rule contradicts D1–D35. Owner-observed phone fact: Termux/proot python 3.12, 140 cells, ~3500 CLOSED bars/cell, `train-e11 --json` D30 defaults printed one `TRAIN_CELL cell=BTCUSDT:1h closed_bars=3522 eligible_samples=0 elapsed_seconds=1176.762` then `{"artifact_written": false, "reason": "TRAINING_TIME_LIMIT", "status": "REFUSED"}` — one cell unfinished in 19.6 min (~0.33 s/bar), 20-min limit discarded all work, 50-cell fallback infeasible.

D35 part 2 of 3 (as sent, transcribed): CP-14.1 (part 2 of 3) — OWNER DECISION D35, binding. (a) RESUMABLE TRAINING: persist each completed cell's finalized sample set (feature vectors, D21 labels, as_of, exclusion counts, per-cell input hash) under data/e11_train_cache/<training_query_sha256>/<symbol>_<timeframe>.json (data/ is gitignored; never in Git). On rerun with the same protocol hash, completed cells are loaded, not recomputed; a cell whose inputs changed is recomputed. --max-minutes now bounds ONE invocation: on timeout keep all completed cells, print REFUSED with reason TRAINING_TIME_LIMIT plus cells_completed, cells_remaining, next_cell; the artifact is written only when every scoped cell is complete and all nine classes are present (D21 unchanged). (b) NEW FLAG --max-bars-per-cell N: each cell uses only its latest N CLOSED bars (default unlimited = D30 unchanged); N enters training_window and the protocol hash. (c) NEW FLAG --profile: per cell, a stderr line with cumulative seconds per engine E01..E11 and per stage (window read, label confirmation, fit), plus exclusion counts by named reason; independent of --json stdout. (d) LIVENESS: a progress line at least every 250 bars (cell, bars_done/bars_total, elapsed_seconds), not only at cell end. (e) PERFORMANCE: build a 3500-bar single-cell synthetic scratch store (outside Git), measure seconds/bar with cProfile before any change, then reduce it using ONLY exact-parity techniques (per-invocation memoization, incremental native state as in ISSUE-030/031, avoiding repeated window re-reads/re-parsing); target ≤0.05 s/bar in the sandbox; report before/after numbers; every optimisation needs a parity test proving canonical JSON of engine outputs and the resulting artifact bytes equal an unoptimised reference. (f) DETERMINISM: the existing two-process G2 proof must pass cold-cache, and a new test must prove warm-cache (resumed) training yields byte-identical artifact and hash to cold-cache. (g) Timeout test: partial run leaves cache files only, no artifact; a second run completes from cache. Part 3 follows.

D35 part 3 of 3 (as received): CP-14.1 (part 3 of 3) — controls and closeout. Docs: DECISION_LOG D35 verbatim (parts 1–3 as sent) + ISSUE-CP14-059 (A: D30 defaults infeasible on the target phone, evidence line above; B: non-resumable trainer discards work on timeout) + ADR-CP14-022; E11 §3.3 one sentence after the D30 sentence (resumable per-cell cache, bar cap recorded in training_window, artifact unchanged in schema) + one AJ row; §9.5 tree gets `data/e11_train_cache/` marked gitignored; HANDOFF_CP14.1 in PHASE2_HANDOFF_[CP9.md](http://CP9.md) with ARTIFACTS / RECORD (before/after s per bar, both suite totals) / INTERFACES (flags, cache layout, exit codes 0 TRAINED, 2 REFUSED with reasons TRAINING_TIME_LIMIT | EMPTY_CLASS:<name>, 1 error) / PHONE ACCEPTANCE: exact owner commands in order with expected output and stop conditions — `termux-wake-lock` (in the Termux shell, before proot), then `.venv/bin/python scripts/run_apex.py train-e11 --max-bars-per-cell 1000 --max-minutes 25 --profile --json` repeated until TRAINED, then if EMPTY_CLASS the same with 2000 bars, then `serve --cycles 1 --interval 5 --json`, then `status`; board CP-14.1 line; MATRIX row. Verification: `git status` clean; `git diff 2448168 --stat -- apex/data_catalog apex/research params/ PROMPT.md` must be EMPTY (paste it); classifier absent from tree; full suite twice `python -m pytest -q -p no:cacheprovider`, raw lines pasted, equal green counts. Then ONE PR to main titled "[CP-14.1] resumable bar-capped E11 training with profiling and exact-parity speedups", body: D35, ISSUE-059, before/after s per bar, both suite lines, phone acceptance steps; paste `gh pr view --json number,url,headRefOid,mergeable`; record number in HANDOFF and board, commit, push. Final report: PR URL, head sha, both suite lines, empty frozen diff, "no uncommitted work remains". Do not pause for progress reports. D1–D35 binding.

- Numbering note: part 3 designates the resumability record as ISSUE-CP14-059, but that number is already CLOSED in the CP-14 closeout above (bounded timestamp/content-hash memo). Duplicating the number would corrupt traceability, so the instructed A/B is recorded verbatim under the next free number, ISSUE-CP14-060; the closeout memo keeps 059.
- [ISSUE-CP14-060] severity: MAJOR | status: IMPLEMENTED, verification pending
  A: D30 defaults infeasible on the target phone, evidence line above (BTCUSDT:1h, 3522 CLOSED bars, 1176.762 s, zero eligible samples, TRAINING_TIME_LIMIT with all work discarded).
  B: non-resumable trainer discards work on timeout (per-cell progress lives only in the killed worker's memory; the 20-minute bound erases it).
  Rule applied: D35 (a)–(g) exactly; D21 labels, nine classes, thresholds, engine formulas and D30 defaults unchanged; no frozen-file change; data/ stays gitignored and uncommitted.
  Interim behavior: per-cell finalized-sample cache under data/e11_train_cache/<training_query_sha256>/ with full consumed-row input identity (cell bars plus prefetched HTF rows); same-hash reruns load completed cells; --max-minutes bounds one invocation and reports cells_completed/cells_remaining/next_cell; --max-bars-per-cell caps each cell (recorded in training_window and the protocol hash); --profile prints per-cell engine/stage timing; liveness ticks every 250 bars; degenerate reason is EMPTY_CLASS:<name>; artifact bytes are written only when every scoped cell completes with all nine classes present.
  Needs from owner: none; D35 is the authority.

### ADR-CP14-022 — CP-14.1 resumable training surface

Governed additive paths and contracts (ADR-P2-003): `data/e11_train_cache/<training_query_sha256>/<symbol>_<timeframe>.json` (gitignored via ADR-P2-013, never committed) carries finalized samples (feature vectors in E11.VECTOR_KEYS order, D21 labels, as_of), per-cell exclusion counts, and the full consumed-row input hash; any schema/input mismatch means recompute, never partial reuse. The spawn-isolated worker owns cache writes under this fixed conventional layout and still never knows the artifact output path; only the parent returns a validated artifact to the atomic writer. CLI gains `--max-bars-per-cell N` (default unlimited) and `--profile`; stderr gains `TRAIN_PROGRESS` (every 250 bars) and `TRAIN_PROFILE`/`TRAIN_PROFILE_FIT` lines while `--json` stdout stays machine-readable and `TRAIN_CELL` keeps its D30 shape. `training_window` gains `max_bars_per_cell` (top-level artifact schema unchanged); degenerate refusals carry `EMPTY_CLASS:<name>`; timeouts carry `cells_completed`/`cells_remaining`/`next_cell`. Every D35(e) speedup must be an exact-parity technique with a committed parity test (canonical engine-output JSON plus resulting artifact/sample bytes equal an unoptimized reference); `optimize=False` preserves that reference path. No engine formula, label rule, class count, threshold, frozen file, or D30 default changes.

### OWNER DECISION D36 (2026-09-21, binding)

D36 (option a): E11 first training accepts an empty TRANSITION class. The D21 delayed-label rule (no BOS/CHoCH confirmation in t+1..t+48 ⇒ TRANSITION) produced 0 TRANSITION members in 1000 real samples (20 cells, N=168, window 2026-09-03T08:00Z to 2026-09-17T15:00Z). TRANSITION is by design a derived state (E11 §1.4: H ≥ θ_H ⇒ TRANSITION by construction; §3.2 tree branch 2), not a learned class. Training therefore refuses only when any of the eight rule-tree classes (CRISIS, EXPANSION, TREND_EXPANSION, TREND_CONTRACTION, TREND, COMPRESSION, CHOP, RANGE) is empty. K stays 9, W stays (9,8), b (9,); artifact schema, TRAINING_QUERY, training_protocol_hash, cell_input_hash and E11_TRAIN_CACHE_FORMAT are unchanged so existing per-cell caches are reused. Runtime E11 is unchanged. A mandatory post-fit entropy/confidence validation report is added. Relabelling TRANSITION as a regime-change neighbourhood is deferred to the streaming-engine checkpoint (D38).

- [ISSUE-CP14-061] severity: MAJOR | status: RESOLVED by D36 (owner decision 2026-09-21)
  TRANSITION unreachable under D21 on real data: the delayed-label rule (no BOS/CHoCH confirmation in t+1..t+48 ⇒ TRANSITION) produced 0 TRANSITION members in 1000 real samples (20 cells, N=168, window 2026-09-03T08:00Z to 2026-09-17T15:00Z), so the nine-class first training could never admit the harvested store.
  Resolution: D36 (option a) — TRANSITION is a derived state (E11 §1.4 / §3.2 branch 2), not a learned class; training refuses only when one of the eight rule-tree classes is empty.
  Needs from owner: none; D36 is the authority.

### ADR-CP14-023 — CP-14.2 eight-class fit rule and validation report

Governed additive contracts (ADR-P2-003): `fit_multinomial` requires the eight rule-tree classes (`FIT_REQUIRED_CLASSES` = every `E11.REGIMES` member except TRANSITION) and refuses `CONFIGURATION_INVALID` "eight rule-tree classes required for fitting" when any is absent; an empty TRANSITION histogram is accepted and K stays 9. `DegenerateTraining`/`train_classifier` raise only when a required class has zero delayed-label members; `refusing_class` is the first such name in `E11.REGIMES` order. The optimizer protocol (seeded init, 2000 full-batch iterations, learning rate 0.2, W (9,8), b (9,)) is untouched, so a nine-class input fits bit-identically to main@c5f0261 (golden W/b embedded in the test with provenance). The new `training_validation(X, labels, W, b)` report computes every sample's H/probabilities with the runtime `E11.compute_logits_softmax` itself (no reimplementation) and carries samples, theta_H, share_H_ge_theta / share_H_ge_0_80 / share_H_lt_0_40 / share_pmax_ge_0_50, H_min/H_median/H_max, pmax_median, per-class {n, share_H_ge_theta, share_pmax_ge_0_50} and a verdict (PASS iff 0.05 ≤ share_H_ge_theta ≤ 0.25 and share_pmax_ge_0_50 ≥ 0.50; rationale: θ_H drives §3.2 branch 2 and the Q3 cascade, p_max drives D34 U_dis = 1 − p_max and hence C = 0.40 + 0.2·p_max in PAPER, so C ≥ C_min (0.50) iff p_max ≥ 0.50). It rides the `train_classifier` report under "validation" and the `train-e11` CLI prints one `TRAIN_VALIDATION` stderr line and writes the full report to gitignored `data/e11_train_report_<UTC stamp>.json` (path printed). The report never enters the artifact: artifact schema, `validate_classifier`, `artifact_sha256`, `TRAINING_QUERY`, `training_protocol_hash`, `cell_input_hash` and `E11_TRAIN_CACHE_FORMAT` are unchanged, so existing per-cell caches are reused. A WARN verdict never blocks the artifact (the owner decides); the exit code stays 0 TRAINED. No engine formula, label rule, class count, threshold, frozen file, or D30 default changes; runtime E11 is byte-identical.

### Planning-agent error register

- #20 — the D21 TRANSITION label rule was made binding (2026-09-17) without validation on real data; discovered 2026-09-21 as 0 of 1000.
- #21 — a 20–30× speed-up was promised for the owner's laptop before its specification (2011 i5-2430M, 4 GB RAM, Windows 10 1803) was known; corrected the same day; the laptop is backup-only.
- #22 — the D25 strictest-anchor recommendation was not checked against the bootstrap P: with the bootstrap forecast (apex/forecast/logistic.py: package None ⇒ p_hat = p_raw = 0.5; apex/ops/engine_context.py never supplies forecast_package) and params/decision_runtime_v1.yaml p_min_tf (0.55 for every timeframe except 1m 0.52 and 1d 0.50; D25), apex/decision/pipeline.py eligibility returns BELOW_P_MIN for every timeframe except 1d, so apex/ops/plan_bridge.py raises DECISION_INELIGIBLE on 130 of 140 cells before any trade. Recorded under ISSUE-CP14-063 (OPEN, awaiting owner decision D46).

### ISSUE-CP14-062 — OPEN (owner decision pending; not implemented)

Design observation: in apex/engines/e11_regime/engine.py `rule_tree_priority`, branch 2 (H ≥ θ_H ⇒ TRANSITION) means a classifier that became confident about TRANSITION (low H) could never yield TRANSITION through the tree; a learned TRANSITION class is self-neutralising under the current §3.2 ordering. Any future relabelling (D38 era) must settle the tree semantics first. Awaiting owner decision.

### ISSUE-CP14-063 — CLOSED by D46 (owner decision 2026-09-21)

PAPER eligibility: with the bootstrap forecast (apex/forecast/logistic.py: package None ⇒ p_hat = p_raw = 0.5; apex/ops/engine_context.py never supplies forecast_package) and params/decision_runtime_v1.yaml p_min_tf (0.55 for every timeframe except 1m 0.52 and 1d 0.50; D25), apex/decision/pipeline.py eligibility returns BELOW_P_MIN for every timeframe except 1d, so apex/ops/plan_bridge.py raises DECISION_INELIGIBLE on 130 of 140 cells before any trade. APEX_GEN5.md L16053 and L16074 declare constant-0.5 PAPER-eligible while L16646 requires P ≥ P_min(tf): a document contradiction. Planning-agent error #22: the D25 strictest-anchor recommendation was not checked against the bootstrap P. Awaiting owner decision D46.
  Resolution: D46 (route two, owner decision 2026-09-21) — in PAPER only, and only while no calibrated walk-forward forecast package exists (build_forecast bootstrap: package None ⇒ p_hat = p_raw = 0.5), the eligibility minimum P is the governed PAPER bootstrap value bootstrap_p_min = 0.50 for every timeframe, stored under paper_bootstrap in params/decision_runtime_v1.yaml. The D25 SL-12 p_min_tf table is unchanged and remains authoritative for LIVE and for PAPER as soon as a calibrated package is present. C_min 0.50 unchanged. LIVE never reads bootstrap_p_min.
  Evidence: apex/ops/engine_context.py validate_paper_bootstrap requires exactly {arbitration_weights, bootstrap_p_min} (missing key or a non-finite/out-of-[0,1] value ⇒ CONFIGURATION_INVALID, no default) and eligibility_p_min returns (0.50, D46_BOOTSTRAP) for PAPER with the bootstrap forecast while LIVE returns the D25 table and load_decision_runtime('LIVE') carries no paper_bootstrap section at all; the producer cell context carries p_min_tf = 0.50 plus p_min_source and records both in the COMPONENTS fact; params/decision_runtime_v1.yaml holds bootstrap_p_min: 0.50. Tests: tests/unit/test_engine_context.py::test_d46_paper_bootstrap_p_min_resolves_the_1h_p_check (PAPER + bootstrap ⇒ p_min 0.50 on 1h and the P check passes; the D25 0.55 reproduces BELOW_P_MIN / INSUFFICIENT_EVIDENCE), ::test_d46_live_never_reads_bootstrap_p_min, ::test_d46_invalid_or_missing_bootstrap_p_min_fails_closed (6 parametrised defects), ::test_d46_p_min_source_is_an_allowlisted_context_provenance_key, and tests/integration/test_cp14_producer.py::test_d46_producer_records_bootstrap_p_min_and_provenance. D25/D28 twin-rule tests are unchanged and green; plan_bridge eligibility logic and its 38-key contract are untouched (p_min_source is a validated producer allowlist key, ADR-CP14-024).
  Needs from owner: none; D46 is the authority.

### OWNER DECISION D46 (2026-09-21, binding)

D46 (route two): resolves ISSUE-CP14-063. In PAPER only, and only while no calibrated walk-forward forecast package exists (build_forecast bootstrap: package None ⇒ p_hat = p_raw = 0.5), the eligibility minimum P is the governed PAPER bootstrap value bootstrap_p_min = 0.50 for every timeframe, stored under paper_bootstrap in params/decision_runtime_v1.yaml. The D25 SL-12 p_min_tf table is unchanged and remains authoritative for LIVE and for PAPER as soon as a calibrated package is present. C_min 0.50 unchanged. LIVE never reads bootstrap_p_min. APEX_GEN5.md Ch.13 (L16053/L16074) and Ch.14 (L16646) are reconciled by one sentence stating this rule.

- Numbering note: D46 is the second route for ISSUE-CP14-063 (route one was to keep the strict D25 table and accept DECISION_INELIGIBLE on 130 of 140 PAPER cells). The planning-agent error #22 above stays recorded; it is not rewritten.
- Session CP-14.3 (parts 1–3, 2026-09-21) carried D46 plus the extended E11 validation fields and the research-only `--fit-study` surface; both are recorded below as ADR-CP14-024 and ADR-CP14-025, and the D46 implementation evidence is under ISSUE-CP14-063.

### ADR-CP14-024 — D46 PAPER bootstrap P minimum: storage and provenance

Governed additive contracts (ADR-P2-003): `params/decision_runtime_v1.yaml` carries `paper_bootstrap.bootstrap_p_min` (a governed value, 0.50) beside the D28 `arbitration_weights`; `validate_paper_bootstrap` accepts exactly those two keys — a missing key, a non-numeric, non-finite or out-of-[0,1] value is `CONFIGURATION_INVALID` with no defaulted 0.50. `eligibility_p_min(policy, timeframe, environment, bootstrap_prior)` is the single decision point: PAPER + bootstrap forecast ⇒ `(bootstrap_p_min, "D46_BOOTSTRAP")`; every other case (LIVE, or PAPER with a calibrated package) ⇒ `(p_min_tf[timeframe], "D25_SL12")`. `load_decision_runtime(environment="LIVE")` never even attaches the section, so LIVE cannot read `bootstrap_p_min`. The producer cell context carries the value as `p_min_tf` plus the new allowlisted provenance key `p_min_source ∈ {D25_SL12, D46_BOOTSTRAP}`, validated by `validate_produced_context`, and both are recorded in the `COMPONENTS` fact together with `c_min`; `plan_bridge.REQUIRED_CONTEXT_KEYS` stays the frozen 38-key contract (the key is not a 39th required key) and the bridge's eligibility logic is unchanged — `apex/decision/pipeline.py` receives exactly the same inputs. The D25 SL-12 `p_min_tf` table, `capital_hard_cap_fraction` and `c_min` are unchanged; `paper_package_binding`'s digest necessarily changes because the governed file changed (that is the documented effect of a governed parameter change, ADR-CP14-005).

### ADR-CP14-025 — E11 validation extensions and the research-only fit-study surface (explicitly non-governing)

Governed additive contracts (ADR-P2-003): `training_validation` returns, in addition to every D36 field and value, `train_accuracy` (share `argmax(probs) == label`), `train_log_loss` (mean `-ln p_label`, with the label probability floored at `E11.EPS` so a saturated misclassification is finite, never `-inf`), `share_h_norm_gt_0_85` (the Gate 7 fail share `H/ln 9 > 0.85`, strict), `H_percentiles` {p10, p25, p50, p75, p90} and `entropy_by_pmax_bucket` (H median over p_max ∈ [0,0.3) [0.3,0.5) [0.5,0.7) [0.7,1.0]; the last bucket is closed at 1.0 because a saturated fit reaches p_max = 1.0 exactly and no sample may fall outside every bucket; an empty bucket is None, never a fabricated median). All of it is read-only over W/b and uses the runtime `E11.compute_logits_softmax`; `fit_multinomial` is byte-for-byte unchanged and its D36 golden bit-identity test stays green. `fit_multinomial_study(...)` is a separate research fitter (seeded, full-batch, with optional L2 on W, inverse-frequency class weights normalised to mean 1 over the present classes, and z-score standardisation folded back into W/b so the runtime contract stays raw-8 → logits) and `run_fit_study` evaluates the fixed, named grid P0..P9 on the D35 cached samples. `train-e11 --fit-study` is RESEARCH-ONLY: it reads the per-cell cache for the same protocol hash (`input_hash=None` is the cache-only mode of `read_cell_cache`; every other D35 guard still applies), refuses `FIT_STUDY_REQUIRES_CACHE` when any scoped cell is not a hit, never recomputes a feature, never writes a classifier artifact, never touches `params/`, prints one `FIT_STUDY` stderr line per variant plus `FIT_STUDY_THETA` (P0) and `FIT_STUDY_REPORT`, and writes gitignored `data/e11_fit_study_<UTC stamp>.json`. NON-GOVERNING: P0 IS the current optimizer protocol (bit-identical to `fit_multinomial`); no other variant becomes the protocol, and no threshold (including θ_H = 0.65) changes, without a further owner decision (D47 track).

### ISSUE-CP14-064 — OPEN (owner decision pending; not implemented)

First real artifact (sha256 0e8c84d81015966bd7777b124dbe54469d3a7cd5261400a7b0f66c2c7b72d748, 1000 samples, 20 cells N=168) validated WARN: share_H_ge_theta 0.835, share_H_ge_0_80 0.766, share_pmax_ge_0_50 0.577, pmax_median 0.538, H_median 1.169; per-class share_pmax_ge_0_50 TREND 0.00, TREND_EXPANSION 0.16, CHOP 0.17, RANGE 0.29. Consequences with the current tree: runtime regime ≈ TRANSITION and Q2/Q3 on most bars; D34 C < C_min on ≈42% of bars. Root causes: linear softmax vs multi-condition rule tree, θ_H = 0.65 never calibrated on real data, fit protocol likely under-converged and class-imbalanced. Awaiting owner decision D47 after the --fit-study results (fit protocol and/or θ_H recalibration — both value-changing, both pre-PAPER).

  Severity: MAJOR | status: OPEN (owner decision pending; not implemented) | Evidence: the owner-observed first real artifact above; the study surface that will inform D47 is ADR-CP14-025 (`train-e11 --fit-study`). No fit protocol, class count, label rule or threshold has been changed by CP-14.3.
